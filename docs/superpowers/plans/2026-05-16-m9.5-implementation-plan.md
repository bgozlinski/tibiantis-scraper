# M9.5 — Hetzner first deploy MVP — Implementation plan

**Data:** 2026-05-16
**Spec:** [`docs/superpowers/specs/2026-05-16-m9.5-hetzner-deploy-design.md`](../specs/2026-05-16-m9.5-hetzner-deploy-design.md)
**Status:** READY (spec accepted, decyzje §3.1-3.7 zaakceptowane przez developera 2026-05-16).

---

## Źródła

- **CLAUDE.md** §10 (Docker stack reguły — multi-stage, single image dla services, healthchecki, volumes), §13.2 (`docker.yml` workflow template z `docker/metadata-action@v5` + `docker/build-push-action@v6`), §12 (zmiana `.github/workflows/` może być w D-task PR gdy minimalna, ale **registry swap to istotna zmiana** — D45 dorzuca jako pierwszy task w milestone'e).
- **Design spec M9.5** — kluczowy dokument referencyjny. Każdy issue body linkuje do spec'a §X.
- **Precedensy z M0-M9:**
  - **M3-D17** — Celery `-P solo` na Windows. M9.5 prod runs Linux container, default prefork OK. Dev workflow capture w `docs/dev-runbook.md` (D48).
  - **M6-D28** — graceful disable handlers przy braku resource. M9.5 `.env` blank vars handled via `env(..., default="")` w base.py — Discord bot bez token'a zostaje up (no crash).
  - **M7-D32** — django-environ `env.int(...)` z empty crashuje (Pułapka H). M9.5 deploy runbook explicit ostrzega operatora "Operator MUSI wypełnić" przy każdym wymaganym secrecie (M9-D43 dorzucił `.env.example` komentarze, M9.5 nie dorzuca nowych env vars).
  - **M7-D33** — `pre-commit clean` przed flagowaniem mypy errors jako blocker (cache po fresh deps). M9.5 dorzuca to do `docs/dev-runbook.md` (D48).
  - **M8-D37/D38** — lazy import dla pre-commit mypy isolated venv. M9.5 NIE dotyczy Python code — tylko infra (workflow YAML + compose + bash script).
  - **M9-D41** — `apps/core/health/` endpoint dorzucony jako foundation dla Docker HEALTHCHECK + Kuma monitor. M9.5 wykorzystuje (Kuma monitor #1 hit'uje `http://web:8000/health/`).
  - **M9-D42** — `Dockerfile` multi-stage. M9.5 NIE zmienia Dockerfile, tylko `image:` references w compose.
  - **M9-D43** — 5 gotchas (`procps` not in slim, inherited HEALTHCHECK, Compose `$VAR` interpolation, MSYS path conversion, `docker compose build` race). M9.5 dorzuca wszystkie do `docs/dev-runbook.md` (D48). Plus M9.5 specific `.env` filling lesson: alphanumeric SECRET_KEY ZAWSZE (Compose v2 interpoluje `$` w VM environment same way co dev).
  - **M9-D44** — `.github/workflows/docker.yml` original (push do ghcr.io). M9.5-D45 modyfikuje 3 lines (registry, secrets vars, permissions).
  - **M9-D44 closure pattern** — feature PR + closure PR od fresh master. M9.5-D48 powtarza pattern (7. raz pod rząd, M1-D8 + M5-D27 + M6-D30 + M7-D35 + M8-D40 + M9-D44).
  - **Conventional commits scope dot** — M9.5 spec commit `docs(m9.5):` był odrzucony przez `conventional-pre-commit` hook (`.` w scope nieprawidłowe). Workaround: `docs(m9_5):` z underscore. M9.5 commits używają `m9_5` jako scope tag.

---

## Pre-flight checklist (przed startem D45)

- [ ] **Docker Hub account** — `https://hub.docker.com` registered + payment NOT required (free public repos)
- [ ] **Docker Hub PAT** — generated z `https://hub.docker.com/settings/security` z scope **Read + Write + Delete** dla `bgozl/tibiantis-scraper` only (NIE account-wide). Zapisany w password manager lub w GitHub Secrets bezpośrednio.
- [ ] **GitHub Secrets** — `gh secret list` confirm `DOCKERHUB_USERNAME` + `DOCKERHUB_TOKEN` przed D45 PR (alternatively, dorzuca w D45 PR sequence)
- [ ] **Hetzner Cloud account** — `https://console.hetzner.cloud` registered + credit card / IBAN setup (CX22 = ~6€/mc billing)
- [ ] **Local SSH key** — `~/.ssh/id_ed25519` existing OR generate: `ssh-keygen -t ed25519 -C "tibiantis-deploy"`. Pubkey ready do upload do Hetzner Cloud Console.
- [ ] **Discord Developer Portal** — bot existing (z M7), token gotowy do paste w `.env` na VM (NIE w spec/runbook!). Plus dev guild ID jeśli używasz `DISCORD_DEV_GUILD_ID`.
- [ ] **GitHub milestone M9.5 utworzony** — `gh api -X POST repos/bgozlinski/tibiantis-scraper/milestones -f title="M9.5 — Hetzner first deploy MVP" -f description="Provision Hetzner VM + hardening + first manual deploy + Uptime Kuma + deploy runbook. 3-4 dni." -f state=open`
- [ ] **`default_workflow_permissions: "write"` z M9-D44** — verify `gh api repos/.../actions/permissions/workflow --jq '.default_workflow_permissions'`. M9.5-D45 może revert na "read" post-merge (Docker Hub auth nie używa GITHUB_TOKEN).
- [ ] **Conventional commit scope:** używaj `m9_5` (underscore) NIE `m9.5` (dot rejected by `conventional-pre-commit` hook).
- [ ] **PROCESS: pre-commit `no-commit-to-branch` hook** (PR #105 M5) — blokuje commits na master. **Każdy D-task wymaga `git checkout -b <type>/<#>-...` PRZED kodowaniem** (CLAUDE.md §12).

---

## Otwarte pytania (rozstrzygnięte 2026-05-16, spec §3)

Wszystkie 7 decyzji designowych ze spec'a §3 zaakceptowane bez modyfikacji:

1. ✅ **§3.1** Registry swap ghcr.io → Docker Hub (PUBLIC repo `bgozl/tibiantis-scraper`, anonymous pull, secrets-based auth).
2. ✅ **§3.2** Manual Hetzner Cloud Console provisioning (CX22 Falkenstein, Ubuntu 24.04). NIE Terraform.
3. ✅ **§3.3** Hardening minimal must-haves (deploy user + SSH key-only + UFW + Docker). NIE comprehensive (fail2ban/unattended-upgrades to M-future).
4. ✅ **§3.4** First deploy `/opt/tibiantis` z manual `curl + .env edit + docker compose pull/up`. NIE git clone full source.
5. ✅ **§3.5** Uptime Kuma 9. service, port 3001 localhost-bound, 4 monitors. NIE Grafana/Prometheus.
6. ✅ **§3.6** `docs/deploy-runbook.md` 10 sekcji w D46+D47.
7. ✅ **§3.7** `docs/dev-runbook.md` M3-M9 carry-over consolidation w D48.

**Open questions z §10 spec'a** (do M9.5 retro, NIE w M9.5 scope):
- `bootstrap.sh` idempotency (`useradd` fail przy re-run)
- Hetzner Cloud Firewall vs UFW redundancy decision
- `.env` ownership/perms (`chown deploy:deploy` explicit?)
- Image size 480MB pull duration over Hetzner network
- Docker Hub PAT rotation policy
- Kuma DB backup
- Hetzner public IPv4 ephemeral (static IP M-future)
- Docker Hub anonymous rate limit (100/6h)

---

## Risk + mitigation

| Ryzyko | Prawdopodobieństwo | Impact | Mitigation |
|---|---|---|---|
| **Docker Hub PAT scope insufficient** (np. Read-only zamiast Read+Write+Delete) | Średnie (operator UI mistake) | D45 workflow `push: true` fail "denied" | Issue body D45 explicit "Read + Write + Delete scope" + verify pre-PR z `docker login` test |
| **GitHub Secrets typo (`DOCKERHUB_TOKEN` vs `DOCKER_HUB_TOKEN`)** | Niskie | Workflow "Username and password required" | `gh secret list` sanity pre-PR + workflow YAML match exact names |
| **Image name conflict** (`bgozl/tibiantis-scraper` zajęty przez kogoś) | Niskie (krótki ale unique username) | Push fails, push fallback name | Pre-check: `curl https://hub.docker.com/v2/repositories/bgozl/tibiantis-scraper/` (404 = available) |
| **Hetzner CX22 RAM insufficient pod load** | Niskie (~1.5-2GB idle, 4GB total) | OOM kill któregoś service'a | Monitoring via Kuma + `docker stats` po deploy. Upgrade na CCX13 (4GB → 8GB, ~14€/mc) gdy się okaże |
| **SSH lockout w D46** (operator `ufw deny` przed `ufw allow 22`) | Średnie (bootstrap script ordering) | Lost SSH access do VM | Hetzner Cloud Console → server → Console (web TTY rescue) → `ufw allow 22 + reload`. Bootstrap script ma `ufw allow 22` PRZED `ufw enable` (sanity check w issue body) |
| **`docker compose pull` rate limit anonymous (100/6h)** | Niskie (single VM, dev iteration rare) | 429 toomanyrequests | `docker login -u bgozl` z PAT bypassuje (200/6h authenticated). Issue body D47 §troubleshooting |
| **Discord bot token nieprawidłowy w `.env`** | Średnie (operator copy-paste mistake) | `discord_bot` restart loop | `docker compose logs discord_bot` shows "Improper token". Regenerate token w Developer Portal, edit `.env`, `docker compose up -d --no-deps discord_bot` |
| **Kuma initial setup race** (web form skip) | Niskie | UI shows login bez admin creation | `docker compose restart uptime-kuma` re-renders setup form (DB w `uptime_kuma_data` empty → setup mode) |
| **First `docker compose pull` slow (480MB image)** | Wysokie (one-time) | ~30-60s w Falkenstein z Docker Hub | Acceptable, operator informed w runbook §4 "first pull ~1min, subsequent rolling pulls only diff layers" |
| **VM IPv4 ephemeral** (server destroy = new IP) | Średnie M-future | DNS A record stale | M9.5 zostaje IP-based; M-future static IP reservation w Hetzner |
| **`docker compose ps` differences MSYS vs Linux** | Niskie (output format) | Visual difference (Windows tabular jest narrow) | Nieblokujące, operator widzi swój output. Note w runbook |
| **`bootstrap.sh` re-run idempotency** (`useradd deploy` fail) | Niskie (operator zwykle nie re-runs) | Set -e exit | Acceptable — `bootstrap.sh` design dla fresh VM. M-future idempotency wrap |
| **`docker compose pull` z private network issue** | Niskie | First deploy fail | Operator retry; check `ping -c 3 registry-1.docker.io` z VM |
| **Conventional commit scope `m9.5`** | Wysokie (dot rejected) | Pre-commit hook fail | Use `m9_5` jako workaround (sprawdzone empirycznie na spec commit) |
| **Workflow `default_workflow_permissions: "read"` post-D45 revert** | Niskie | Future workflows nie mają `packages: write` ale i tak nie używają Docker Hub | Trade-off acceptable; revert na `read` po M9.5 close jest cleanup, ale możemy zostawić `write` dla safety (zero koszt) |
| **Operator forgets `chmod 600 .env`** | Wysokie (typowa pomyłka) | `.env` world-readable, secrets exposed | Runbook §4 explicit `chmod 600 .env` jako krok. Plus `docker compose` nadal działa, więc nie crashuje — tylko security debt do flag jako tech debt |

---

## Task overview

| # | ID | Tytuł | Czas | Zależy od | Branch |
|---|---|---|---|---|---|
| 1 | **M9.5-D45** | Registry swap ghcr.io → Docker Hub (workflow + compose + secrets + cleanup ghcr.io) | 2-3h | M9 closed | `chore/<#>-dockerhub-swap` |
| 2 | **M9.5-D46** | Hetzner VM provisioning + hardening + `bootstrap.sh` + `docs/deploy-runbook.md` sections 1-3 | 3-4h | D45 merged | `docs/<#>-deploy-runbook-init` |
| 3 | **M9.5-D47** | First deploy + Uptime Kuma 9. service + `docs/deploy-runbook.md` sections 4-10 + smoke verify w prod | 3-4h | D46 merged + VM running | `feat/<#>-uptime-kuma-first-deploy` |
| 4 | **M9.5-D48** | `docs/dev-runbook.md` carry-over consolidation + closure (PROGRESS.md retro + milestone close) | 2-3h | D47 merged | `docs/<#>-dev-runbook-and-close` |

**Total:** ~10-14h. Mieści się w 4-day M9.5 budget (16h).

**Strict chain:** D45 → D46 → D47 → D48. Brak parallelism (D47 wymaga VM hardened z D46, D45 swap musi być przed D47 deploy by Docker Hub był source-of-truth dla pull).

---

## Task #1 — [M9.5-D45] Registry swap ghcr.io → Docker Hub

### 🎯 Cel

Zmienić registry z `ghcr.io` na `docker.io` (Docker Hub) — workflow login + image name, plus `docker-compose.yml` 5× `image:` references. Dorzucić `DOCKERHUB_USERNAME` + `DOCKERHUB_TOKEN` jako GitHub Secrets (manual pre-PR via `gh secret set`). Cleanup ghcr.io package post-merge (`gh api -X DELETE`). Po D45: każdy push do master triggeruje GHA workflow → push obrazu do `docker.io/bgozl/tibiantis-scraper:master` + `:sha-<commit>`. Lokalny `docker pull bgozl/tibiantis-scraper:master` z laptopa działa **anonymous** (no `docker login` needed for public repo).

### 🧠 Czego się nauczysz

- **Docker Hub public repos** — anonymous pull works bez auth (default), idealny dla VPS deploy gdy nie chcesz `docker login` na każdej VM. Rate limit 100/6h anonymous, 200/6h authenticated.
- **Docker Hub PAT scoping** — Personal Access Tokens w Docker Hub mogą być scoped do specific repos (Read, Write, Delete) zamiast account-wide. Best practice: least privilege per service.
- **`docker/login-action@v3` default registry** — bez `registry:` directive, action defaults to `index.docker.io` (Docker Hub). Wystarczy podać `username` + `password` (PAT).
- **`docker/metadata-action@v5` image naming** — `images: bgozl/tibiantis-scraper` (bez `docker.io/` prefix, choć też działa). Auto-resolves jako `index.docker.io/bgozl/tibiantis-scraper`.
- **GitHub package deletion via `gh api`** — `gh api -X DELETE user/packages/container/<name>` nuke całego package + wszystkich versions. Alternative: per-version delete `gh api -X DELETE user/packages/container/<name>/versions/<id>`.

### ✅ Acceptance criteria

(Pełne AC w issue body — `.github/issue-bodies/m9_5-d45.md`.)

**Kluczowe punkty:**

- **Pre-PR manual ops:**
  - Generate Docker Hub PAT (https://hub.docker.com/settings/security) z scope **Read + Write + Delete** dla `bgozl/tibiantis-scraper` only
  - `gh secret set DOCKERHUB_USERNAME --body "bgozl"`
  - `gh secret set DOCKERHUB_TOKEN --body "<paste-PAT>"`
  - Verify: `gh secret list` → shows both

- **`.github/workflows/docker.yml`** changes (verbatim w spec §3.1):
  - Usuń `packages: write` z permissions
  - Usuń `registry: ghcr.io` z login-action
  - Replace `${{ secrets.GITHUB_TOKEN }}` z `${{ secrets.DOCKERHUB_TOKEN }}`
  - Replace `username: ${{ github.actor }}` z `username: ${{ secrets.DOCKERHUB_USERNAME }}`
  - Replace `images: ghcr.io/${{ github.repository }}` z `images: bgozl/tibiantis-scraper`

- **`docker-compose.yml`** 5× swap:
  - `migrate.image`, `web.image`, `celery_worker.image`, `celery_beat.image`, `discord_bot.image`: `ghcr.io/bgozl/tibiantis-scraper:master` → `bgozl/tibiantis-scraper:master`

- **Post-PR cleanup:**
  - `gh api -X DELETE user/packages/container/tibiantis-scraper` (nuke ghcr.io)
  - Sanity: `curl -fsS https://hub.docker.com/v2/repositories/bgozl/tibiantis-scraper/` → 200 + JSON with `pull_count > 0` (po pierwszym push)

- **Optional cleanup:** revert workflow permissions `gh api -X PUT repos/.../actions/permissions/workflow -f default_workflow_permissions=read` (M9-D44 flipped na "write" dla ghcr.io; Docker Hub nie wymaga)

- **Manual smoke (post-master-merge):**
  - GHA workflow log → "Logging in to docker.io" → "Login Succeeded"
  - GHA workflow log → `bgozl/tibiantis-scraper:master` + `bgozl/tibiantis-scraper:sha-<commit>` w tags
  - `docker pull bgozl/tibiantis-scraper:master` z laptopa **bez `docker login`** → success
  - Verify w UI: `https://hub.docker.com/r/bgozl/tibiantis-scraper` → public + tags visible

### ⚠️ Pułapki do uwagi

- **A — PAT scope MUSI być Read + Write + Delete.** Read-only NIE pozwala na push z CI. Issue body D45 explicit czeck w pre-PR steps.
- **B — Secrets names case-sensitive.** `DOCKERHUB_USERNAME` (NIE `dockerhub_username` ani `DOCKER_HUB_USERNAME`). Workflow YAML match exact.
- **C — `images: bgozl/tibiantis-scraper`** bez `docker.io/` prefix (choć działa też z prefix). Lowercase username (Docker Hub case-insensitive ale convention lowercase).
- **D — Image name już wzięty?** Pre-check: `curl -fsS https://hub.docker.com/v2/repositories/bgozl/tibiantis-scraper/` — jeśli 200 returned (existing repo), to nie ours (lub our previous attempt). Jeśli 404, available. Po pierwszym push → 200 z tags.
- **E — `default_workflow_permissions: "read"` post-revert** — Docker Hub workflow nie potrzebuje `packages: write`, ale ci.yml też nic z tym nie robi (tylko `contents: read`). Revert harmless. Można zostawić "write" — zero risk dla M9.5+M10.
- **F — ghcr.io cleanup async** — `gh api -X DELETE` zwraca 204 No Content. Verify w UI: `https://github.com/bgozlinski?tab=packages` → tibiantis-scraper package gone (może chwilę).
- **G — Workflow uses `paths:` filter na PR.** Zmiana `.github/workflows/docker.yml` triggers workflow na PR (file w `paths:` list). Sanity: workflow odpala się na PR D45 + zielony.
- **H — Conventional commit scope `m9.5` rejected** — use `m9_5` (underscore). Commit message przykład: `chore(m9_5): swap ghcr.io → Docker Hub registry (M9.5-D45, #<#>)`.

### 🧪 Testing plan

```bash
# Pre-PR sanity
gh secret list   # → DOCKERHUB_USERNAME + DOCKERHUB_TOKEN visible
docker login -u bgozl -p <PAT>   # test PAT scope works
docker pull busybox && docker tag busybox bgozl/tibiantis-scraper:smoke
docker push bgozl/tibiantis-scraper:smoke   # smoke push przez PAT (delete tag after)
docker rmi bgozl/tibiantis-scraper:smoke   # cleanup

# Workflow YAML sanity
yamllint .github/workflows/docker.yml
poetry run pre-commit run --files .github/workflows/docker.yml docker-compose.yml

# Post-PR (po master merge)
gh run watch <workflow-run-id>
docker pull bgozl/tibiantis-scraper:master   # bez docker login
docker image inspect bgozl/tibiantis-scraper:master --format '{{.Created}}'

# Cleanup ghcr.io
gh api "user/packages/container/tibiantis-scraper/versions" --jq '.[].name'   # list before delete
gh api -X DELETE "user/packages/container/tibiantis-scraper"
```

### 📦 Definition of Done

- [ ] AC spełnione (workflow + compose updated, secrets w GitHub, ghcr.io package usunięty).
- [ ] PR zmergowany squash (`chore(m9_5): swap ghcr.io → Docker Hub registry (M9.5-D45, #<#>)`).
- [ ] CI lint + test + docker.yml workflow zielone.
- [ ] Docker Hub repo public + visible at `https://hub.docker.com/r/bgozl/tibiantis-scraper`.
- [ ] `docker pull bgozl/tibiantis-scraper:master` z laptopa anonymous = success.
- [ ] ghcr.io package wiped.

---

## Task #2 — [M9.5-D46] Hetzner VM + hardening + bootstrap.sh + runbook init

### 🎯 Cel

Provision Hetzner CX22 VM (Falkenstein, Ubuntu 24.04) via Cloud Console UI. Stworzyć `docs/scripts/bootstrap.sh` (deploy user + SSH hardening + UFW + Docker install). Operator scp's + runs bootstrap na fresh VM. Stworzyć `docs/deploy-runbook.md` z sekcjami 1-3: Prerequisites (checklist co operator musi mieć), Provision VM (Cloud Console click sequence), First SSH + bootstrap (`scp + ssh + bash + verify`). Po D46: VM hardened, deploy user works, Docker installed, runbook gotowy dla operator który robi pierwszy deploy.

### 🧠 Czego się nauczysz

- **Hetzner Cloud Console workflow** — project creation, server creation (location, type, image, SSH key, firewall, labels), Cloud Firewall rules (inbound TCP/ICMP, outbound allow-all).
- **SSH hardening minimum** — `PasswordAuthentication no`, `PermitRootLogin prohibit-password`, `ChallengeResponseAuthentication no` w `/etc/ssh/sshd_config`. Plus `systemctl reload sshd` to apply.
- **UFW configuration** — `ufw default deny incoming` + `ufw allow 22/tcp` + `ufw --force enable`. Defense-in-depth layer ponad Hetzner Cloud Firewall.
- **Docker install via official script** — `curl -fsSL https://get.docker.com | sh` — najszybsza droga get latest Docker + docker-compose-plugin. Alternative `apt-get install docker.io` lags wersje.
- **Bash `set -euo pipefail`** — strict mode: fail-fast na unset vars + pipeline errors. Standard practice dla provisioning scripts.
- **`gh api` Hetzner alternative** — Hetzner ma own CLI (`hcloud`) ale dla M9.5 manual UI sufficient.

### ✅ Acceptance criteria

(Pełne AC w issue body — `.github/issue-bodies/m9_5-d46.md`.)

**Operator manual steps (poza repo, dokumentowane w runbook):**
1. Hetzner Cloud Console — create project `tibiantis-monitor`
2. Upload SSH pubkey (`~/.ssh/id_ed25519.pub`)
3. Create server: CX22, Falkenstein, Ubuntu 24.04, SSH key attached, Firewall attached, no Cloud Config, no Volumes, no Backups, labels `env=prod`+`project=tibiantis`
4. Create Cloud Firewall `tibiantis-firewall`: inbound 22/tcp from `<your-IPv4>/32` + ICMP from anywhere; outbound all
5. SSH as root → `scp bootstrap.sh /tmp/` → `ssh root@<ip> "bash /tmp/bootstrap.sh"`
6. Verify: `ssh deploy@<ip>` works, `ssh root@<ip>` fails or works (key-based, no password)
7. Capture: Hetzner public IPv4, screenshots Cloud Console (optional placeholders dla runbook)

**`docs/scripts/bootstrap.sh`** — pełna zawartość ze spec §3.3 (cytuj verbatim z spec'a).

**`docs/deploy-runbook.md`** — sekcje 1-3 (Markdown, ~100-150 lines):

```markdown
# Tibiantis Monitor — Deploy runbook

This runbook walks operator (Ty) through first deploy of Tibiantis Monitor
on Hetzner VM. Estimated time: ~1h end-to-end for someone with SSH + Docker
familiarity.

## §1 Prerequisites

Before starting:
- [ ] Hetzner Cloud account (https://console.hetzner.cloud) z payment method
- [ ] Local SSH key pair: `~/.ssh/id_ed25519` (existing) or `ssh-keygen -t ed25519`
- [ ] Discord bot token (Developer Portal, M7 setup) — paste later na VM
- [ ] (Optional) Docker Hub account dla pull rate limit bypass z `docker login`

Estimated cost: **~6€/mc** (Hetzner CX22 + free Docker Hub public + free GHA public)

## §2 Provision VM

1. Cloud Console → Project → "Add Project" → `tibiantis-monitor`
2. Sidebar → "Security" → "SSH Keys" → Add → paste `~/.ssh/id_ed25519.pub`
3. Sidebar → "Servers" → "Add Server"
   - Location: **Falkenstein DE-FSN** (~30ms z PL)
   - Image: **Ubuntu 24.04**
   - Type: **CX22** (~6€/mc, 2 vCPU shared, 4GB RAM, 40GB SSD)
   - Networking: Public IPv4 + IPv6 (default)
   - SSH Keys: select your uploaded key
   - Firewall: leave empty for now (Firewall added next step)
   - Labels: `env=prod`, `project=tibiantis`
   - Backups: disabled (M-future cheaper alternative)
   - Create & start server, capture **public IPv4** address.
4. Sidebar → "Firewalls" → "Create Firewall" → `tibiantis-firewall`
   - Inbound rules:
     - Allow TCP 22 from `<your-IPv4>/32` (check via `curl ipify.org`)
     - Allow ICMP from `0.0.0.0/0` (ping/traceroute debug)
   - Outbound rules: allow all (default)
   - Apply to: server `tibiantis-monitor-cx22` (created step 3)

## §3 First SSH + bootstrap

1. From laptop: `scp docs/scripts/bootstrap.sh root@<hetzner-ip>:/tmp/`
2. `ssh root@<hetzner-ip>` → confirm SSH key auth works
3. `bash /tmp/bootstrap.sh` → check output "Bootstrap complete..."
4. Verify hardening (from laptop, new terminal):
   - `ssh deploy@<hetzner-ip>` → should work
   - `ssh -o PreferredAuthentications=password root@<hetzner-ip>` → "Permission denied" (password disabled)
   - On VM as deploy: `docker ps` → empty list, no errors (Docker accessible)
   - On VM as deploy: `sudo ufw status verbose` → 22/tcp ALLOW, default deny incoming

Next: §4 First deploy (covered w M9.5-D47).
```

### ⚠️ Pułapki do uwagi

- **A — Hetzner SSH key MUSI być uploaded przed server create.** Inaczej Hetzner emails initial root password (security risk + extra step). Issue body D46 emphasize krok 2 przed 3.
- **B — Firewall rule order matters.** Najpierw allow 22/tcp, potem default deny. Inaczej SSH lockout. `bootstrap.sh` ma `ufw allow 22/tcp` PRZED `ufw enable` — sanity.
- **C — `<your-IPv4>/32` w Cloud Firewall** — dynamic ISP IP może się zmienić → lockout. Fallback: `0.0.0.0/0` + fail2ban (M-future). M9.5 default na user's static-ish home IP.
- **D — Hetzner Cloud Console → Server → Console (web TTY)** — rescue gdy SSH lockout. Bookmark dla recovery scenario.
- **E — `bootstrap.sh` set -e exit przy re-run** (`useradd deploy` fail bo user exists). Acceptable dla fresh VM design. M-future idempotency: `id deploy &>/dev/null || useradd ...`.
- **F — `sed -i` na sshd_config bez backup** — jeśli regex fail, lose config. Defensive: `cp /etc/ssh/sshd_config /etc/ssh/sshd_config.bak` przed sed (dorzucone w bootstrap.sh).
- **G — `curl -fsSL https://get.docker.com | sh` runs as root** — security-conscious operatorzy review script first (`curl ... | less`). Mention w runbook.
- **H — `usermod -aG docker deploy` wymaga relogin** — deploy user musi się wylogować + zalogować ponownie aby docker group take effect. `bootstrap.sh` runs as root więc deploy nie używa Dockera w bootstrap; pierwszy `ssh deploy@<ip>` daje świeży session z group membership.
- **I — Conventional commit scope `m9_5` (underscore), nie `m9.5`** (pre-commit hook).
- **J — Cloud Firewall apply async** — zaaplikowanie rules to firewall na server może trwać ~30s. SSH lockout w między czasie? Niska szansa, ale watch.

### 🧪 Testing plan

```bash
# Pre-PR sanity (na laptopie)
bash -n docs/scripts/bootstrap.sh   # syntax check
shellcheck docs/scripts/bootstrap.sh   # optional, jeśli installed

# Pre-commit
poetry run pre-commit run --files docs/scripts/bootstrap.sh docs/deploy-runbook.md

# Manual smoke (operator wykonuje na real VM)
# 1. Provision VM via Cloud Console (clicks)
# 2. scp + ssh + bash bootstrap
scp docs/scripts/bootstrap.sh root@<ip>:/tmp/
ssh root@<ip> "bash /tmp/bootstrap.sh"

# Verify hardening
ssh deploy@<ip> "whoami && groups && docker ps && sudo ufw status verbose"
# Expected:
# - whoami: deploy
# - groups: deploy sudo docker
# - docker ps: empty (no containers, no error)
# - ufw status: 22/tcp ALLOW, default deny

ssh root@<ip> -o PreferredAuthentications=password 2>&1 | grep -q "Permission denied"
# Expected: exit 0 (password auth denied)
```

### 📦 Definition of Done

- [ ] AC spełnione (VM provisioned, bootstrap runs, deploy user works, Docker available).
- [ ] PR zmergowany squash (`docs(m9_5): runbook sections 1-3 + bootstrap.sh hardening script (M9.5-D46, #<#>)`).
- [ ] CI lint zielone (PR docs + bash script tylko, no test impact).
- [ ] Operator confirmation w PR body: VM provisioned + IP captured, `ssh deploy@<ip>` works, `docker ps` works as deploy user, `ufw status verbose` shows 22/tcp + default deny.
- [ ] `docs/scripts/bootstrap.sh` shellcheck-clean (optional ale recommended).
- [ ] `docs/deploy-runbook.md` sections 1-3 wypełnione (Prerequisites + Provision VM + First SSH + bootstrap).

---

## Task #3 — [M9.5-D47] First deploy + Uptime Kuma + runbook complete

### 🎯 Cel

Dorzucić `uptime-kuma` jako 9. service do `docker-compose.yml` (port 3001 localhost-bound, volume `uptime_kuma_data`, no healthcheck, `restart: unless-stopped`). Operator wykonuje first deploy na hardened VM z D46: `/opt/tibiantis` directory, `curl docker-compose.yml + .env.example`, fill secrets, `docker compose pull && up -d`. Verify 9 services + migrate exit 0. SSH tunnel z laptopa, smoke `curl /health/`, Kuma UI initial setup (4 monitors), Discord bot live test (`/bedmage add` w dev guild). Rozszerzenie `docs/deploy-runbook.md` o sekcje 4-10. Po D47: app live na Hetzner, dostępny via SSH tunnel, runbook complete.

### 🧠 Czego się nauczysz

- **Uptime Kuma** — self-hosted monitoring (Docker container z SQLite), HTTP/TCP/Ping/DNS monitors z notification integrations (Slack, Discord, Email, Webhook). Free, ~150MB RAM.
- **`127.0.0.1:3001:3001` bind syntax** — Docker port mapping localhost-only (NIE `0.0.0.0:3001:3001` ani `3001:3001` default). Defense: usługa NIE accessible z internetu, nawet jeśli UFW/Firewall fail.
- **Docker internal DNS** — services w compose default network resolve by service name (`web`, `postgres`, `redis`). Kuma monitor URL `http://web:8000/health/` używa Docker DNS, nie localhost.
- **SSH tunnel multiplexing** — `ssh -L 8000:localhost:8000 -L 3001:localhost:3001 deploy@<ip>` forwarduje 2 ports w jednym session.
- **Discord bot live verification** — `/bedmage add <character>` slash command → bot ephemeral response → DB row visible przez `docker compose exec web python manage.py shell`. End-to-end test from Discord gateway through Celery beat through Postgres.

### ✅ Acceptance criteria

(Pełne AC w issue body — `.github/issue-bodies/m9_5-d47.md`.)

**Compose change** (`docker-compose.yml`):

Dorzucenie service po `discord_bot`:

```yaml
  uptime-kuma:
    image: louislam/uptime-kuma:1
    volumes:
      - uptime_kuma_data:/app/data
    ports:
      - "127.0.0.1:3001:3001"
    restart: unless-stopped
```

I `uptime_kuma_data:` w top-level `volumes:` block.

**Operator deploy flow (z runbook):**

```bash
ssh deploy@<hetzner-ip>
sudo mkdir -p /opt/tibiantis
sudo chown deploy:deploy /opt/tibiantis
cd /opt/tibiantis

# Pull 2 pliki z GitHub raw
curl -fsSL https://raw.githubusercontent.com/bgozlinski/tibiantis-scraper/master/docker-compose.yml -o docker-compose.yml
curl -fsSL https://raw.githubusercontent.com/bgozlinski/tibiantis-scraper/master/.env.example -o .env.example

cp .env.example .env
chmod 600 .env
vim .env
# Fill (alphanumeric SECRET_KEY ZAWSZE!):
# - DJANGO_SECRET_KEY=<50-char alphanumeric>
# - POSTGRES_PASSWORD=<random strong>
# - DISCORD_BOT_TOKEN=<real bot token>
# - DJANGO_ALLOWED_HOSTS=<hetzner-ipv4>,localhost

docker compose pull
docker compose up -d
sleep 90
docker compose ps   # → 9 services Up + migrate Exited (0)
docker compose logs migrate
docker compose exec web curl -fsS http://localhost:8000/health/
```

**Smoke z laptopa (SSH tunnel):**

```bash
ssh -L 8000:localhost:8000 -L 3001:localhost:3001 deploy@<hetzner-ip>
# inny terminal na laptopie:
curl -fsS http://localhost:8000/health/   # 200 + {"db":"ok","redis":"ok"}
# przeglądarka: http://localhost:3001 → Kuma initial setup form
```

**Kuma initial setup (via UI):**
- Admin account creation (zapisz password manager)
- Add 4 monitors:
  1. **web /health/** — HTTP(s), URL `http://web:8000/health/`, interval 60s, expect 200
  2. **postgres** — TCP Port, Hostname `postgres`, Port `5432`, interval 60s
  3. **redis** — TCP Port, Hostname `redis`, Port `6379`, interval 60s
  4. **mongo** — TCP Port, Hostname `mongo`, Port `27017`, interval 60s
- Wait 2 min → all 4 monitors green w dashboard

**Discord live test:**
- `/bedmage add yhral` w dev guild
- Bot ephemeral response (z M7-D33 fix)
- Verify DB:
  ```bash
  docker compose exec web python manage.py shell -c "from apps.bedmages.models import BedmageWatch; print(BedmageWatch.objects.all())"
  ```
  → row visible

**`docs/deploy-runbook.md` rozszerzony o sekcje 4-10:**

- §4 First deploy (mkdir, curl, .env edit, docker compose pull/up)
- §5 Smoke verification (SSH tunnel + curl + Kuma init)
- §6 Discord bot live test (`/bedmage add` + DB verify)
- §7 Rolling update (`docker compose pull && up -d`)
- §8 Rollback (edit `image:` na pinned `:sha-<commit>` + `up -d`)
- §9 Troubleshooting (10 typowych issues: SECRET_KEY z `$`, `.env` chmod, Discord bot restart loop, migrate exit 1, port 8000 in use, UFW lockout, DNS fail, disk full, Docker Hub rate limit, Cloud Firewall sync)
- §10 Cost tally (~6€/mc breakdown)

**Manual smoke output w PR description:**
- `docker compose ps` (9 services Up + migrate Exited 0)
- `curl /health/` JSON output
- Kuma dashboard screenshot (4 monitors green)
- Discord bot ephemeral response screenshot

### ⚠️ Pułapki do uwagi

- **A — `127.0.0.1:3001:3001` bind** — NIE `0.0.0.0:3001:3001` (Kuma UI exposed publicly!). Issue body D47 explicit + pre-commit `docker compose config` może highlight różnicę.
- **B — Kuma monitor URL `http://web:8000/health/`** — Docker internal DNS, NIE `http://localhost:8000/health/` (Kuma container's localhost = sama Kuma, nie web).
- **C — `chmod 600 .env` MUSI być przed `docker compose up -d`** — inaczej secrets world-readable. Issue body D47 + runbook explicit step.
- **D — Alphanumeric SECRET_KEY ZAWSZE** — Compose v2 `$VAR` interpolation gotcha (M9-D43). Generator: `python -c "import string, secrets; print(''.join(secrets.choice(string.ascii_letters + string.digits) for _ in range(50)))"`. Runbook §4 explicit warning.
- **E — `curl -fsSL` z GitHub raw** — używa `master` branch. Jeśli operator chce specific commit, edit URL `raw/sha-<commit>` zamiast `raw/master`. M-future ułatwi.
- **F — Discord bot live test ID change** — `discord_id` z M2 to CharField. M9.5 NIE zmienia, operator's prod discord_id auto-create via `/bedmage add` (M7 service path).
- **G — `docker compose exec` MSYS path conversion** — operator może uderzyć przy `docker compose exec web ls /tmp/` na Windows Git Bash. Mention w `docs/dev-runbook.md` (D48 carry-over).
- **H — Discord bot crashed loop diagnostics** — `docker compose logs discord_bot` z exit code 1 + "Improper token" znaczy invalid `DISCORD_BOT_TOKEN`. Regenerate w Developer Portal, edit `.env`, `docker compose up -d --no-deps discord_bot`.
- **I — Kuma re-render setup form** — gdy operator skipped admin creation (e.g. tab close), `docker compose restart uptime-kuma` re-renders setup mode (DB w volume empty `users` → setup mode).
- **J — `migrate` Exited (0) NIE pokazuje się w `docker compose ps` default** — one-shot, post-exit Docker hides. Use `docker compose ps -a` lub `docker compose logs migrate` to confirm.

### 🧪 Testing plan

```bash
# Pre-PR (laptopie)
docker compose -f docker-compose.yml config --quiet   # YAML + chain validation
poetry run pre-commit run --files docker-compose.yml docs/deploy-runbook.md

# Smoke (operator wykonuje na real VM po PR merge)
# (sequence per §3.4 spec)

# Smoke z laptopa
ssh -L 8000:localhost:8000 -L 3001:localhost:3001 deploy@<hetzner-ip> &
sleep 5
curl -fsS http://localhost:8000/health/   # 200 + JSON
# browser localhost:3001 → Kuma UI

# Discord live test
# manual: /bedmage add yhral w dev guild
docker compose exec web python manage.py shell -c "from apps.bedmages.models import BedmageWatch; print(BedmageWatch.objects.filter(character__name='yhral').exists())"
# → True
```

**Coverage cel:** brak (M9.5 to infra, no code changes).

### 📦 Definition of Done

- [ ] AC spełnione (9 services Up healthy, /health/ 200, Kuma 4 monitors green, Discord live test passed).
- [ ] PR zmergowany squash (`feat(m9_5): Uptime Kuma + first prod deploy w Hetzner (M9.5-D47, #<#>)`).
- [ ] CI lint + test + docker.yml workflow zielone.
- [ ] `docs/deploy-runbook.md` complete (sections 4-10 dorzucone, 10 sections total).
- [ ] Manual smoke output w PR body (docker compose ps + curl /health/ + Kuma screenshot + Discord screenshot).
- [ ] App live on Hetzner VM (confirmation w PR body z public IPv4 IP — uważaj nie commit IP do repo, tylko w PR description).

---

## Task #4 — [M9.5-D48] dev-runbook + closure

### 🎯 Cel

Stworzyć `docs/dev-runbook.md` z 6 sekcjami carry-over z M3-M9 retro fragments (Windows Celery `-P solo`, MSYS path conversion, `pre-commit clean` workflow, `docker compose build` race fix, Compose `$VAR` interpolation, local dev setup). Closure PR od fresh master (M5-D27 + M6-D30 + M7-D35 + M8-D40 + M9-D44 pattern repeat 7. raz pod rząd) — `PROGRESS.md` rozszerzony o sekcję M9.5 z retro per Issue (D45-D48) + DoD + Podsumowanie + Tech debt. Manual smoke 7 punktów w closure PR description. Po merge: `gh api -X PATCH milestones/<#> -f state=closed`.

D48 ma **2 PR-y w 1 issue** (M5-D27 + M6-D30 + M7-D35 + M8-D40 + M9-D44 pattern):

1. **Feature PR** (`docs/<#>-dev-runbook`) — `docs/dev-runbook.md` (carry-over consolidation).
2. **Closure PR** (`docs/close-m9_5-hetzner-deploy` od **fresh master** po feature merge) — PROGRESS.md retro + manual smoke + milestone close.

### 🧠 Czego się nauczysz

- **PROGRESS.md M9.5 retro pattern** — kontynuacja M0-M9 (Ukończone + Notatki retro + DoD + Podsumowanie + Tech debt). 7-th milestone w PROGRESS.md.
- **`gh api -X PATCH milestones/<#>`** — M5-D27 + M6-D30 + M7-D35 + M8-D40 + M9-D44 precedens (6. raz, M9.5 = 7.).
- **Conventional commits scope dot rejection** — `m9.5` rejected przez hook, użyj `m9_5`. Lesson dla future fractional milestones.

### ✅ Acceptance criteria

(Pełne AC w issue body — `.github/issue-bodies/m9_5-d48.md`.)

**Feature PR (`docs/<#>-dev-runbook`) kluczowe punkty:**

`docs/dev-runbook.md` — pełna zawartość (Markdown, ~150-200 lines), 6 sekcji:

1. **Local dev setup**
   - `poetry install`
   - `cp .env.example .env` (DEV defaults: `localhost:5435/6379/27017`)
   - `docker compose -f docker-compose.dev.yml up -d` (DB only)
   - `poetry run python manage.py migrate`
   - `poetry run python manage.py runserver`
   - In other terminal: `poetry run celery -A config worker -P solo -l info`
   - In another: `poetry run celery -A config beat -l info`
   - For bot: `poetry run python manage.py run_discord_bot` (wymaga `DISCORD_BOT_TOKEN` w `.env`)

2. **Celery na Windows (M3-D17 lesson)**
   - Default prefork pool crashes z `WinError 5/6` z `billiard`
   - Workaround: `-P solo` (single-threaded, OK for dev)
   - Prod (Linux container) używa default prefork — `-P solo` NIE w Dockerfile CMD

3. **MSYS path conversion gotcha (M9-D43 lesson)**
   - Git Bash translates `/tmp/` → Windows path `C:/Users/.../AppData/Local/Temp/`
   - Symptom: `docker compose exec celery_beat ls /tmp/` → wrong path
   - Workaround 1: `MSYS_NO_PATHCONV=1 docker compose exec celery_beat ls /tmp/`
   - Workaround 2: `docker compose exec celery_beat ls //tmp/` (double-slash escape)
   - Cosmetic dev gotcha — runtime container Linux paths działają OK (compose YAML healthcheck via Docker daemon, nie shell)

4. **`pre-commit clean` workflow (M7-D33 lesson)**
   - Fresh dep installed + mypy false-positive na `# type: ignore[unused-ignore]`
   - Workaround: `poetry run pre-commit clean` → clears mypy isolated venv cache
   - Re-run mypy → typical correct result
   - Decision tree: jeśli error po `pre-commit clean`, dorzuć override do `[[tool.mypy.overrides]]` w `pyproject.toml`

5. **`docker compose build` przed `up -d --no-build` (M9-D43 lesson)**
   - Hybrid `image:` + `build:` pattern (5 services share image)
   - `docker compose up -d` z auto-build = parallel buildx → race na shared tag `:master`
   - Symptom: `target X: failed to solve: image "bgozl/tibiantis-scraper:master": already exists`
   - Fix: split na 2 commands:
     1. `docker compose build` (single-shot, no race)
     2. `docker compose up -d --no-build` (uses cached image)

6. **Generated SECRET_KEY alphanumeric only (M9-D43 lesson)**
   - Django's `get_random_secret_key()` może zawierać `$` chars
   - Compose v2 interpoluje `$VAR` w `.env` values jako shell expansion
   - Symptom: `The "v4" variable is not set` + corrupted SECRET_KEY
   - Fix: alphanumeric-only generator:
     ```bash
     python -c "import string, secrets; print(''.join(secrets.choice(string.ascii_letters + string.digits) for _ in range(50)))"
     ```
   - Applies do **prod `.env` na Hetzner** tak samo jak dev

**Closure PR (`docs/close-m9_5-hetzner-deploy`) kluczowe punkty:**

`PROGRESS.md` rozszerzone o:
- `## 🎉 Milestone M9.5 — Hetzner first deploy MVP COMPLETED (2026-MM-DD)` header
- `### Ukończone (M9.5)` — 4 issues + PR linki + squash hashes
- `### Notatki z retro M9.5` — per Issue D45-D48 (Docker Hub vs ghcr.io comparison, UFW config issues, Kuma init quirks, real-world deploy gotchas)
- `### Definition of Done M9.5` — checklist z spec'a §7
- `### Podsumowanie M9.5` — data range, dni vs budżet, lessons
- `### Tech debt z M9.5` — open questions z spec'a §10 + new gotchas złapane

**Manual smoke 7 punktów w closure PR body** (z spec'a §8):
1. Registry swap verify (`docker pull` anonymous z laptopa)
2. VM hardening verify (`ssh deploy@<ip>` + `ufw status`)
3. First deploy + Kuma (`docker compose ps` 9 services)
4. Health + smoke (SSH tunnel + `curl /health/`)
5. Kuma dashboard screenshot (4 monitors green)
6. Discord bot live test
7. Cost tally (~6€/mc)

**Po merge closure PR:** `gh api -X PATCH repos/bgozlinski/tibiantis-scraper/milestones/<#> -f state=closed`.

**Sanity:** `gh issue list --milestone "M9.5 — Hetzner first deploy MVP" --state open` → empty.

### ⚠️ Pułapki do uwagi

- **A — Closure branch od fresh master** (Pułapka C, M1-D8 + M5-D27 + M6-D30 + M7-D35 + M8-D40 + M9-D44 lekcja repeat **7. raz**) — `git checkout master && git pull && git checkout -b docs/close-m9_5-hetzner-deploy` PRZED edycją PROGRESS.md.
- **B — Conventional commit scope underscore** — `docs(m9_5):` NIE `docs(m9.5):` (hook rejects dot).
- **C — Milestone exact title match** — `gh issue list --milestone "M9.5 — Hetzner first deploy MVP"` wymaga exact title (em-dash `—`, NIE myślnika `-`).
- **D — `gh api -X PATCH milestones/<#>`** — `<#>` to **number** milestone'a. Verify: `gh api repos/bgozlinski/tibiantis-scraper/milestones --jq '.[] | select(.title|startswith("M9.5")) | .number'`.
- **E — Closure PR jako 2. PR w D48 issue, NIE feature PR osobno** — `Closes #<D48-issue-#>` w feature PR body auto-closes issue post-merge. Closure PR NIE potrzebuje `Closes` (issue już closed).
- **F — Manual smoke 7 punktów wymaga real VM running** — operator powinien mieć VM od D47 nadal up. Jeśli destroyed, recreate per runbook §1-3 + redo §4 deploy. M9.5 acceptable bez (operator decision).
- **G — `docs/dev-runbook.md` consolidation** może być rozbity na osobny PR jeśli za duże — D48 to 2 PR-y w 1 issue, można dorzucić 3. PR jeśli reviewer prosi.

### 🧪 Testing plan

**Feature PR:**

```bash
# Pre-PR
poetry run pre-commit run --files docs/dev-runbook.md

# Manual smoke (operator dev-runbook validation)
# Verify każda sekcja działa po opisanym workflow:
# 1. Local dev setup: poetry install + docker compose -f dev.yml up + runserver
# 2. Celery solo: -P solo działa na Windows
# 3. MSYS no_pathconv: MSYS_NO_PATHCONV=1 docker compose exec ...
# 4. pre-commit clean: po fresh dep zmianie, clean + mypy retry
# 5. docker compose build: 2-step na docker-compose.yml lokalnie
# 6. SECRET_KEY alphanumeric: generator produces no $
```

**Closure PR:**

```bash
# Manual smoke 7 punktów (operator wykonuje na hetzner-deployed app)
# (sequence per spec §8)

# Post-merge
gh api -X PATCH repos/bgozlinski/tibiantis-scraper/milestones/<#> -f state=closed --jq '.state'
# → "closed"

gh issue list --milestone "M9.5 — Hetzner first deploy MVP" --state open --json number,title
# → []
```

### 📦 Definition of Done

**Feature PR:**
- [ ] AC spełnione (`docs/dev-runbook.md` complete z 6 sekcji).
- [ ] Feature PR zmergowany squash (`docs(m9_5): dev-runbook M3-M9 carry-over consolidation (M9.5-D48, #<#>)`).
- [ ] CI lint zielone.

**Closure PR:**
- [ ] Closure PR zmergowany squash (`docs(progress): close M9.5 — Hetzner first deploy MVP COMPLETED + retro D45-D48`).
- [ ] CI lint zielone.
- [ ] PROGRESS.md sekcja M9.5 dorzucona.
- [ ] Manual smoke 7 punktów w closure PR body.
- [ ] Milestone M9.5 zamknięty na GitHub via `gh api -X PATCH .../milestones/<#> -f state=closed`.
- [ ] Wszystkie M9.5 issues CLOSED (`gh issue list --milestone "M9.5 — Hetzner first deploy MVP" --state open` → empty).

---

## Tech debt do flag'owania w M9.5 retro

Te kandydaci mogą surface'ować podczas M9.5 implementacji (zaznacz w PROGRESS.md "Tech debt z M9.5"):

- **`bootstrap.sh` idempotency** — `useradd` fail przy re-run. M-future: `id deploy &>/dev/null || useradd ...`.
- **Hetzner public IPv4 ephemeral** — server destroy = new IP. M-future: static IP reservation (~1€/mc) gdy DNS A record stabilność.
- **Docker Hub anonymous rate limit (100/6h)** — `docker login` z PAT na VM bypassuje. M-future: pull-through cache (Harbor / Registry mirror).
- **Image size 480MB first pull duration** — ~30-60s w Falkenstein. M-future: split scraper image + multi-arch + distroless (M9 tech debt carry-over).
- **Discord bot Kuma monitor heartbeat** — M-future: Mongo write + custom Kuma monitor type lub py-cord HTTP exposed endpoint.
- **`.env` ownership/perms explicit** — `chown deploy:deploy` poza `chmod 600`. M-future hardening polish.
- **fail2ban + unattended-upgrades** — M-future hardening polish (M10 kandydat).
- **Conventional commit scope dot rejection** — `m9.5` fail. M-future: PR z hook config update żeby accept dots, lub change milestone naming convention.
- **Carry-over z M4-M9 tech debt** (wciąż otwarte):
  - Branch protection master required `test/Pytest` check
  - `django_stubs_ext.monkeypatch()` sweep
  - `celery-types` package
  - TTL retention indexes na Mongo
  - User.email model divergence
  - Character.house null=True vs spec
  - Static files w prod (M10 kandydat)
  - Image scanning Trivy/Snyk (M10 kandydat)
  - DNS+TLS reverse proxy
  - CI deploy automation
  - Backups

---

## Skill usage map

| D | Skills wymagane | Skills opcjonalne |
|---|---|---|
| D45 | `verification-before-completion` (Docker Hub push verify post-merge) | `requesting-code-review` (workflow changes are subtle) |
| D46 | `verification-before-completion` (SSH hardening verify) | `systematic-debugging` (jeśli SSH lockout) |
| D47 | `verification-before-completion` (9 services healthy + Discord live test) | — |
| D48 | `verification-before-completion` (closure PR + milestone close + sanity check) | — |

---

## Closing notes

- **Strict chain:** D45 → D46 → D47 → D48 (każdy wymaga merge poprzedniego). Brak parallelism.
- **2 PR-y feature (D45+D47 są feat, D46+D48-feature są docs) + 1 closure PR** = 5 PRs total. Plus ewentualne hotfixy (M7/M8 wzorzec — 1-2 hotfixy per milestone).
- **Manual smoke jest jedyną siecią** dla VM provisioning + Docker Hub auth flow (kontynuacja M7 retro lekcja). Nie testujemy real VPS deploy automatycznie.
- **Closure PR pattern od fresh master** to **7. raz pod rząd** (M1-D8 → M5-D27 → M6-D30 → M7-D35 → M8-D40 → M9-D44 → M9.5-D48). Wzorzec utrwalony permanent.
- **Conventional commits scope:** używaj `m9_5` (underscore) zamiast `m9.5` (dot rejected). Lesson dla future fractional milestones.
