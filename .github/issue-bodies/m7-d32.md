**Milestone:** M7 — Discord bot commands
**Czas:** 2-3h
**Branch:** `feat/<this-issue-#>-discord-bot-bootstrap`
**Type:** `feat` (+ `build(deps)` osobny commit dla `py-cord` dependency)
**Zależy od:** M7-D31 merged
**Spec:** [`docs/superpowers/specs/2026-05-13-m7-discord-bot-commands-design.md`](../blob/master/docs/superpowers/specs/2026-05-13-m7-discord-bot-commands-design.md) §3.6, §3.8, §6.1
**Plan:** [`docs/superpowers/plans/2026-05-13-m7-implementation-plan.md`](../blob/master/docs/superpowers/plans/2026-05-13-m7-implementation-plan.md) Task #2

---

## 🎯 Cel

Dorzucić `py-cord` jako dependency (osobny `build(deps)` commit), utworzyć `discord_bot/bot.py` z bot instance + `on_ready` event handler (sync commands per-guild dev / globally prod) + global `on_application_command_error` listener (clean ephemeral msg + Mongo log z `exc_info`), `discord_bot/management/commands/run_discord_bot.py` jako entry point, edytować `config/settings/base.py` o `DISCORD_BOT_TOKEN` / `DISCORD_DEV_GUILD_ID` env reads + `"discord_bot"` named logger w `LOGGING` dict, dorzucić env vars do `.env.example`. **Bez cogów** — `setup_bot()` zwróci `discord.Bot` bez `add_cog(...)` calls (D33+D34 dorzucą). Po D32: management command startuje, łaczy się z Discord (jeśli token valid), nie ma żadnych slash commands jeszcze.

## 🧠 Czego się nauczysz

- **py-cord 2.x API** — `discord.Bot(intents=discord.Intents.default())` (default intents wystarczą dla slash commands), `bot.run(token)` blocks main thread, `@bot.event async def on_ready()` lifecycle hook.
- **Discord intents** — `Intents.default()` jest minimalne (no message content read). Slash commands NIE wymagają privileged intents.
- **`bot.sync_commands(guild_ids=[...])` vs globalny sync** — per-guild = instant propagacja, globalny = do 1h. Dev env var `DISCORD_DEV_GUILD_ID` przełącza tryby.
- **`on_application_command_error` global listener** — łapie unhandled exceptions z każdego coga.
- **`ctx.response.is_done()`** — Discord interaction ma 3s deferred response window. Jeśli cog handler już respondował, `ctx.followup.send(...)` zamiast `ctx.respond(...)`.
- **Django logging `propagate: True` z named logger** — M6 retro lekcja #2, `propagate: False` ciszy pytest caplog.

## ✅ Acceptance criteria

### Dependency commit (osobny)

- [ ] `poetry add py-cord` — dorzuca `py-cord (>=2.6,<3.0)` do `[project.dependencies]` w `pyproject.toml`.
- [ ] Osobny commit `build(deps): add py-cord for Discord bot (M7-D32, #<#>)` (analogicznie do `build(deps): add pymongo` w M6-D28).

### `.env.example`

- [ ] Dorzucone:
  ```
  # Discord bot
  DISCORD_BOT_TOKEN=
  DISCORD_DEV_GUILD_ID=
  ```

### `config/settings/base.py` rozszerzenia

- [ ] Env reads:
  ```python
  # Discord bot
  DISCORD_BOT_TOKEN = env("DISCORD_BOT_TOKEN", default="")
  DISCORD_DEV_GUILD_ID = env.int("DISCORD_DEV_GUILD_ID", default=0)
  ```
- [ ] LOGGING dict rozszerzony o named logger `"discord_bot"`:
  ```python
  "loggers": {
      "apps": { ... existing ... },
      "discord_bot": {
          "handlers": ["console", "mongo"],
          "level": "INFO",
          "propagate": True,
      },
  },
  ```

### `discord_bot/bot.py`

- [ ] Pełna zawartość:
  ```python
  from __future__ import annotations

  import logging

  import discord
  from django.conf import settings

  logger = logging.getLogger(__name__)

  intents = discord.Intents.default()
  bot = discord.Bot(intents=intents)


  @bot.event
  async def on_ready() -> None:
      logger.info("Bot logged in as %s (id=%s)", bot.user, bot.user.id)
      if settings.DISCORD_DEV_GUILD_ID:
          await bot.sync_commands(guild_ids=[settings.DISCORD_DEV_GUILD_ID])
          logger.info("Synced commands to dev guild %s", settings.DISCORD_DEV_GUILD_ID)
      else:
          await bot.sync_commands()
          logger.info("Synced commands globally")


  @bot.event
  async def on_application_command_error(
      ctx: discord.ApplicationContext, error: discord.DiscordException
  ) -> None:
      logger.exception("Slash command error in /%s: %s", ctx.command, error)
      msg = "❌ Something went wrong. The admins have been notified."
      if ctx.response.is_done():
          await ctx.followup.send(msg, ephemeral=True)
      else:
          await ctx.respond(msg, ephemeral=True)


  def setup_bot() -> discord.Bot:
      """Add cogs and return configured bot. D32: no cogs yet (D33+D34 add them)."""
      return bot
  ```

### Management command

- [ ] `discord_bot/management/__init__.py` (pusty).
- [ ] `discord_bot/management/commands/__init__.py` (pusty).
- [ ] `discord_bot/management/commands/run_discord_bot.py`:
  ```python
  from __future__ import annotations

  from django.conf import settings
  from django.core.management.base import BaseCommand

  from discord_bot.bot import setup_bot


  class Command(BaseCommand):
      help = "Run the Discord bot (blocks until interrupted)"

      def handle(self, *args, **options):
          if not settings.DISCORD_BOT_TOKEN:
              self.stderr.write("DISCORD_BOT_TOKEN not set in env")
              return
          bot = setup_bot()
          bot.run(settings.DISCORD_BOT_TOKEN)
  ```

### Testy

- [ ] `tests/unit/discord_bot/test_bot_bootstrap.py`:
  - `test_setup_bot_returns_discord_bot_instance`
  - `test_on_ready_syncs_per_guild_when_dev_guild_id_set`
  - `test_on_ready_syncs_globally_when_dev_guild_id_unset`
- [ ] `tests/unit/discord_bot/test_run_discord_bot_command.py`:
  - `test_run_discord_bot_exits_when_token_empty`
  - `test_run_discord_bot_calls_bot_run_when_token_set`
- [ ] `tests/unit/discord_bot/test_error_handler.py`:
  - `test_error_handler_responds_with_ephemeral_generic_when_response_not_done`
  - `test_error_handler_uses_followup_when_response_already_done`

## ⚠️ Pułapki do uwagi

- **A — `py-cord` NIE `discord.py`** — PyPI: `py-cord`, import: `import discord`. Konflikt nazwy gdy oba zainstalowane (Python crash przy `import discord`). Sprawdź `pyproject.toml` że NIE ma `discord.py` przed `poetry add py-cord`.
- **B — Default intents wystarczą dla slash commands** — `Intents.default()` nie zawiera `message_content` (privileged). Slash commands lecą przez Interaction Gateway. NIE wymaga toggle'a w Discord Developer Portal.
- **C — `bot.sync_commands(guild_ids=[...])` jest async — musi być wewnątrz async event handler'a** (np. `on_ready`). `setup_bot()` tylko returns bot.
- **D — `on_ready` może odpalić wielokrotnie** w jednej sesji (jeśli bot reconnect'uje się). Domyślne behavior: sync at każde on_ready, `sync_commands` jest idempotent. NIE wymaga state-tracking.
- **E — `ctx.respond` vs `ctx.followup.send` w error handlerze** — jeśli cog już respondował przed raise, drugi `ctx.respond` rzuca `InteractionResponded`. Defense: `if ctx.response.is_done(): followup` else `respond`.
- **F — Management command `bot.run(...)` BLOCKING** — `bot.run` nigdy nie returns (poza CTRL+C). NIE wykonuj nic po `bot.run()` w `handle()`.
- **G — `DISCORD_BOT_TOKEN` empty w env** — management command NIE crash'uje Python'em (czytelne stderr + clean exit). `self.stderr.write(...)` + `return` (NIE `sys.exit(1)`).
- **H — `env.int("DISCORD_DEV_GUILD_ID", default=0)`** — `django-environ` `env.int` requires default jako int. Empty env var → fallback 0. `0` → on_ready uses global sync.
- **I — Test mocking `bot.run(...)`** — `bot.run` is sync (blocking forever). Mock `bot.run` z `MagicMock()` w testach żeby `Command.handle()` nie hang'ował: `monkeypatch.setattr("discord_bot.bot.bot.run", MagicMock())`.
- **J — Sprawdź że NIE pulls `discord.py` przez resolution** — `poetry show py-cord` po add, verify że NIE ma `discord.py` w dep tree.

## 🧪 Testing plan

```bash
poetry run pytest tests/unit/discord_bot/test_bot_bootstrap.py tests/unit/discord_bot/test_run_discord_bot_command.py tests/unit/discord_bot/test_error_handler.py --cov=discord_bot.bot --cov=discord_bot.management --cov-report=term -v
```

**Coverage cel:** `discord_bot/bot.py` ~80% (sync_commands API hard to mock kompletnie), `discord_bot/management/commands/run_discord_bot.py` 100%.

**Manual smoke (po merge'u):**
- `DISCORD_BOT_TOKEN="" poetry run python manage.py run_discord_bot` → "DISCORD_BOT_TOKEN not set" + exit.
- Jeśli user ma real token: `DISCORD_BOT_TOKEN=<real> DISCORD_DEV_GUILD_ID=<dev> poetry run python manage.py run_discord_bot` → "Bot logged in as ..." + "Synced commands to dev guild ..." (0 commands bo brak cogów jeszcze).

## 📦 Definition of Done

- [ ] AC spełnione (py-cord dep, bot.py, management command, LOGGING edit, env vars).
- [ ] **2 commity osobne:** `build(deps): add py-cord` + `feat(discord): bot bootstrap + management command + LOGGING dispatcher`.
- [ ] PR zmergowany squash (jeden squash łączy oba commity).
- [ ] CI lint + test zielone.
- [ ] Manual smoke (testowy lub real token) zaobserwowany.
- [ ] Issue zamknięty.
