# Tibiantis Monitor — Progress

## 🎉 Milestone M0 — Bootstrap COMPLETED (2026-04-17)
Wszystkie 3 zadania ukończone, milestone zamknięty.

## 🎉 Milestone M1 — First character scrape COMPLETED (2026-04-22)
Wszystkie 5 zadań ukończone, milestone zamknięty.

## 🎉 Milestone M2 — Auth + GraphQL fundament COMPLETED (2026-04-27)
Wszystkie 4 zadania ukończone, milestone zamknięty. Design spec: [`docs/superpowers/specs/2026-04-22-m2-auth-graphql-fundament-design.md`](docs/superpowers/specs/2026-04-22-m2-auth-graphql-fundament-design.md).

### Ukończone (M0)
- ✅ #1 [M0-D1] Inicjalizacja repo + GitHub + branch protection (2026-04-17) — PR [#9](https://github.com/bgozlinski/tibiantis-scraper/pull/9) — squash `d611e2a`
- ✅ #2 [M0-D2] Django 6 project + Postgres lokalnie + pierwszy runserver (2026-04-17) — PR [#10](https://github.com/bgozlinski/tibiantis-scraper/pull/10) — squash `cc89de3`
- ✅ #3 [M0-D3] pre-commit + ruff + mypy + CI lint (2026-04-17) — PR [#11](https://github.com/bgozlinski/tibiantis-scraper/pull/11) — squash `1f9b072`

### Ukończone (M1)
- ✅ #4 [M1-D4] apps/ struktura + app `characters` zarejestrowana (2026-04-17) — PR [#12](https://github.com/bgozlinski/tibiantis-scraper/pull/12) — squash `10bbf44`
- ✅ #5 [M1-D5] Model `Character` + migracja + admin + test job w CI + pierwszy test (2026-04-18) — PR [#14](https://github.com/bgozlinski/tibiantis-scraper/pull/14) — squash `831344c`
- ✅ #6 [M1-D6] Service layer: `upsert_character()` (2026-04-18) — PR [#16](https://github.com/bgozlinski/tibiantis-scraper/pull/16) — squash `04d1b88`
- ✅ #7 [M1-D7] Scrapy: minimalny spider `character_spider` (2026-04-18) — PR [#18](https://github.com/bgozlinski/tibiantis-scraper/pull/18) — squash `114ff86`
- ✅ #8 [M1-D8] Pipeline Scrapy → service + management command (2026-04-22) — PR [#25](https://github.com/bgozlinski/tibiantis-scraper/pull/25) — squash `75e516c`

### Ukończone (M2)
- ✅ #28 [M2-D9] accounts app + custom User + AUTH_USER_MODEL (2026-04-22) — PR [#33](https://github.com/bgozlinski/tibiantis-scraper/pull/33) — squash `56961b3`
- ✅ #29 [M2-D10] REST auth endpoints (register/login/refresh/logout) (2026-04-24) — PR [#35](https://github.com/bgozlinski/tibiantis-scraper/pull/35) — squash `af582d5`; follow-up testy PR [#36](https://github.com/bgozlinski/tibiantis-scraper/pull/36) — squash `7abfb5f`
- ✅ #30 [M2-D11] Strawberry schema + `/graphql/` + `me` query (2026-04-24) — PR [#40](https://github.com/bgozlinski/tibiantis-scraper/pull/40) — squash `0e3067e`; follow-up testy PR [#41](https://github.com/bgozlinski/tibiantis-scraper/pull/41) — squash `7e7793b`
- ✅ #31 [M2-D12] JWT w GraphQL + `character(name)` + e2e test (2026-04-27) — PR [#43](https://github.com/bgozlinski/tibiantis-scraper/pull/43) — squash `1371e20`; follow-up testy PR [#44](https://github.com/bgozlinski/tibiantis-scraper/pull/44) — squash `2e3d170`


### Ukończone (M4)
- ✅ #78 [M4-D18] DeathEvent model + admin + initial migration (2026-05-05) — PR [#84](https://github.com/bgozlinski/tibiantis-scraper/pull/84) — squash `268d411`
- ✅ #79 [M4-D19] deaths_spider + DeathItem + pipeline dispatch (2026-05-06) — PR [#85](https://github.com/bgozlinski/tibiantis-scraper/pull/85) — squash `fe0429a`
- ✅ #80 [M4-D20] `save_death_event` service + `scrape_deaths` mgmt command + pipeline counter (2026-05-07) — PR [#86](https://github.com/bgozlinski/tibiantis-scraper/pull/86) — squash `6ee3063`
- ✅ #81 [M4-D21] Celery `scrape_deaths` task + PeriodicTask seed migration + `DEATH_LEVEL_THRESHOLD` env (2026-05-07) — PR [#88](https://github.com/bgozlinski/tibiantis-scraper/pull/88) — squash `bc3d871`
- ✅ #82 [M4-D22] `recentDeaths` GraphQL query + e2e test + M4 closure (2026-05-07) — PR [#90](https://github.com/bgozlinski/tibiantis-scraper/pull/90) — squash `dbfa298`

### Notatki z retro M0
- **#1 (merge 2026-04-17):** Issue #1 wymagał drobnego fixup commita — w pierwotnym commicie brakowało 8 wzorców z AC. Wniosek: warto przed push przeklikać AC checklist linia po linii.
- **#2 (merge 2026-04-17):** dwa drobne bugi złapane w lokalnym review przed pushem (`env("DJANGO_SECRET_KEY,")` z przecinkiem i `BASE_DIR = parent.parent` zamiast `parent.parent.parent` w subpackage settings). Wniosek: przy rozbijaniu plików sprawdzaj wszystkie ścieżki względne.
- **#3 (merge 2026-04-17):** długa walka z CI (11 commitów fixów). Kluczowe odkrycia:
  - Poetry 2.x + PEP 621 `[project]` wymaga spójności: pusta sekcja `[tool.poetry]` wymusza legacy mode i blokuje PEP 621.
  - `requires-python` w `[project]` musi być PEP 440 (`>=3.13,<4.0`), nie Poetry caret (`^3.13`).
  - `[dependency-groups]` (PEP 735) w Poetry 2.0.x nie jest instalowane przez `--with dev` ani `--all-groups`. **Uzupełnione po #5 (2026-04-18):** nawet w Poetry 2.1.4 `--all-groups` nie obejmuje PEP 735 groups — dopiero `[tool.poetry.group.dev.dependencies]` (Poetry-native) działa przewidywalnie. Wniosek: używaj Poetry-native groups dla CI-heavy projektów, PEP 735 w Poetry nie jest jeszcze battle-tested.
  - Ostateczne rozwiązanie dla lint joba: **pre-commit bez Poetry** — `pip install pre-commit` + `pre-commit run --all-files`. Pre-commit ma własne isolated envs per hook, Poetry niczego tu nie daje. Ten wzorzec przetrwa dla `lint` joba, test job dostanie Poetry w swoim czasie.
  - **Wniosek:** zapisywać w CLAUDE.md aktualne wersje tylko jak się potwierdzą w CI, nie z głowy. Sesja #3 zjadła sporo czasu bo rev hooków (Poetry 1.8.4) i python-version ("3.12") w CLAUDE.md nie odpowiadały rzeczywistej instalacji (Poetry 2.x, Python 3.13) — przenosiliśmy CLAUDE.md "wyprzedająco", ale rzeczywistość trafiała dopiero po wielu iteracjach.
- **Solo-repo paradox:** GitHub blokuje self-approval własnego PR, dlatego Claude nie może `--approve` — zostawia komentarz LGTM. Branch protection ma `enforce_admins=false`, więc Ty mergeujesz jako admin.
- **Squash-only enforced (2026-04-17):** repo skonfigurowane tak, że UI GitHuba pokazuje tylko „Squash and merge" (`allow_merge_commit=false`, `allow_rebase_merge=false`).

### Obserwacje techniczne do adresowania w kolejnych issues
- ~~W `config/settings/base.py` brakuje `DEFAULT_AUTO_FIELD`~~ — rozwiązane w #4.
- ~~`CharactersConfig` ma redundantny `default_auto_field`~~ — rozwiązane w #5.
- ~~`INSTALLED_APPS` 2-way split~~ — rozwiązane w #5 (3-way z `DJANGO_APPS`/`THIRD_PARTY_APPS`/`LOCAL_APPS`).
- ~~Test job w CI~~ — rozwiązane w #5 (Postgres 16 service + pytest + coverage).
- ~~`dev.py` hardcoduje `DEBUG = True` i `ALLOWED_HOSTS = ['*']`, override'ując wartości z env~~ — rozwiązane w #49 (PR #53), `env.bool()` + `env.list()` z defaultami.
- ~~`django-upgrade` target pinowany na `5.1` (maks. który narzędzie zna w rev `1.22.1`)~~ — rozwiązane w PR #46 (rev `1.30.0`, target Django `6.0`).
- **Tech debt z #5 (do adresowania post-M1):**
  - ~~`Meta.indexes = [models.Index(fields=["name"])]` — redundant z `unique=True` na `name` (już tworzy unique btree)~~ — rozwiązane w #38 (PR #39, schema drift wykryty przy #29).
  - `admin.ModelAdmin` bez generic parameter (`# type: ignore[type-arg]` zamiast `ModelAdmin[Character]`). Alternatywa — `django-stubs-ext.monkeypatch()` w `base.py`. Decyzja na później.
  - ~~`coverage threshold = 0` — AC #5 Pułapka F mówi podnosić do 70 w osobnym PR post-M1~~ — rozwiązane w #50 (PR #51), próg podniesiony do 70%.
  - Branch protection master: dodać required status check `test / Pytest` (obecnie tylko `lint`).
  - ~~CLAUDE.md §12 pokazuje `[dependency-groups]` (PEP 735) jako pattern — w praktyce okazało się że Poetry 2.1.4 nie instaluje takich groups przez `--all-groups`~~ — rozwiązane w PR #47 (CLAUDE.md §15.16 explicit ostrzeżenie).
- **Tech debt z #6 (do adresowania post-M1):**
  - ~~Brak docstringa na `upsert_character()`~~ — rozwiązane w #8 (docstring opisuje kontrakt + race window).
  - ~~Race condition w `update_or_create`~~ — rozwiązane w #8 (retry na `IntegrityError` w osobnym `transaction.atomic()` savepoincie).
- **Tech debt z #7 (do adresowania post-M1):**
  - ~~**Bug `self.name` vs `self.character_name` w `character_spider.py:30`** — log warning dla postaci "not found" pokazuje nazwę spidera (`"character"`), nie imię postaci~~ — rozwiązane w #21 (PR #22) z parametrized regression guards.
  - ~~**Bug `_parse_last_login` crashuje na "Never logged in"** — `rsplit(" ", 1)` na takim stringu da `("Never logged", "in")` i `strptime` rzuci `ValueError`~~ — rozwiązane w #21 (PR #22), early return + fixtury edge case.
  - **Brak defensywnego parsowania `level`** — `int(level_raw)` pada gdy layout strony się zmieni lub pojawi się np. `"118 (deleted)"`. Rozważyć regex `^\d+` albo try/except.
  - **`_parse_last_login` hardcoduje `Europe/Berlin`** zamiast walidować odczytaną TZ (`_tz` jest odrzucona). Nit, ale gdyby serwer kiedyś wysłał inny TZ, cichy bug. Minimum: assert `_tz in {"CEST","CET"}` albo warning.
  - **Fragile fixture path `parents[3]`** w `tests/unit/scrapers/test_character_spider.py` — przy przeniesieniu katalogu cicho zwróci złą ścieżkę. Refactor do `conftest.py` w `tests/`.
  - **Deviation od AC#7** (świadoma, nie bug): `scrapy.cfg` w rootcie repo zamiast `scrapers/`, wszystkie importy z prefiksem `scrapers.tibiantis_scrapers...`. Konsekwencja dla #8: management command musi ustawić `SCRAPY_SETTINGS_MODULE=scrapers.tibiantis_scrapers.settings` (z prefiksem) i uruchomić crawl z roota repo.

### Notatki z retro M1
- **#6 (merge 2026-04-18):** PyCharm auto-import wrzucił `from IPython.core.magic_arguments import defaults` do `types.py` bo zmienna lokalna nazywała się `defaults`. Wniosek: po napisaniu service przelecieć wzrokiem top-of-file imports, PyCharm czasem halucynuje. Drugi wniosek: mieszanie `services.py` i `types.py` w jednym pliku (pierwotnie wszystko w `types.py`) złapane w review — zgodnie z CLAUDE.md §3 logika do `services.py`, typy osobno. Trzeci: funkcja deklarowała `-> Character` ale nie miała `return` — mypy strict by to złapał, ale warto przed pushem odpalić `poetry run mypy apps/` lokalnie zamiast liczyć na CI.
- **#7 (merge 2026-04-18):** Spider parsuje fixture HTML poprawnie (6 testów green), wszystkie pola modelu pokryte. W retro-review wyszły 2 bugi niewyłapane przez testy (`self.name` w logu = nazwa spidera, nie postaci; `_parse_last_login` crashuje na "Never logged in") → follow-up PR dopisze fixturki edge-case i pokryje te ścieżki, bugi do naprawy w osobnym issue. **"Dependency conflicts" w PR title** sugeruje walkę z Poetry przy `poetry add scrapy` — warto spisać co się wydarzyło (bg: Scrapy pociąga Twisted + cryptography + lxml, potencjał kolizji z django-stubs). Świadoma deviation od AC: `scrapy.cfg` w root repo + prefiks `scrapers.` w importach → uprości #8 (brak `cd scrapers/`), ale w management command będzie `SCRAPY_SETTINGS_MODULE=scrapers.tibiantis_scrapers.settings` zamiast `tibiantis_scrapers.settings`.
- **#8 (merge 2026-04-22):** Cztery AC, pięć commitów (`build(deps)`, AC1 pipeline, AC2 command, AC3 tests, AC4 race retry). Wyzwania:
  - **Scrapy 2.x async pipelines** — `process_item` musi być `async def` i ORM-y owinąć `sync_to_async`; pierwsze podejście (sync) dało `SynchronousOnlyOperation` przy pierwszym scrapie.
  - **Twisted reactor + crochet + Windows** — trzy gotchas w jednej linii: (1) `asyncioreactor.install()` MUSI być przed `from crochet import ...`, (2) na win32 `WindowsSelectorEventLoopPolicy` zamiast ProactorEventLoop (niekompatybilny z `asyncioreactor`), (3) w `scrapers/settings.py` trzeba dodać `django.setup()` żeby direct `scrapy crawl` też działał.
  - **Pre-commit mypy w isolated env** — bez `scrapy`/`crochet`/`twisted` w `additional_dependencies` hook nie widział base classes → landed jako osobny PR #24 zgodnie z CLAUDE.md §12 (infrastruktura odseparowana od kodu funkcjonalnego).
  - **AC4 race condition** — rozwiązanie: jeden retry w `except IntegrityError`, każda próba w swoim `transaction.atomic()` savepoint (bez tego aborted transaction po pierwszym `IntegrityError` blokuje retry). Sygnał z retro #6 zadziałał — był flagowany jako tech debt do #8.
  - **Ops blunder — accidental duplicate PR #26:** po PR #25 (zmergowany) otworzyłem docs/close-m1-tracker z zamiarem aktualizacji PROGRESS.md, ale branch był utworzony OD feat/8 (nie od master) i nigdy nie dopisał się commit z PROGRESS.md. Pushed + zmergowany jako PR #26 = no-op squash `fe805eb` na master duplikujący treść PR #25. Wniosek: **zawsze twórz docs-close branch OD świeżego master po merge'u feature'a**, nie od feature-brancha. `git checkout master && git pull && git checkout -b docs/...` jako stały procedurę.
  - **Wniosek:** Scrapy + Django + Celery-ready = 3 event loopy (Twisted, asyncio, Django sync). Kluczowe było trzymać integrację w jednym miejscu (management command + pipeline) i trzymać pipeline po stronie async.

### Notatki z retro M2
- **#28 (merge 2026-04-22):** Implementacja minimalistyczna zgodnie ze spec §5/D9 — 5 linii `models.py`, 10 linii `admin.py`. R1 (kolejność migracji) zweryfikowane pre-flightem (żadnego FK na User jeszcze nie ma), R4 (`UserAdmin.add_fieldsets` vs password fields) zaadresowane przez spread `*(BaseUserAdmin.add_fieldsets or ())` który zachowuje `password1`/`password2` z base + doklejenie sekcji Discord. PR zmerge'owany bez code review (solo-repo paradox z retro M0 — self-approval zablokowany przez GitHub, mergujemy jako admin). Testy jednostkowe dopisywane follow-upem zgodnie z workflow.
- **Tech debt z #28 (do adresowania post-M2):**
  - **`db_index=True` + `unique=True` na `discord_id`** — redundant (Postgres `UNIQUE` tworzy btree automatycznie). Ten sam pattern co `Character.name` flagowany w retro M1 (#5 tech debt). Kandydat na chore PR razem z Character cleanup + regeneracja migracji.
  - **Escaped markdown w body PR #33** (`\##`, `&#x20;`, `\\\`) — `gh pr create --body` na Windows zjada heredoc/quoting inaczej niż na Linux. Rozwiązanie na przyszłość: pisać body do pliku tymczasowego i używać `--body-file`, albo PR-y z UI GitHuba gdy treść ma markdown formatting.
- **#29 (merge 2026-04-24):** REST auth (DRF + simplejwt) + 9 testów endpointów jako follow-up PR #36. Cztery rundy review serializera zanim wszedł na branch — klasyczne pułapki DRF złapane w review, nie w runtime:
  - `serializers.Serializer` + `Meta.model/fields` — `Meta` ignorowane w `Serializer` (działa tylko w `ModelSerializer`), wybór klasy bazowej to świadoma decyzja.
  - `write_only_fields` w `Meta` nie istnieje (cicho zignorowane) — prawidłowy mechanizm to `extra_kwargs = {"password": {"write_only": True}}`.
  - `validate_<field>` bez `return value` → `None` trafia do `validated_data`, `create_user(password=None)` robi `set_unusable_password()`. **Classic gotcha DRF.**
  - `django.contrib.auth.password_validation.validate_password` rzuca `django.core.exceptions.ValidationError` — trzeba przepakować na `rest_framework.serializers.ValidationError`, inaczej DRF zwróci 500.
  - Shadowing importu `validate_password` przez metodę o tej samej nazwie — technicznie działa, ale czytelność zero. Alias import.
  - Views: `rest_framework.authtoken` to **inny system** niż simplejwt (DRF Token auth, nie JWT) — łatwo pomylić przy pierwszym kontakcie. Re-export z simplejwt jest minimalny (3 linie), custom subclassy nie są potrzebne.
  - **`IntegrityError` przy migracji `email unique=True`** — istniejący superuser miał `email=''`, Postgres traktuje `''` jako wartość (nie `NULL`), więc unique constraint widzi duplikaty. Fix dev: `User.objects.filter(email='').delete()` + migrate. Production pattern: osobna data-migration z backfill **przed** schema-migration z constraint. Zapamiętać.
  - **Dryft schema:** `makemigrations` wygenerowało niepowiązaną migrację `apps/characters/0002_remove_character_characters__name_6d8b81_idx` (tech debt z retro M1 #5 — redundant index był nadal w `0001_initial` ale nie w modelu). Świadomie **nie weszła do PR #35** — osobny fix PR w planach.
  - **Windows CRLF vs LF:** `mixed-line-ending` hook failuje na każdym edycie w Windows, ale auto-fixuje. `git add -u` po hookach wystarczy. Gdyby ktoś commitował z Linuksa po mnie, CI byłby zielony od pierwszego strzału.
  - **Mypy strict na DRF:** `ModelSerializer[User]`, `CreateAPIView[User]`, `validated_data: dict[str, Any]` — `djangorestframework-stubs` daje generics, warto parametryzować od razu, inaczej pre-commit `mypy` blokuje commit (`no-untyped-def`, `type-arg`).
- **#30 (merge 2026-04-24):** R5 pre-flight (`poetry add strawberry-graphql-django --dry-run` na Django 6) zwrócił OK — bez planu B. Implementacja: `apps/accounts/schema.py` (UserType + `me` z `sync_to_async` dla `request.user` LazyObject). Follow-up testowy PR [#41](https://github.com/bgozlinski/tibiantis-scraper/pull/41) — split per concern: `test_graphql_me.py` (introspection + me sync) + `test_graphql_async_canary.py` (jeden test pełnego async flow). Decyzja split okazała się **kluczowa w D12** — async canary natychmiast złapał regresję `request.user = AnonymousUser()` w nowym JWT dispatch (intent w nazwie pliku ułatwił post-mortem). Wniosek: testy z explicit "canary" intent trzymać osobno, nie zlewać w jeden test_graphql.py.
- **#31 (merge 2026-04-27):** Trzy iteracje code review zanim mypy strict zielony — Strawberry + DRF + simplejwt mają nakładające się gotcha:
  - **`JWTAuthentication.authenticate()` wymaga `rest_framework.request.Request`**, nie `django.http.HttpRequest`. Runtime działa przez duck-typing (auth czyta tylko `request.META`), ale mypy + DRF stubs łapią rozbieżność. Fix: `DRFRequest(request)` **lokalnie** dla auth call. Do `super().dispatch()` idzie oryginalny Django request — Strawberry oczekuje `HttpRequest`, nie DRFRequest.
  - **`# type: ignore[override]` na `dispatch`** — Django stuby twierdzą `View.dispatch` jest sync, Strawberry override'uje na async. Strawberry sam ma `# pyright: ignore` w źródle. Niezgodność stubów, nie bug — utrwalone w ekosystemie.
  - **`# type: ignore[arg-type]` na `sync_to_async(_authenticator.authenticate)`** — generic `AuthUser` TypeVar w stubach DRF nie przekłada się przez `sync_to_async` wrapper (mypy degraduje TypeVar do `Never`). Znana limitation type inference dla generic Callable, runtime bezbłędne.
  - **`afirst()` bez `await`** — async ORM zwraca coroutine, bez `await` zwracasz `<coroutine>` zamiast `Character | None`. Mypy nie złapie (signature pasuje formalnie do `Awaitable[T | None]`), ale Strawberry serializer rzuci runtime error. **Smoke `curl` + manual eyeballing pierwszego review pozwolił złapać przed CI.**
  - **`request.user = AnonymousUser()` w `else`** wybuchło w testach #41 (async canary celowo dla tego scenario). Bug: dispatch zawsze nadpisywał `request.user`, kasował to co session middleware (lub `force_login`) ustawił. Skutek prod: zalogowany przez session admin tracił auth na `/graphql/`. Fix: nadpisuj **tylko** w `except AuthenticationFailed` (explicit zły token = explicit anonymous); brak headera = no-op.
  - **`merge_types(...)` z Strawberry tools** zamiast multiple inheritance — flat merge pól, hard fail przy konflikcie nazw przy starcie schemy. Dziedziczenie cicho rozwiązuje przez MRO = anti-pattern dla GraphQL.
  - **Pierwszy plik w `tests/integration/`** (CLAUDE.md §3 + spec D12 §7 — "integration e2e tylko w D12"). Założono katalog z `__init__.py`. Test e2e używa **dwóch klientów**: `APIClient` (sync DRF) dla `/api/auth/*` i `AsyncClient` (async Django) dla `/graphql/`. Mieszanka jest konieczna — sync REST view zawinięty w AsyncClient zamaskowałby SynchronousOnlyOperation, na odwrót Strawberry AsyncGraphQLView wymaga async clienta.

### Podsumowanie M2 (2026-04-22 → 2026-04-27, 5 dni vs 4 z budżetu)
- 4 Issues (#28-#31) + 4 follow-up testowe (#36, #41, #44) + 4 docs PR (#34, #37, #42, ten) + 1 fix PR (#39 schema drift z M1 #5 tech debt). Total 12 PR.
- **Strict chain D9→D10→D11→D12 zachowany** — żadnego paralelizmu, każdy issue po pełnym merge poprzedniego.
- **DoD M2 spełnione:** REST auth (register/login/refresh/logout) działa, JWT autentykuje `/graphql/`, mixed query `{ me + character(name) }` zwraca dane. E2E test pokrywa cały flow.
- **Co przeniesione na post-M2 jako tech debt — wszystkie zamknięte w ciągu 24h po M2-closure (sweep chore PR-ów #46/#47/#51/#52/#53):**
  - ~~Redundant index na `Character.name` (`unique=True` + `db_index=True` przez Meta.indexes)~~ — rozwiązane w #38 (PR #39, w trakcie M2).
  - ~~Redundant `db_index=True + unique=True` na `User.discord_id`~~ — rozwiązane w #48 (PR #52).
  - ~~`coverage threshold = 0`~~ — rozwiązane w #50 (PR #51), próg podniesiony do 70%.
  - ~~`dev.py` hardcoduje `DEBUG=True` + `ALLOWED_HOSTS=['*']`~~ — rozwiązane w #49 (PR #53).
  - ~~`django-upgrade` target pinowany na `5.1`~~ — rozwiązane w PR #46 (rev `1.30.0`, target Django `6.0`).
- **Realny tech debt otwarty po M2 (do adresowania w M3 lub osobnych chore):**
  - `admin.ModelAdmin` bez generic parameter — z retro M1 #5; alternatywa `django-stubs-ext.monkeypatch()` w `base.py`. Decyzja na później.
  - Branch protection `master` nie ma required status check `test / Pytest` (tylko `lint`). Dodać w GitHub Settings → Branches.
  - Spider defensive parsing (z retro #7, częściowo niezamknięte przez #21):
    - `int(level_raw)` w `character_spider.py:49` — pada na np. `"118 (deleted)"`; potrzebny regex `^\d+` lub try/except.
    - `_parse_last_login` hardcoduje `Europe/Berlin` zamiast walidować odczytaną TZ (`_tz` jest odrzucony) — cichy bug gdyby serwer kiedyś zmienił TZ. Minimum: assert `_tz in {"CEST","CET"}` albo warning.
    - Fragile fixture path `Path(__file__).resolve().parents[3]` w `test_character_spider.py:15` — przy reorg katalogów cicho zwróci złą ścieżkę. Refactor do `conftest.py` w `tests/`.
- **Najwartościowsza lekcja M2:** **mini-retro w trakcie milestone** (po 3/4 issuesach) zadziałała — drift schema check przed #31 oszczędził minimum jedną rundę naprawy. Zachować jako pattern dla M3.

## 🎉 Milestone M3 — Celery infrastructure COMPLETED (2026-04-29)

**Design spec:** [`docs/superpowers/specs/2026-04-28-m3-celery-fundament-design.md`](docs/superpowers/specs/2026-04-28-m3-celery-fundament-design.md) — zmergowany PR [#57](https://github.com/bgozlinski/tibiantis-scraper/pull/57) (squash `f599c69`).
**Budżet:** 5 dni roboczych (~16-20h, z buforem +1d po lekcji M2).
**Strict chain:** D13 → D14 → D15 → D16 → D17 (zero paralelizmu, każdy Issue po pełnym merge poprzedniego — kontynuacja konwencji M0-M2).
**Świadomie wąski scope:** Celery + Beat + Redis + scheduled `Character.objects.all()` scrape. **Bez** Discorda, bedmage trackera, deathów, Mongo loggingu — to M4+.

### Ukończone (M3)
- ✅ [#58](https://github.com/bgozlinski/tibiantis-scraper/issues/58) [M3-D13] docker-compose.dev.yml + Celery deps + django-celery-beat (2026-04-28) — PR [#64](https://github.com/bgozlinski/tibiantis-scraper/pull/64) — squash `9838696`
- ✅ [#59](https://github.com/bgozlinski/tibiantis-scraper/issues/59) [M3-D14] Celery app config + ping task (2026-04-28) — PR [#68](https://github.com/bgozlinski/tibiantis-scraper/pull/68) — squash `14ddb76`; deps via PR [#66](https://github.com/bgozlinski/tibiantis-scraper/pull/66) (mypy additional_dependencies) + PR [#67](https://github.com/bgozlinski/tibiantis-scraper/pull/67) (mypy module overrides); follow-up testy PR [#70](https://github.com/bgozlinski/tibiantis-scraper/pull/70) — squash `72770df`
- ✅ [#60](https://github.com/bgozlinski/tibiantis-scraper/issues/60) [M3-D15] Worker + Beat dev runners + DatabaseScheduler default (2026-04-28) — PR [#71](https://github.com/bgozlinski/tibiantis-scraper/pull/71) — squash `ba4cd35`
- ✅ [#61](https://github.com/bgozlinski/tibiantis-scraper/issues/61) [M3-D16] `scrape_watched_characters` task + Beat schedule (PeriodicTask data migration) (2026-04-29) — PR [#73](https://github.com/bgozlinski/tibiantis-scraper/pull/73) — squash `0ad4c53`
- ✅ [#62](https://github.com/bgozlinski/tibiantis-scraper/issues/62) [M3-D17] Celery e2e test + unit tests + M3 closure (2026-04-29) — PR [#75](https://github.com/bgozlinski/tibiantis-scraper/pull/75) — squash `ae25cfc`

### Pre-flight checklist (przed startem D13, ze spec'a §10) — ✅ wszystkie domknięte
- [x] **Docker Desktop / Engine** zainstalowany (`docker --version` 28.5.1, `docker compose version` v2.40).
- [x] **Decyzja portu:** **5435** (lokalny Postgres na 5432 + inne projekty Docker na 5433-5434 zajmują niższe porty) — udokumentowane w PR [#64](https://github.com/bgozlinski/tibiantis-scraper/pull/64) i `.env.example`.
- [x] `poetry add celery redis django-celery-beat --dry-run` zwraca OK (brak konfliktu z Django 6, simplejwt, strawberry-django) — finalne wersje: `celery 5.6.3`, `redis 7.4.0`, `django-celery-beat 2.9.0`.
- [x] Milestone "M3 — Celery infrastructure" utworzony, 5 Issues (#58-#62) z linkami do spec'a.

### Notatki z retro M3 (dopisywane progresywnie)
- **#58 (merge 2026-04-28):** Trzy classic Postgres image gotchas złapane przy smoke testach przed mergem:
  - **`POSTGRES_NAME` ≠ `POSTGRES_DB`** — Postgres image rozpoznaje **wyłącznie** `POSTGRES_USER`/`POSTGRES_PASSWORD`/`POSTGRES_DB` (case-sensitive, exact match). `POSTGRES_NAME` z pierwszej wersji `.env` (skopiowane z innego projektu) było cicho ignorowane → fallback do default user/db. Lekcja: `.env.example` jest jedynym źródłem prawdy dla nazw zmiennych, `.env` musi być 1:1.
  - **`initdb` only-on-first-start** — Postgres image tworzy role + bazę z `POSTGRES_*` **tylko** przy starcie na pustym volume. Każda kolejna zmiana `POSTGRES_*` w `.env` jest cicho ignorowana, dopóki volume nie zostanie zrzucony (`down -v`). To powtarzalna pułapka — w prod (M9) z prawdziwymi danymi koszt by był znacznie wyższy (`pg_dump` + recreate + restore zamiast `down -v`).
  - **`DATABASE_URL` musi być w pełni zsynchronizowany z `POSTGRES_*` + portem** — to dwa różne kanały konfiguracji do tej samej bazy (`POSTGRES_*` → kontener przy initdb, `DATABASE_URL` → Django URL parser). Brak synchronizacji = sukces `up -d` + fail Django connect (silent w produkcji). Refactor "konstruuj DSN z komponentów w settings" (Opcja B z dyskusji) odłożony na osobny chore PR po D13 — DRY trade-off vs scope creep, scope wygrał.
- **#58 deviation od spec'a:** port hosta **5435** zamiast 5433 (z spec D13 AC), bo lokalny Postgres + `scrapper-service-db` (inny projekt Docker) zajmowały 5432-5434. Świadoma decyzja, udokumentowana w PR + README + `.env.example`.
- **#58 ops:** PR description nie zawierał `Closes #58` → Issue nie auto-zamknął się przy mergu, zamknięty ręcznie przez `gh issue close 58`. Lekcja na M3-D14+: w PR body dodać explicit `Closes #<N>`.
- **#59 (merge 2026-04-28):** Trzy chore PR-y "satellite" wymagane wokół jednego feature commit, plus jeden fix-up CI w trakcie:
  - **`@shared_task` + mypy strict** — Celery 5.6 nie ma kompletnych typed signatures dla `shared_task`/`@app.task`. Inline `# type: ignore[misc]` failuje cross-env (pre-commit env raportuje `[misc]`, direct `poetry run mypy` raportuje `[untyped-decorator]` — różne mypy versions/configs w isolated envs dają różne error codes). Final approach: per-module override w `pyproject.toml` (`disallow_untyped_decorators = false` dla `apps.*.tasks`) — granularny, działa identycznie w obu envs, DRY dla M3-D16/M4 future tasks. Landed jako PR #67.
  - **Pre-commit isolated env vs poetry venv** — `poetry add celery` dał paczkę do venv aplikacji, ale **pre-commit mypy hook biega w odseparowanym sandboxie** (`additional_dependencies` w `.pre-commit-config.yaml`). Bez `celery` + `django-celery-beat` w tej liście → `ModuleNotFoundError` przy `pre-commit run mypy`. Precedens był M1 #24 (scrapy/crochet/twisted). Każda nowa Python lib importowana w `apps/` wymaga **drugiego dodawania** w pre-commit config — repeatable gotcha. Landed jako PR #66.
  - **CI env vars sync z `settings/base.py`** — dodanie `env("CELERY_RESULT_BACKEND")` w `base.py` bez default'u → `ImproperlyConfigured` przy `manage.py migrate` w CI test job (env nie ustawione w `.github/workflows/ci.yml`). Fix-up commit w D14 PR (`fix(ci): add CELERY_RESULT_BACKEND env`). M0 retro #3 lekcja "11 commitów fixów CI" wraca — settings w trybie strict (no defaults) + env vars w CI = checklist do trzymania razem.
- **#59 lekcja procesu** — *przy każdym dodaniu deps lub `env(...)` w settings sprawdzić TRZY miejsca przed pushem:*
  1. `pyproject.toml` (poetry add) — venv aplikacji
  2. `.pre-commit-config.yaml` mypy `additional_dependencies` — pre-commit isolated env (jeśli importowany w `apps/`)
  3. `.github/workflows/ci.yml` env block — CI test job (jeśli `env(...)` w `settings/base.py` bez default)
  Sanity przed pushem: `poetry run pre-commit run --all-files` ZIELONY + `git grep -l "env(" config/settings/` vs `grep "env_var:" .github/workflows/ci.yml` ręczna kontrola.
- **#59 deviation od spec'a:** żadna — wszystkie trzy AC fix'y (settings.dev zamiast base, `Celery("tibiantis")` zamiast "config", drop `debug_task`) zaaplikowane po review. Spec D14 §AC był niezmieniony.
- **#59 ops:** `Closes #59` w PR body zadziałało — Issue auto-closed at merge. Lekcja z #58 ops zaaplikowana skutecznie.
- **#60 (merge 2026-04-28):** Najczystszy issue M3 — 1 linia config (`CELERY_BEAT_SCHEDULER`) + sekcja README "Running Celery dev" z 3-terminalowym smoke (worker `-P solo`, beat, runserver). Zero satellite chore PR-ów, zero dramatów (kontrast z #59 wymagającym #66 + #67 + fix-up CI). Smoke przeszedł od pierwszego strzału — beat wystartował z `Scheduler: django_celery_beat.schedulers.DatabaseScheduler` od razu (settings podchwycone), worker wykrył `apps.characters.tasks.ping` przez autodiscover.
- **#60 lekcja:** stricte docs/config issues idą szybko po dramach z infra (#58) i mypy/Celery typing (#59). M3 budżet 5 dni — po 3 issues mamy ~6-8h work, na harmonogramie lub lekko wyprzedzeniu. Pozostają #61 (D16, infra-heavy: subprocess Twisted, freshness threshold, data migration) i #62 (testy + closure).
- **#60 ops:** `Closes #60` w PR body zadziałało — Issue auto-closed.
- **#61 (merge 2026-04-29):** Najgrubszy issue M3 (~4h spec'em), ale fix-cycle zjadł czas — pełny code review z 2 critical bugs + 4 suggestions zanim implementacja przeszła smoke. Detale:
  - **Critical bug 1 — settings name mismatch:** task używał `getattr(settings, 'SCRAPING_THRESHHOLD_MINUTES', 30)` (z literówką + Scraping prefix), settings deklarował `CELERY_SCRAPE_FRESHNESS_MINUTES`. `getattr` cicho fallbackowało do hardcoded `30` — runtime override z `.env` nigdy by nie zadziałał. Złapane w code review przed mergem. **Lekcja:** kiedy settings + task używają tego samego symbolu, **sprawdzaj match nazw 1:1 zaraz po dodaniu** — łatwa pułapka jak settings w jednym pliku, task w innym.
  - **Critical bug 2 — counter swap:** w bloku "fresh skip" inkrementował `scraped += 1` zamiast `skipped += 1`. Fix-cycle zaalarmował dopiero gdy smoke pokazał `{"scraped": 1, "skipped": 0}` zamiast spodziewanego `{"scraped": 0, "skipped": 1}` na fresh character. **Lekcja na D17 testy:** explicit assertion na **oba** counters (`scraped == X AND skipped == Y`), nie tylko jeden.
  - **`@shared_task(bind=True)` + mypy strict cross-env:** identyczny pattern co w #59 — pre-commit isolated env (z `celery` w additional_dependencies, brak py.typed marker) dawał `[no-untyped-def]` na `self` parameter, direct `poetry run mypy` (z `[celery, celery.*] ignore_missing_imports = true` override z #67) `Success`. Fix: `self: Any` workaround. **Trzeci `mypy cross-env gymnastics` w M3** — wzorzec sugeruje `celery-types` package w post-M3 chore PR jako trwałe rozwiązanie wszystkich one-by-one workarounds.
  - **Subprocess gotcha — spec D16 vs M1 cmd:** spec używał `["scrape_character", "--name", name]` (named), M1-D8 cmd ma `parser.add_argument("name")` (positional). Wykryte w pre-flight przed implementacją (`grep "add_arguments" apps/characters/management/commands/scrape_character.py`). Spec'y design'owe pisane przed sprawdzeniem konkretnych M1 sygnatur są ryzykiem — pre-flight grep cmd args jest taniym sanity.
  - **`auto_now=True` blokuje set przy `create()`** — pułapka B z spec'a D17 §AC, weryfikowana podczas testów seed: `Character.objects.create(name='StaleTester', last_scraped_at=...)` ignoruje passed value (auto_now overrideuje przy save). Workaround: osobny `update()` po create. Klasyczna Django gotcha — D17 testy będą używać tego patternu.
- **#61 deviation od spec'a:** subprocess call z positional `name` zamiast `--name <value>` (spec'em §5/D16) — udokumentowane w pre-flight + PR description. Spec design pisany przed sprawdzeniem M1 cmd sygnatury.
- **#61 ops:** `Closes #61` w PR body zadziałało — Issue auto-closed at 2026-04-29T00:11:36Z. Solo-repo paradox: Claude zostawił LGTM (self-approve zablokowany przez GitHub), user zmerge'ował jako admin via `enforce_admins=false`.
- **#62 (merge 2026-04-29):** Closure issue M3 — testy follow-up per workflow (3 unit + 1 integration e2e). PR #75 zielony od pierwszego strzału w CI (lint 1m30s, pytest 1m18s), zero satellite chore PR-ów, zero CI fix-up commits. Detale:
  - **Mock path discipline — `apps.characters.tasks.subprocess.run`** (nie `subprocess.run` globalnie). Klasyczne "patch where it's used, not where it's defined" — gdyby ktoś przeniósł `subprocess.run` w refactorze do innego modułu (np. `apps.characters.runners.run_subprocess`), patch by cicho ucichł i live spider hit'tałby tibiantis. **Defensive guard:** `mock.assert_called_once_with(...)` z explicit argumentami w teście success path — pozytywna walidacja, nie tylko negatywna.
  - **Pułapka B z #61 zaadresowana w testach:** `auto_now=True` na `last_scraped_at` ignoruje passed value w `create()`. Helper `_make_stale_character()` w unit tests + inline `Character.objects.filter(pk=...).update(last_scraped_at=tz_dt)` w integration. `update()` omija `save()` → omija `auto_now`. Klasyczna Django gotcha, **dwa miejsca** w testach M3 ją trafiły (#61 implementation + #62 testy) — wzorzec wart zapamiętania.
  - **`apply()` vs `delay()` w testach** — `apply()` zawsze sync, ignoruje `CELERY_TASK_ALWAYS_EAGER`. Predictable dla 3 unit + 1 integration testów. `delay()` w `test_ping_returns_pong_via_eager_delay` (z #70) używa eager flag — różny intent, różny tool.
  - **`mock.assert_not_called()` jako positive guard** w testach `freshness_threshold` + `empty_watchlist` — bezpośrednia regresja na critical bug 2 z retro #61 (counter swap fresh→scraped). Gdyby ktoś znów napisał `scraped += 1` w branchu fresh-skip, test wybuchnie.
  - **Coverage:** 88% lokalnie (próg 70% spełniony z 18pp marginesem), `apps/characters/tasks.py` **100%** (29/29 stmts). Tylko `scrape_character.py` management command (27 stmts, 0%) — pokryty integration testem M1-D8 ale liczony osobno; nie regresja.
  - **CRLF→LF noise w worktree** — pre-commit hook `mixed-line-ending --fix=lf` przy pierwszym `--all-files` przebiegu zafiksował 55 plików (Windows `core.autocrlf=true` renormalizuje CRLF przy commit). **Sanity:** `git diff` na te pliki nic nie pokazuje (warning "LF will be replaced by CRLF" + pusta diff = no real change). Stage tylko 2 plików testowych przez explicit `git add tests/...` — reszta zostaje unstaged i out of commit. Lekcja M2 #29 zaaplikowana skutecznie.
- **#62 deviation od spec'a:** żadna — wszystkie 4 testy (3 unit + 1 integration) zgodne z AC issue #62. Smoke manualny per AC do zrobienia poza PR (3-terminalowy worker + beat + runserver), nie część automated CI.
- **#62 ops:** `Closes #62` w PR body zadziałało — Issue auto-closed at 2026-04-29T11:19:31Z. Solo-repo paradox: Claude zostawił LGTM komentarz, user zmerge'ował jako admin (squash `ae25cfc`).

### Zasady przeniesione z retro M0-M2 (do egzekwowania w M3)
- **Pattern z M2:** mini-retro po 3/4 Issues (po #61 a przed #62) — drift schema check / coverage check zanim wjedziemy w testy + closure.
- **Pattern z M1 retro #8:** branch dla `docs/close-m3-tracker` zakładać **OD świeżego master po merge'u #62**, nie OD test brancha. `git checkout master && git pull && git checkout -b docs/close-m3-tracker`.
- **Pattern z M2 #28:** `gh issue create --body-file <plik>` zamiast `--body` na Windows (heredoc/quoting zjada markdown).
- **Pattern z M0 #3:** wersje narzędzi (Poetry 2.x, Python 3.13) nie wymyślać z głowy — sprawdzać CI przed pinowaniem w CLAUDE.md / pre-commit. M3 dotyka deps (`poetry add`) i Docker (`postgres:16-alpine`, `redis:7-alpine`); jeśli wersja w spec'u nie pasuje do realnej rzeczywistości — fix w spec'u, nie w branchu funkcjonalnym.

### Definition of Done M3 (ze spec'a §8) — ✅ wszystkie domknięte
- [x] **5 PR merged, 5 Issues zamknięte** — D13 (#64), D14 (#68 + #66 + #67 + #70), D15 (#71), D16 (#73), D17 (#75). Issues #58-#62 wszystkie CLOSED.
- [x] **`celery -A config worker -P solo` startuje, podpięty do Redis** — verified D14 smoke (`ping → pong` przez eager + worker autodiscover task).
- [x] **`celery -A config beat` startuje, podpięty do `DatabaseScheduler`** — verified D15 smoke (`Scheduler: django_celery_beat.schedulers.DatabaseScheduler` w log starcie beat'a).
- [x] **`PeriodicTask("scrape_watched_characters")` enableable w admin, scrape'uje `Character.objects.all()` co interval** — D16 PeriodicTask data migration (`apps/characters/migrations/0003_seed_default_periodic_task.py`) + manual smoke z 1-min interval.
- [x] **Subprocess `scrape_character` aktualizuje `Character.last_scraped_at`** — D16 task wywołuje `[sys.executable, "manage.py", "scrape_character", name]` przez `subprocess.run()`, M1-D8 cmd zapisuje przez service.
- [x] **Freshness threshold działa (skip jeśli < 30min od ostatniego scrape)** — `CELERY_SCRAPE_FRESHNESS_MINUTES=30` (env-overridable) + D17 unit test `test_scrape_watched_characters_respects_freshness_threshold` weryfikuje pozytywnie (`mock.assert_not_called()`).
- [x] **Wszystkie pre-commit + CI zielone na master** — ostatni master commit `ae25cfc` przeszedł lint (1m30s) + pytest (1m18s) bez błędów.
- [x] **`coverage threshold = 70` zachowane** — lokalnie 88% (margines 18pp). `apps/characters/tasks.py` 100%.
- [x] **PROGRESS.md: "🎉 M3 COMPLETED" + retro per Issue** — ten dokument.
- [x] **Milestone M3 zamknięty na GitHub** — TODO ostatni krok po merge tego PR-a (`gh api -X PATCH repos/:owner/:repo/milestones/11 -f state=closed`).

### Podsumowanie M3 (2026-04-28 → 2026-04-29, 2 dni vs 5 z budżetu — 3 dni wyprzedzenia)
- **5 Issues (#58-#62) + 4 satellite PR-y** (#66 mypy additional_deps, #67 mypy module overrides, #70 ping testy follow-up, #75 D17 testy follow-up). Total 9 PR. Strict chain D13→D14→D15→D16→D17 zachowany.
- **DoD M3 spełnione** — Celery + Beat + DatabaseScheduler + scheduled `scrape_watched_characters` co interval, freshness threshold działający, smoke manualny przeszedł, coverage 88%.
- **Najwartościowsze lekcje M3:**
  1. **Mypy cross-env gymnastics** — pre-commit isolated env vs poetry venv generuje **różne error codes** dla tego samego problemu (`[misc]` vs `[untyped-decorator]` na `@shared_task`). Trzy razy w M3 (D14, D16, parametrize w D17). Wzorzec sugeruje `celery-types` package jako trwałe rozwiązanie w **post-M3 chore PR** (kandydat na pierwsze zadanie M4 pre-flight).
  2. **Triple-source-of-truth dla deps** (lekcja procesu z #59) — każda nowa lib importowana w `apps/` wymaga **trzech** miejsc: `pyproject.toml` (poetry), `.pre-commit-config.yaml` mypy `additional_dependencies` (isolated env), `.github/workflows/ci.yml` env (jeśli `env(...)` w `settings/base.py` bez default). Brak choćby jednego = ImproperlyConfigured w CI lub ModuleNotFoundError w pre-commit. Checklist do trzymania razem.
  3. **Postgres image gotchas** (D13) — `POSTGRES_NAME` ≠ `POSTGRES_DB`, `initdb` only-on-first-start, `DATABASE_URL` musi być **w pełni** zsynchronizowany z `POSTGRES_*`. W M9 (Dockeryzacja prod) to się odbije bo `down -v` nie będzie opcją na żywych danych — wzorzec do weryfikacji wcześniej.
  4. **Code review przed merge — 2 critical bugs w #61** (settings name typo `SCRAPING_THRESHHOLD_MINUTES` z literówką, counter swap `scraped` zamiast `skipped`). Oba klasyczne pułapki, oba przeszły by smoke gdyby nie review (pierwszy: `getattr` cicho fallbackuje do default, drugi: `assert_called_once` na success path nie wyłapuje błędnego countera w skip path). **D17 testy mają explicit oba counters** (`assert result == {"scraped": X, "failed": Y, "skipped": Z}`), nie pojedyncze fragmentaryczne asercje.
  5. **`auto_now=True` testowanie** — workaround `Character.objects.filter(pk=...).update(last_scraped_at=tz_dt)` (skip save → skip auto_now). Wzorzec stosowany dwa razy w M3 (#61 implementation + #62 testy), wart zapamiętania na M4 (BedmageWatch ma `created_at` auto_now_add — podobna sytuacja).

### Tech debt z M3 (do adresowania post-M3)
- **`celery-types` package** — trwałe rozwiązanie cross-env mypy gymnastics (3 razy w M3). Kandydat na pierwsze chore PR M4 lub osobny standalone PR.
- **DSN konstrukcja z komponentów w settings** — Opcja B z dyskusji D13. Obecnie `DATABASE_URL` musi być w pełni zsynchronizowany z `POSTGRES_USER`/`PASSWORD`/`DB`/portem ręcznie. DRY refactor odłożony na M4+ (priorytet niski, wzorzec działa stabilnie).
- **Branch protection master** — wciąż brak required status check `test / Pytest` (z post-M2 tech debt, niezamknięte w M3). Dodać w GitHub Settings → Branches.
- **`admin.ModelAdmin` bez generic parameter** (z post-M2 tech debt) — `# type: ignore[type-arg]` zamiast `ModelAdmin[Character]`. Alternatywa: `django-stubs-ext.monkeypatch()` w `base.py`. Decyzja na później.
- **Spider defensive parsing** (z post-M2 tech debt, niezamknięte w M3):
  - `int(level_raw)` w `character_spider.py:49` — pada na np. `"118 (deleted)"`; potrzebny regex `^\d+` lub try/except.
  - `_parse_last_login` hardcoduje `Europe/Berlin` zamiast walidować odczytaną TZ — cichy bug gdyby serwer kiedyś zmienił TZ. Minimum: assert `_tz in {"CEST","CET"}` albo warning.
  - Fragile fixture path `Path(__file__).resolve().parents[3]` w `test_character_spider.py:15` — refactor do `conftest.py` w `tests/`.

### Mini-retro w trakcie M3 — czy zadziałał (lekcja z M2)
**Tak.** Pre-flight grep `add_arguments` w `scrape_character.py` przed implementacją D16 wykrył spec/M1 deviation (named vs positional arg) zanim trafił do branchu — oszczędność rundy fix'ów. Pattern do zachowania w M4: pre-flight code reading **przed** napisaniem nowego subprocess/integration call'a.

---

## 🎉 Milestone M4 — Deaths monitor (backend) COMPLETED (2026-05-07)
Wszystkie 5 zadań ukończone, milestone zamknięty. Design spec: [`docs/superpowers/specs/2026-04-30-m4-deaths-monitor-design.md`](docs/superpowers/specs/2026-04-30-m4-deaths-monitor-design.md). Implementation plan: [`docs/superpowers/plans/2026-04-30-m4-implementation-plan.md`](docs/superpowers/plans/2026-04-30-m4-implementation-plan.md). Drugi spider w projekcie (deaths z `tibiantis.info`), pełna persistence + GraphQL surface, Beat schedule co 5 min ready do enable'u przez admina.

### Notatki z retro M4 (dopisywane progresywnie)
- **#78 D18 (merge 2026-05-05):** Model minimalistyczny zgodnie ze spec'iem — 5 pól domain (`character_name`, `level_at_death`, `killed_by`, `died_at`, `scraped_at`) + `Meta.constraints` z `UniqueConstraint(("character_name", "died_at"), name="unique_death_event_per_character_time")`. Zamierzone deviations vs CLAUDE.md §5 szkic: (a) `killed_by` jako `TextField` zamiast `CharField` (długie opisy "killed at level X by Y, Z, and W" mogą przekroczyć 255), (b) brak `announced_on_discord` (M5+ Discord territory, niepotrzebne w M4 backend-only), (c) `Meta.ordering = ["-died_at"]` jako default — `recentDeaths` query w D22 dostaje to bez explicit `order_by`. Migration `0001_initial` deterministyczna, bez sequence "delete + recreate" jak w `apps/accounts` z M2-D9.
- **Tech debt z #78 (do adresowania post-M4):**
  - ~~`db_index=True` na `character_name` + `unique_together("character_name", "died_at")` — czy redundant?~~ — **świadoma decyzja, nie redundant**: composite unique constraint tworzy btree na `(character_name, died_at)` z prefiksem character, ale single-column lookup (`recentDeaths(character_name=...)` w D22) korzysta lepiej z osobnego btree na samym `character_name`. Composite index jest used only when query filters by leading column — działa również dla `WHERE character_name = X`, ale z gorszą selektywnością. Pozostawić oba, udokumentować docstringiem przy modelu, żeby nie kusiło "uprzątnąć".
  - `admin.ModelAdmin[DeathEvent]` generic subscript — w review pierwszej iteracji rekomendowane jako "modern, resolves M2 tech debt" — okazało się **runtime trap**: `TypeError: type 'ModelAdmin' is not subscriptable` przy import time. Root cause: `django-stubs` jest type-only, runtime subscriptability wymaga `django_stubs_ext.monkeypatch()` w `config/settings/base.py` (niezarejestrowane w projekcie). Trzymać `# type: ignore[type-arg]` (mirror `apps/characters/admin.py`) dopóki post-M4 chore PR nie wdroży `monkeypatch()`. Auto-memory updated z lekcją.
- **#79 D19 (merge 2026-05-06):** Spider `deaths_spider.py` + `DeathItem` w `items.py` + `pipeline` rozszerzony o `elif isinstance(item, DeathItem)` z lazy-imported stub `save_death_event` (`raise NotImplementedError("D20")`). Fixture `deaths_sample.html` (50 deathów ze strony, snapshot live'u). Spider parsuje tabelę DOM przez selectory, regex na `level_at_death` (`int(re.search(r"\d+", raw))` — zgodnie z M3 retro tech debt #7), TZ `Europe/Berlin` z explicite `ZoneInfo` (naive datetime byłoby bug). Stub w pipeline świadomy — D20 zastąpi pełną impl, daje bezpieczny dispatch hook bez crashujących prod'a gdyby ktoś odpalił spider'a przed D20.
- **#80 D20 (merge 2026-05-07):** `save_death_event(payload) -> DeathEvent | None` + `apps/deaths/management/commands/scrape_deaths.py` + pipeline counter `custom/death_duplicates`. Smoke confirmed: run #1 `{"yielded": 50, "duplicates": 48}`, run #2 `{"yielded": 50, "duplicates": 50}` — idempotent, dedup constraint działa, counter inkrementuje. 19/19 unit testów (5 service + 5 mgmt cmd + 9 pipeline), coverage 100% na `apps/deaths/services.py` i `scrape_deaths.py`. Najbardziej wartościowe momenty:
  - **3 blockery wyłapane w pre-push code review** — wszystkie w pierwszej iteracji impl, żaden nie wszedł na branch:
    1. **Duplikat `elif isinstance(item, DeathItem)`** w `pipelines.py` — pierwszy elif wywoływał `save_death_event` ignorując return, drugi (martwy kod, nieosiągalny) miał counter logic. Counter `custom/death_duplicates` nie inkrementowałby się **nigdy** w prod, D21 task pokazywałby `duplicates: 0` przez całą wieczność. Klasyczny refactor trap — gdy rozszerzasz istniejącą gałąź, doklejenie nowej obok zamiast zastąpienia. Lekcja: `git diff <file>` przed `git add`, kluczowe linie powinny być **zastąpione**, nie zduplikowane.
    2. **Nazwa pliku `scrape_death.py` (singular) zamiast `scrape_deaths.py`** — psuło `manage.py scrape_deaths` invocation z D21 subprocess call'a. Spec acceptance criteria mówiło explicite `scrape_deaths.py`, CLAUDE.md §6 + cheatsheet §14 też. Lekcja: AC checklist linia po linii przed pushem (powtórka z M0 retro #1).
    3. **`DeathPayload` inline w `services.py`** zamiast `apps/deaths/types.py` — soft blocker, konsystencja architektury rozbita (`apps/characters/types.py` ma `CharacterPayload`). Świadomie pominięty w D20 — niski coupling, łatwy do refactoru w osobnym chore PR. **Carry-over do M4 tech debt.**
  - **Pre-commit mypy cache trap po `stubs.py` edit** — `apps.deaths` dodany do `config/settings/stubs.py` INSTALLED_APPS żeby django-stubs widział `DeathEvent.objects` deskryptor; `poetry run mypy` natychmiast passed, ale `poetry run pre-commit run mypy --all-files` nadal failował z tym samym `"type[DeathEvent]" has no attribute "objects"`. Root cause: pre-commit isolated env w `~/.cache/pre-commit/` cachuje rezultat django-stubs plugin discovery (czytany **raz** przy init env'u). `pre-commit clean` rozwiązał — env zbudowany od zera, plugin re-czytał świeży stubs.py. **Wzorzec do M5+ (każda nowa appka domenowa):** trzy zmiany w jednym ruchu — `base.py` LOCAL_APPS + `stubs.py` INSTALLED_APPS + `pre-commit clean`. Auto-memory updated.
  - **`crawler.stats: StatsCollector | None`** w stubach Scrapy 2.x — runtime po `process.start()` zawsze set, ale stuby są bardziej defensywne. Wybrano **defensive early-return** (zamiast `assert` które `python -O` strip'uje z bytecode'u — ważne dla CLI tools, prod może kiedyś być uruchomione z optimization mode). Dodany `test_command_handles_none_stats_collector_gracefully` żeby pokryć nową gałąź, coverage utrzymany na 100%. Kontynuacja patternu "defensywne narrowing zamiast bandage" z M3.
  - **Pułapka B z Issue #80 confirmed pragmatically** — `transaction.atomic()` przed `DeathEvent.objects.create()`. Bez tego `IntegrityError` w testach z `@pytest.mark.django_db` (każdy test jest sam w transakcji) abortowałby całą transakcję → pierwszy duplikat w `test_duplicate_returns_none_and_does_not_insert` zabiłby resztę testów `TransactionManagementError`. Mirror M1-D8 race retry w `upsert_character` — atomic = savepoint, IntegrityError aborts tylko savepoint, outer transaction zostaje czysta. Tworzy się stabilna konwencja "ORM operacja która może rzucić IntegrityError = wrap w `transaction.atomic()`".
  - **Smoke baseline awareness** — pierwszy live run pokazał `duplicates: 48` (a nie 0) bo baza miała już rzędy z wcześniejszych D19 manual smoke'ów. Issue #80 implicite zakładał czystą bazę. **Idempotency testem jest drugi run** (50/50, count stabilny), nie pierwszy. Subtelne — przy debugowaniu prod'a "duplicates >0" w pierwszym scrape'ie po deploy nie znaczy bug, tylko że poprzednie scrape'y zostawiły dane.
- **#81 D21 (merge 2026-05-07):** `apps.deaths.tasks.scrape_deaths` Celery task + PeriodicTask seed migration `0002_seed_periodic_task.py` (`every=5, period="minutes", enabled=False`) + `DEATH_LEVEL_THRESHOLD = env.int("DEATH_LEVEL_THRESHOLD", default=30)` w `base.py` + `.env.example`. Smoke confirmed: `scrape_deaths.delay().get()` przez worker (`-P solo`) zwrócił `{'yielded': 50, 'duplicates': 49, 'returncode': 0}` — 49/50 dup z poprzedniego D20 smoke'a, 1 świeży death między scrape'ami. 4 unit testy przechodzą, coverage 100% `apps/deaths/tasks.py`. Mirror M3-D16 `scrape_watched_characters` z 4 świadomymi rozszerzeniami (capture_output, JSON parse, retry-on-timeout, sentinel) bo D21 musi parsować stdout. Najbardziej wartościowe momenty:
  - **Duplikat task'a w złym pliku** — pierwsza iteracja skopiowała szkic D21 do `apps/characters/tasks.py` zamiast `apps/deaths/tasks.py`, potem osobno utworzony `apps/deaths/tasks.py`. Stara kopia w characters została z `timeout=60` (nie naprawiona po review na 120). Złapane przez **pre-commit mypy + coverage drop do 68%**. Cleanup przywrócił `apps/characters/tasks.py` do M3-D17 stanu (zero diff vs master), bonus: PR scope czysty — bez "noise" zmian w cudzym pliku. Lekcja: po skopiowaniu szkicu zawsze sanity check `git status` ścieżki, nie polegaj na tym że PyCharm wyświetla "właściwy" plik.
  - **`json.loads()` Any → `dict[str, int]` no-any-return** w `apps/deaths/tasks.py:43` — `json.loads` zwraca `Any`, `summary["returncode"] = ...` mutuje `Any`, `return summary` w funkcji deklarującej `-> dict[str, int]` jest `Any → dict[str, int]` cast i mypy strict to flag'uje. Fix: **explicit type annotation** `summary: dict[str, int] = json.loads(...)` (1 linia, zero importów, idiomatic). Alternatywy `cast()` / `# type: ignore` gorsze (extra import / band-aid). Subtelność: pre-commit mypy raportowało **tylko** characters/tasks.py:109 błąd (gdzie był duplikat), `mypy direct` na deaths/tasks.py:50 też pokazało ten sam błąd. Hipoteza: pre-commit isolated env strict-stops na pierwszym błędzie per-module group lub cache nie odśwież po cleanup'ie — nie potwierdzone. Praktyczna konsekwencja: nie polegaj na pre-commit jako **pełnej** liście błędów; zawsze finalizuj `mypy direct` lokalnie po cleanup'ie.
  - **`python -c "..."` ładuje czysty Python — defaultuje broker na `amqp://`** zamiast czytać `CELERY_BROKER_URL=redis://...` z Django settings. Worker (`celery -A config worker`) ładuje settings przez `-A config` więc widzi Redis prawidłowo, ale klient firing task'a (`python -c "scrape_deaths.delay()..."`) bez `manage.py shell -c` defaultuje do AMQP, próbuje port 5672, dostaje `ConnectionRefusedError [WinError 10061]`. **Drugie uderzenie tego samego wzorca w jednej sesji** — wcześniej `python -c "print(settings.DEATH_LEVEL_THRESHOLD)"` też wybuchło. Utrwala się reguła: **dla wszystkiego co czyta Django settings ALBO odpala Celery z istniejącej konfiguracji projektu — `manage.py shell -c`, nie `python -c`.** Wymowne że pierwsze uderzenie nie zaszczepiło lekcji wystarczająco mocno.
  - **Celery prefork pool na Windows = WinError 5/6 z `billiard`** — worker prawidłowo connected do Redis, taski discoverowane (`apps.deaths.tasks.scrape_deaths` w `[tasks]`), `celery@bartek ready.` wypisane. Sekundę później wszystkie 32 prefork workers crashują z `OSError [WinError 6] Nieprawidłowe dojście` + `PermissionError [WinError 5] Odmowa dostępu`. Root cause: `prefork` używa `fork()` semantics, Windows nie ma `fork()`, billiard emuluje przez `spawn` ale inter-process sync primitives (`_semlock` w `billiard/synchronize.py`) korumpują się przy handle inheritance. Fix: **`-P solo`** (1 task naraz, zero billiard multiprocessing). To dev-on-Windows only — Docker prod (Linux container) prefork działa idealnie. Saved jako auto-memory `feedback_celery_windows_pool.md` — będzie wracać przy każdym Celery task w M5+ (bedmages, notifications).
  - **Pułapka D z #81 zaadresowana defensywnie od pierwszej iteracji** — `subprocess.run(text=True, encoding="utf-8", errors="replace")`. Bez `encoding="utf-8"` Windows `text=True` używa `locale.getpreferredencoding()` = `cp1250` w Polsce — opisy `killed_by` z polskimi/exotycznymi znakami wybuchłyby `UnicodeDecodeError` przed dotarciem do `json.loads`. Test unit tego nie złapie (mock subprocess.run zwraca controllable string). Dzięki pre-flight code reading flagującemu pułapkę, problem nigdy nie trafił na branch. **Pre-flight działa** — kontynuacja patternu z M3 retro mini-retro.
- **#82 D22 (merge 2026-05-07):** `recentDeaths(minLevel: Int, limit: Int = 50): [DeathEventType!]!` async resolver w `apps/deaths/schema.py` + integration e2e test + `DeathsQuery` w `merge_types`. JWT auth required (`raise PermissionError` — list-based query, raise distinguishes "auth fail" od "empty result"). Limit clamping `min(max(limit, 1), 200)` — runaway protection + edge case `limit: 0`. 6 unit testów GraphQL (default threshold, explicit override, limit cap 200, limit clamp 1, order DESC, auth required) + 1 e2e (full flow eager Celery + mocked subprocess + fixture-based parse, round 1: 50/0, round 2: 50/50, count stable). 94/87 tests, coverage 90.93% global, `apps/deaths/schema.py` 100%, cumulative `apps/deaths/*` ~97% (DoD ≥ 95% ✓). Najbardziej wartościowe momenty:
  - **Brak `@strawberry_django.type(DeathEvent)` decoratora** — pierwsza iteracja miała `class DeathEventType: id: auto, ...` bez decoratora, dawała `TypeError: Query fields cannot be resolved. Unexpected type` przy schema build. `auto` to sentinel wymagający Django model context — sam nie wystarczy. Fix: 1 linia decoratora. Lekcja: porównuj z precedensem (`apps/characters/schema.py:8`) **przed** push'em, nie tylko własną pamięcią.
  - **Niezgodność Issue spec vs precedens — `info.context["request"]` vs `info.context.request`** — Issue body D22 mówi dict-access, ale M2-D11 `apps/accounts/schema.py:22` używa attribute-access (i działa, M2-D11 testy zielone). Mirror precedensu wygrał. Lekcja: spec'owy issue body to **template** stanu repo z momentu pisania spec'a — przy każdym task'u sanity-check vs aktualny kod, bo realne API mogło ewoluować od czasu spec'a.
  - **Auth pattern dla listy ≠ pattern dla single object** — `me: UserType | None` używa `return None` (klient widzi `null`). `recentDeaths: [DeathEventType!]!` musi `raise PermissionError`, bo pusta lista jest semantycznie inna niż "no auth" (pusta lista byłaby valid response gdy filter eliminuje wszystkie deaths). Świadoma różnica dyktowana **return type signaturem**, nie stylem. Wzorzec do zapamiętania na M5+ GraphQL queries: nullable single → `return None`, non-null list → `raise`.
  - **MINGW64 + Python `-c` + zagnieżdżone cudzysłowy = SyntaxError** — `python -c "import requests; r = requests.post(..., headers={'X': 'Y'})"` wybuchło na nested quoting podczas smoke testu. `curl -d '...'` lub `manage.py shell -c "..."` lub po prostu **GraphiQL playground na `/graphql/` w przeglądarce** to czyste alternatywy. Lekcja: dla skomplikowanych dictów na Windows shell — albo zapisz Python do pliku, albo curl, albo playground UI. **Drugie uderzenie tego samego wzorca** w jednej sesji — wcześniej `python -c "scrape_deaths.delay()"` defaultowało broker do AMQP. Wzorzec utrwala się: MINGW64 Bash + zagnieżdżone cudzysłowy = pole minowe.
  - **Mypy `'Settings' object has no attribute 'DEATH_LEVEL_THRESHOLD'`** — django-stubs nie zna custom settings field'ów. Dwie opcje: (A) `# type: ignore[misc]` band-aid, (B) dodać `DEATH_LEVEL_THRESHOLD = 30` do `config/settings/stubs.py` + `pre-commit clean`. Wybrana opcja B — czystsze, mirror PR #86 wzorca dla `INSTALLED_APPS`, schema.py zostaje bez `type: ignore`. **Trzecie uderzenie** carry-over tech debt "single source of truth dla stubs.py" — base.py LOCAL_APPS, stubs.py INSTALLED_APPS, custom settings, wszystkie wymagają sync ręcznego. Argument na opcję A (import from base + os.environ.setdefault) z review #86 dostaje kolejny dowód.

### Definition of Done M4 (ze spec'a §8) — ✅ wszystkie domknięte
- [x] **5 PR merged, 5 Issues zamknięte** (D18-D22 + 3 closure PR-y D18-D20/D21/D22).
- [x] **`apps.deaths` zarejestrowane w `INSTALLED_APPS`**, migracje aplikują się czysto na świeżej bazie (`manage.py migrate` smoke confirmed w D18 + D21 PeriodicTask seed).
- [x] **`DeathEvent` widoczne w Django admin** z `list_display=("character_name","level_at_death","died_at","scraped_at")` i `search_fields=("character_name","killed_by")` (D18).
- [x] **`scrapy crawl deaths`** parsuje fixture lokalną — D19 `test_deaths_spider.py` na `deaths_sample.html` (50 items yielded).
- [x] **`python manage.py scrape_deaths`** wypluwa valid JSON `{"yielded": N, "duplicates": M}` na stdout, returncode 0 — D20 smoke confirmed (run #1: 50/48, run #2: 50/50).
- [x] **`scrape_deaths.delay().get()`** zwraca dict z polami `yielded`, `duplicates`, `returncode` — D21 smoke confirmed `{'yielded': 50, 'duplicates': 49, 'returncode': 0}` przez Celery worker `-P solo`.
- [x] **PeriodicTask `scrape_deaths` enable'uje się w admin**, Beat fires task co 5 min — D21 seed migration `enabled=False` domyślnie, manualnie enable'uje admin po smoke.
- [x] **GraphQL `recentDeaths(minLevel: 1, limit: 10)`** zwraca 10 najnowszych deaths — D22 smoke confirmed przez GraphiQL playground `/graphql/` z JWT Bearer.
- [x] **GraphQL `recentDeaths` bez JWT** → error response — D22 `test_recent_deaths_requires_authentication` + manual smoke confirmed `errors: [{"message": "Authentication required"}]`.
- [x] **Wszystkie pre-commit + CI zielone na master** dla każdego PR-a.
- [x] **`coverage threshold = 70` zachowane** (lokalnie 90.93% global, 100% dla `apps/deaths/services.py`, `tasks.py`, `schema.py`, `scrape_deaths.py` mgmt cmd).
- [x] **PROGRESS.md** rozszerzony o sekcję M4 z retro per Issue (ten dokument).
- [x] **Milestone M4 zamknięty na GitHub** — TODO ostatni krok po merge tego PR-a (`gh api -X PATCH repos/:owner/:repo/milestones/4 -f state=closed`).

### Podsumowanie M4 (2026-05-05 → 2026-05-07, 3 dni vs 5 z budżetu — 2 dni wyprzedzenia)
- **5 Issues (#78-#82) + 3 closure PR-y** (#87 close D18-D20, #89 close D21, #90 = D22 + closure tracker = ten PR). Total 8 PR. Strict chain D18→D19→D20→D21→D22 zachowany.
- **DoD M4 spełnione** — drugi spider dochodzi do projektu, pełna persistence + GraphQL + Beat schedule, JWT-protected query.
- **Najwartościowsze lekcje M4:**
  1. **Pre-flight code reading przed nową gałęzią pipeline'u/subprocess'u/integration call'a** — wzorzec z M3 mini-retro **kontynuacja**. W M4 zaaplikowane przed D19 (pipeline dispatch precedens), D20 (mgmt cmd CrawlerProcess), D21 (subprocess pattern z M3-D16), D22 (Strawberry GraphQL precedens M2-D11/D12). Każdy raz wykrył minimum 1 deviation spec vs reality. Pattern się utrwala — kontynuować w M5+.
  2. **Carry-over tech debt "single source of truth dla stubs.py"** — w D20 dodawanie `apps.deaths` do INSTALLED_APPS, w D22 dodawanie `DEATH_LEVEL_THRESHOLD` jako custom setting. Każdy nowy element domeny + każdy nowy custom setting wymaga `stubs.py` edit + `pre-commit clean`. M5+ bedmages doda `BedmageWatch` + `BEDMAGE_REGEN_MINUTES` setting → znowu 2 ręczne sync'i. **Argument na opcję A z review #86** (import LOCAL_APPS + os.environ.setdefault dla env vars) zyskuje 3 dowody, kandydat na **pierwszy chore PR M5 pre-flight**.
  3. **MINGW64 + zagnieżdżone cudzysłowy = pole minowe** — dwa razy w M4-D22 sesji. `python -c` z dict literals jest pułapką. **Konwencja na przyszłość:** dla CLI smoke testów Django app — zawsze `manage.py shell -c` (single-quote outer + double-quote inner = MINGW64 OK). Dla GraphQL smoke — GraphiQL playground na `/graphql/` w przeglądarce. Dla quick HTTP probe — `curl -d '...'` z singel-quote outer.
  4. **Auth pattern dyktowany return-type signature, nie stylem** — `Optional` single → `return None`, non-null list → `raise`. W M5 GraphQL mutations (np. `addBedmageWatch(...)`) ten wzorzec się przyda.
  5. **`-P solo` na Windows dla Celery worker** — zapisany do auto-memory. Wraca przy każdym Celery task w M5+.
  6. **3 blockery wyłapane w pre-push code review w D20** — duplikat `elif`, nazwa pliku, soft blocker DeathPayload extract. Pattern: czytanie `git diff <file>` przed `git add` jest kluczowe, **szczególnie** gdy refactor'uje istniejący plik (rozszerzenie o nową gałąź zamiast skopiowania bocznie).

### Tech debt z M4 (do adresowania post-M4)
- **Single source of truth dla `INSTALLED_APPS`** między `config/settings/base.py` LOCAL_APPS a `config/settings/stubs.py` — obecnie każda nowa appka domenowa wymaga update w obu miejscach + `pre-commit clean`. Trzy opcje z PR #86 review thread:
  - **A (preferowana):** import `LOCAL_APPS` z `base.py` w `stubs.py` (po `os.environ.setdefault` dla wymaganych env vars). Wymaga ostrożnej kolejności importów i defaultów dla `DJANGO_SECRET_KEY`/`DATABASE_URL`/`CELERY_BROKER_URL`/`CELERY_RESULT_BACKEND`.
  - **B:** asercja w issue body checklisty (nowa appka = update stubs.py).
  - **C:** custom pre-commit hook porównujący listy.
- **`DeathPayload` inline w `services.py`** zamiast osobnego `apps/deaths/types.py` (soft blocker z #80 review). Mirror M1 #6 fix — type osobno, logika osobno. Refactor trywialny, kandydat na chore PR przed D22.
- **`celery-types` package** (carry-over z M3 tech debt) — w D21 **nie uderzyło** (`self: Task` z `from celery import Task` przeszło mypy strict bez gymnastycznych workaround'ów). Carry-over do M5+ chore PR — przy następnym `@shared_task` w bedmages/notifications może uderzyć z innym error code. Niska priority, ale "nie uderzyło raz" nie znaczy "nigdy".
- **Branch protection master required `test / Pytest` check** (carry-over z M2/M3 tech debt) — wciąż brak. Dodać w GitHub Settings → Branches.
- **`admin.ModelAdmin` generic subscript** (carry-over z M2 tech debt + zaobserwowane podczas M4-D18 review) — wymaga `django_stubs_ext.monkeypatch()` w `base.py` żeby było runtime-safe. Sweep wszystkich `# type: ignore[type-arg]` w jednym chore PR po wdrożeniu monkeypatch'a.
- **Dev runbook dla Celery na Windows** — fragmenty rozproszone w retro M3-D17 (e2e tests setup) i M4-D21 (worker `-P solo` + `manage.py shell -c` dla firing). Skonsolidować do `docs/dev-runbook.md` żeby M5+ workflow nie wymagał re-discovery: instrukcje na 1) Redis up via docker-compose, 2) worker `-P solo` na Windows, 3) firing przez `manage.py shell -c`, 4) Beat enable/disable cycle dla periodic tasks. Niska priority, do M5 lub M6.

## 🎉 Milestone M5 — Bedmage tracker (backend) COMPLETED (2026-05-08)
Wszystkie 5 zadań ukończone, milestone zamknięty. Design spec: [`docs/superpowers/specs/2026-05-08-m5-bedmage-tracker-design.md`](docs/superpowers/specs/2026-05-08-m5-bedmage-tracker-design.md). Implementation plan: [`docs/superpowers/plans/2026-05-08-m5-implementation-plan.md`](docs/superpowers/plans/2026-05-08-m5-implementation-plan.md). Pełny chain `addBedmageWatch` (GraphQL mutation) → service → tracker integration w `scrape_watched_characters` → notification handler abstraction + LoggingHandler default. Pierwszy `Mutation` type w projekcie, pierwsze użycie nowego `apps/notifications/` Protocol-based handler swap dla M6 DiscordHandler.

### Ukończone (M5)
- ✅ #94 [M5-D23] BedmageWatch model + admin + initial migration (2026-05-08) — PR [#99](https://github.com/bgozlinski/tibiantis-scraper/pull/99) — squash `278f4ac`
- ✅ #95 [M5-D24] services (add/remove/check) + BedmagePayload types + BEDMAGE_REGEN_MINUTES (2026-05-08) — PR [#100](https://github.com/bgozlinski/tibiantis-scraper/pull/100) — squash `6c09850`
- ✅ #96 [M5-D25] apps/notifications/ + Protocol handler + LoggingHandler + settings switch (2026-05-08) — PR [#101](https://github.com/bgozlinski/tibiantis-scraper/pull/101) — squash `1c4ca00`
- ✅ #97 [M5-D26] tracker integration w scrape_watched_characters task (2026-05-08) — PR [#102](https://github.com/bgozlinski/tibiantis-scraper/pull/102) — squash `0314c17`
- ✅ #98 [M5-D27] GraphQL myBedmages query + add/removeBedmageWatch mutations + e2e (2026-05-08) — PR [#103](https://github.com/bgozlinski/tibiantis-scraper/pull/103) — squash `cbd21e7`

### Notatki z retro M5 (dopisywane progresywnie)
- **#94 D23 (merge 2026-05-08):** Pierwszy app po M2 stubs.py reform — chore PR #92 (single source of truth) **działa w praktyce**, mypy widzi automatycznie nowe `LOCAL_APPS` przez `from config.settings.base import LOCAL_APPS` w `stubs.py`. Wszystkie 5 pułapek z briefu uniknięte: `settings.AUTH_USER_MODEL` jako string FK (nie direct import — circular safety), `"characters.Character"` jako lazy string (różne od M4-D18 direct import — D26 integration foresight), `# type: ignore[type-arg]` na admin (carry-over M2 tech debt), `UniqueConstraint(name=...)` w `Meta.constraints` (Django 4+ idiom, NIE deprecated `unique_together`), brak ghost migrations w innych apps (potwierdzone przez `git status`). Admin **prostszy** niż M4-D18 — bez `readonly_fields`/`has_change_permission` bo `BedmageWatch` jest user-editable (manual `active` toggle), różny use case niż immutable `DeathEvent`. Pre-commit auto-fix: line endings (Windows CRLF artifact).
- **#95 D24 (merge 2026-05-08):** 3 funkcje service (add/remove/check) + `BedmagePayload` TypedDict + `BEDMAGE_REGEN_MINUTES` setting. Najbardziej wartościowe momenty:
  - **Critical bug `from datetime import timezone, timedelta`** (zła `timezone`) — `datetime.timezone` to klasa tzinfo, nie ma `.now()`. Test #9-#11 (4 testy używające `check_bedmage_watches_for_character`) padły z **identyczną** trace `AttributeError: type object 'datetime.timezone' has no attribute 'now'` na services.py:75. **Testy złapały bug zanim trafił na master** — confirmation że Pułapka E z briefu (timezone.now() z django.utils, nie datetime) jest realna. Bez testu wybuchnęłoby w prod przy pierwszym `check_*` call.
  - **3 iteracje mypy** (`AbstractUser` → `User | int | None` lookup error → direct `from apps.accounts.models import User` import → `User = get_user_model()` redundancy F811). Lekcja zapisana w memory `feedback_services_user_type_hint.md`: **services używają direct `User` import**, modele zostają przy `settings.AUTH_USER_MODEL` (lazy string per CLAUDE.md §5). django-stubs jest precyzyjny: FK lookup `user=user` wymaga konkretnego `User` typu, nie supertype'ów. M5+ services pomijają tę iterację.
  - 11 unit testów (originally planowane 8-9, dorzuciliśmy edge cases dla full invariant coverage), 100% coverage `apps/bedmages/services.py` (41/41 stmts).
- **Tech debt z #95 (do adresowania post-M5):**
  - **Memory entry `feedback_services_user_type_hint.md` — pierwsza memory zapisana w trakcie M5.** Wzorzec: po 2+ iteracjach jednego typu mypy/lint error w jednej sesji = save to memory. Skraca cycle dla M6+.
- **#96 D25 (merge 2026-05-08):** `apps/notifications/` Protocol-based handler abstraction + `LoggingHandler` default + `BEDMAGE_NOTIFICATION_HANDLER` settings switch. D24 stub `logger.info("BEDMAGE [STUB]:")` zastąpiony real `handler.notify(watch)` dispatch z defensive `try/except` (resolves D24 Pułapka B, decyzja przeniesiona). Najbardziej wartościowe momenty:
  - **Per-call resolution NIE `lru_cache`** — `@override_settings(BEDMAGE_NOTIFICATION_HANDLER=...)` w testach **musi** działać. Cache by zamroził pierwszy resolved handler i ignored override. Test `test_get_bedmage_handler_resolves_custom_class_via_settings` enforce'uje. Resolution kosztuje microseconds (jeden `import_string`), nie ma sensu cache'ować.
  - **`if TYPE_CHECKING:` import `BedmageWatch`** w handlers.py + forward ref string w Protocol method signature — eliminuje runtime circular `apps.notifications → apps.bedmages.models → ?` przy zachowaniu type-checker happiness. Pułapka C resolved.
  - **Defensive `continue` w `except` wokół `handler.notify`** — `last_notified_login` **NIE set'ed** na fail path. Test `test_check_does_not_set_last_notified_login_when_handler_raises` enforce'uje critical invariant: failed notification = retry on next scrape (idempotency utrzymana, ale watch nie pominie cycle'a).
  - **`cast(BedmageNotificationHandler, handler_class())`** — `import_string` zwraca `Any`, mypy strict wymaga explicit cast aby satisfy `-> BedmageNotificationHandler` annotation. Standardowy idiom dla pluggable backends.
- **#97 D26 (merge 2026-05-08):** Najmniejszy task w M5 (tylko 1 modyfikacja istniejącego `apps/characters/tasks.py` + 3 testy). Tracker fire **tylko** w `if result.returncode == 0` branch — Pułapka E (failed scrape = stale `last_login` = decyzje na stale data byłyby bug). Test `test_scrape_watched_characters_does_not_invoke_tracker_on_failure` jest explicit regression guard dla §4.5 invariant. **Mock target `apps.bedmages.services.*`** (NIE `apps.characters.tasks.*`) bo lazy import — `mock.patch` na source module intercepts lazy import wewnątrz pętli. Najbardziej wartościowe momenty:
  - **Process bug — commit poszedł na `master` lokalnie** (user zapomniał `git checkout -b feat/97-...` po cleanup'cie D25). Recovery: `git branch feat/97-tracker-scrape-integration && git reset --hard origin/master && git checkout feat/97-... && git push -u origin ...`. Stan jest naprawialny (commit nie został push'nięty), ale zużywa 3 destruktywne komendy + zgodę usera na każdy `git reset --hard`. **Lekcja:** po cleanup mergu zawsze `git checkout -b feat/<#>-...` PRZED kodowaniem.
- **#98 D27 (merge 2026-05-08):** Pierwszy `Mutation` type w projekcie. `Query.my_bedmages` + `Mutation.add_bedmage_watch` + `Mutation.remove_bedmage_watch` + `BedmageWatchType` z nested `CharacterType` resolution. Najbardziej wartościowe momenty:
  - **Critical bug wyłapany w review** — brak `mutation=Mutation` parameter w `strawberry.Schema(...)`. SDL dump empirycznie potwierdził `Has Mutation: False` przed fix'em — mutations zdefiniowane, mergowane, ale **nieosiągalne** w GraphQL endpoint. Smoke `addBedmageWatch` w GraphiQL by failował z `"Cannot query field"`. Pułapka E z briefu — pierwszy raz w projekcie, easy miss bo `Query` ma default behavior (Schema(query=...) wystarczy bez explicit), ale `mutation` musi być explicit. Sanity check po fix: `'type Mutation' in schema.as_str()` → True jest baseline dla każdego future Mutation-introducing milestone.
  - **`merge_types("Mutation", (BedmagesMutation,))`** — single-source tuple wymaga trailing comma. `(BedmagesMutation)` to redundant parens, NIE tuple → `TypeError` przy starcie. Pułapka D z briefu uniknięta (user pamiętał z briefu).
  - **`await sync_to_async(service)(...)` w mutations** — services są sync (Django ORM), Strawberry resolvers są async. M2-D11 precedens (`apps/accounts/schema.py:29`) reused. Wzorzec utrwala się jako "boundary conversion w async resolverze".
  - **`.select_related("character")`** w `my_bedmages` queryset — eliminuje `SynchronousOnlyOperation` przy nested `character { ... }` GraphQL query. Async iteracja `[w async for w in qs]` lazy-loaduje FK synchronicznie i crashuje bez prefetch'a. Pułapka C resolved.
  - **`apps/*/types.py` exclude z coverage** — `pyproject.toml` `[tool.coverage.run] omit = ["apps/*/types.py"]`. TypedDict to type hints (definicje pól), nie executable code — coverage tool liczy je jako stmts ale nigdy nie wykonuje, dając misleading 0% reading. Po exclude: cumulative 100% (140/140 stmts), DoD 95% spełnione. Standardowy exclude, nie hack metryki.
  - **Powtórka process bug D26 — commit on master ponownie** (user 2x w jednej sesji). Recovery identyczny do D26. **Konfirm pattern: `.pre-commit-config.yaml` local hook blokujący commits na master** = pierwszy chore PR M6 pre-flight (carry-over tech debt).
  - 11 unit testów (3 query + 4 add mutation + 3 remove mutation) + 1 E2E test (full chain `add_bedmage_watch` → check fires LoggingHandler.notify → idempotency: second call no-op). Cumulative `apps/bedmages/*` + `apps/notifications/*` 100% (28 testów).

### Definition of Done M5 (ze spec'a §8) — ✅ wszystkie domknięte
- [x] **5 PR merged, 5 Issues zamknięte** (D23-D27 + ten closure PR).
- [x] **`apps.bedmages` + `apps.notifications` zarejestrowane w INSTALLED_APPS** (D23 + D25), migracja `bedmages.0001_initial` aplikuje się czysto na świeżej bazie (D23 smoke confirmed).
- [x] **`BedmageWatch` widoczny w Django admin** z `list_display=("user","character","active","last_notified_login","created_at")` i `search_fields=("user__username","character__name")` (D23).
- [x] **Service `add_bedmage_watch` (lazy fetch §4.1)** + **`remove_bedmage_watch` (idempotent §4.2)** + **`check_bedmage_watches_for_character` (delta + idempotency invariant)** działają — D24 11 unit testów, 100% coverage.
- [x] **`check_bedmage_watches_for_character` fires post-success** w `scrape_watched_characters` Celery task — D26 3 unit testy (success path, failure path = §4.5 invariant guard, exception isolation).
- [x] **Notification handler swap przez `BEDMAGE_NOTIFICATION_HANDLER` env var** — D25 Protocol abstraction + LoggingHandler default + per-call resolution (testowalne przez `@override_settings`).
- [x] **GraphQL `myBedmages` + `addBedmageWatch` + `removeBedmageWatch`** z JWT auth — D27 11 unit testy + 1 E2E.
- [x] **GraphQL endpoint zwraca `type Mutation` w SDL** — D27 sanity confirmed `'type Mutation' in schema.as_str()` → True (po Pułapka E fix `mutation=Mutation` parameter).
- [x] **Wszystkie pre-commit + CI zielone na master** dla każdego PR-a.
- [x] **Coverage cumulative `apps/bedmages/*` ≥ 95%** — osiągnięte **100%** (140/140 stmts po `apps/*/types.py` exclude w `pyproject.toml`).
- [x] **PROGRESS.md** rozszerzony o sekcję M5 z retro per Issue (ten dokument).
- [ ] **Milestone M5 zamknięty na GitHub** — TODO ostatni krok po merge tego PR-a (`gh api -X PATCH repos/bgozlinski/tibiantis-scraper/milestones/5 -f state=closed`).

### Podsumowanie M5 (2026-05-08, 1 dzień vs 2-3 dni z budżetu — wyprzedzenie 1-2 dni)
- **5 Issues (#94-#98) + ten closure PR.** Strict chain D23→D24→D25→D26→D27 zachowany. Wszystkie merge'owane tego samego dnia w jednej długiej sesji.
- **DoD M5 spełnione** — `apps/bedmages/` i `apps/notifications/` w pełni funkcjonalne, GraphQL surface kompletny (query + 2 mutations), tracker integration działa end-to-end z idempotency invariant (verified przez E2E test).
- **Najwartościowsze lekcje M5:**
  1. **Brief → kod → review → testy → commit cycle** — utrwalony przez wszystkie 5 D-tasków. Brief od Claude z konkretnymi pułapkami (5-10 per task) → user implementuje produkcyjny kod → review pre-commit → Claude pisze testy → pre-commit + commit + PR. Memory `feedback_services_user_type_hint.md` zapisana podczas D24 zaoszczędziła kolejne iteracje w D27 (mutations używają tej samej konwencji).
  2. **Testy łapią bugi PRZED merge** — D24 import bug (datetime.timezone vs django.utils) złapany przez 4 identyczne `AttributeError` na services.py:75. D27 `mutation=Mutation` missing — wykryty empirycznie przez `'type Mutation' in schema.as_str()` w review (NIE w runtime test bo żaden test nie używał real `/graphql/` mutation routing przed fix'em). Lekcja: SDL dump jako sanity check po każdym Schema(...) modification — dorzuc do briefu D27+ jako standardowy gate.
  3. **Process bug — commit on master 2x** (D26 + D27). Recovery procedure działa, ale to overhead 3 destruktywne komendy + user confirmation per incident. **Carry-over tech debt:** `.pre-commit-config.yaml` local hook blokujący `git commit` gdy `git symbolic-ref --short HEAD == master`. Osobny chore PR (CLAUDE.md §15.12). Następna sesja powinna otworzyć z tym hookiem zanim cokolwiek innego.
  4. **`apps/*/types.py` coverage exclude** — TypedDict definicje to type hints, nie executable code. `pyproject.toml` exclude (1-line config) lepsze niż fake-test type annotations żeby dobić DoD threshold. Standardowa praktyka, kandydat na CLAUDE.md §11 wzmiankę.
  5. **Pułapka E (`mutation=Mutation` w Schema constructor)** — pierwszy Mutation w projekcie wymagał explicit. Easy miss bo `Query` ma default. Wzorzec do M6+: każde dodanie nowego top-level GraphQL operation type (Subscription? Custom?) wymaga sanity check `as_str()` bo Strawberry **nie ostrzega** w runtime kiedy type istnieje ale nie jest podłączony do Schema.
  6. **Memory feedback writing — dwa razy w sesji** (`feedback_services_user_type_hint.md` + reminder o branch creation). Wzorzec utrwala się: surprising/non-obvious correction = save to memory natychmiast po incident. Plus M0-M4 retro lessons fed into briefs (Pułapki D24-D27 wszystkie miały odniesienia do prior M lessons).

### Tech debt z M5 (do adresowania post-M5)
- **`.pre-commit-config.yaml` local hook blokujący commits na master** — proposowany w D27 review jako side note. Proste 5-liniowe `repo: local` z `entry: bash -c '[ "$(git symbolic-ref --short HEAD)" != "master" ]'`. **Wysoka priority** — 2 incidenty w sesji M5 (D26 + D27), każdy = `git reset --hard` recovery + user confirmation. Osobny chore PR (CLAUDE.md §15.12 wymaga osobnego PR dla `.pre-commit-config.yaml` zmian).
- **Untracked `nul` + `.pr-body-close-m4.md`** w głównym repo — Windows artifacts z M4 closure session, niepotrzebne. User wybrał "powiem komendy" dla cleanup w D23 review, ale nie wykonał. Trywialne `Remove-Item nul -Force; Remove-Item .pr-body-close-m4.md`. Niska priority, ale wraca jako "uncommitted changes" warning przy każdym `gh pr create`.
- **`apps/*/types.py` coverage exclude** — dodane w pyproject.toml dla M5. Note dla M6+: każdy nowy `types.py` automatycznie excluded (glob pattern). Brak action item, ale warto wspomnieć w CLAUDE.md §11 ("TypedDict modules excluded from coverage by default").
- **Carry-over z M4 tech debt:**
  - **Branch protection master required `test / Pytest` check** — wciąż brak. Niska priority, ale 5+ M-cykli bez tego check'a to ślepa plama w CI gate.
  - **`django_stubs_ext.monkeypatch()` w `base.py`** — `# type: ignore[type-arg]` na 4 admin classes (characters, accounts, deaths, **nowe: bedmages**). Sweep w jednym chore PR.
  - **Dev runbook dla Celery na Windows** — `docs/dev-runbook.md` skonsolidowany z M3-D17 + M4-D21 retro fragments. M5 nie dorzucił nowych Celery patternów, ale runbook nadal worth.
  - **`celery-types` package** — w M5 NIE uderzyło (D26 modyfikuje istniejący task, nie dorzuca `@shared_task`). Carry-over do M6.

## 🎉 Milestone M6 — Mongo logging COMPLETED (2026-05-13)
Wszystkie 3 zadania ukończone, milestone zamknięty. Design spec: [`docs/superpowers/specs/2026-05-08-m6-mongo-logging-design.md`](docs/superpowers/specs/2026-05-08-m6-mongo-logging-design.md). Implementation plan: [`docs/superpowers/plans/2026-05-08-m6-implementation-plan.md`](docs/superpowers/plans/2026-05-08-m6-implementation-plan.md). MongoDB wprowadzone jako observability backend — `app_logs` (Python logging z `apps.*` loggerów) i `scrape_logs` (1 doc per `scrapy crawl <spider>` invocation). Pierwszy top-level Python package poza `apps/` (`logs_backend/`), pierwsza Scrapy Extension w projekcie (`MongoStatsExtension`), pierwsze e2e przez Django LOGGING dispatch + real Mongo.

### Ukończone (M6)
- ✅ #107 [M6-D28] logs_backend/ package + MongoLogHandler + factory + Django LOGGING integration (2026-05-13) — PR [#110](https://github.com/bgozlinski/tibiantis-scraper/pull/110) — squash `b2c9cd9`
- ✅ #108 [M6-D29] MongoStatsExtension + Scrapy signals + scrape_logs doc (2026-05-13) — PR [#111](https://github.com/bgozlinski/tibiantis-scraper/pull/111) — squash `89082d4`
- ✅ #109 [M6-D30] M6 e2e + closure (PROGRESS.md retro + milestone close) (2026-05-13) — feature PR [#112](https://github.com/bgozlinski/tibiantis-scraper/pull/112) — squash `eb168b3` + ten closure PR

### Notatki z retro M6 (dopisywane progresywnie)
- **#107 D28 (merge 2026-05-13):** Najobszerniejsze code review w projekcie do tej pory — **11 issues w pierwszej iteracji** (4 BLOCKERY, 7 medium). Najbardziej wartościowe momenty:
  - **BLOCKER `handler.py` to dokładny duplikat `__init__.py`** — `LOGGING` dict referencjonował `logs_backend.handlers.factory_or_null`, ale plik nazywał się `handler.py` (singular) ORAZ jego content to copy-paste `__init__.py` (brak `MongoLogHandler`, brak `factory_or_null`). Django start by failował z `AttributeError: module 'logs_backend.handler' has no attribute 'factory_or_null'`. Review caught przed pierwszym push'em.
  - **BLOCKER eager `get_collection("app_logs")` w `MongoLogHandler.__init__`** — `get_collection` przy pierwszym calle robi `create_index` → triggeruje server selection → blokuje 2s i rzuca `ServerSelectionTimeoutError` jeśli Mongo down. Konstruktor handlera odpala się przy Django `dictConfig(LOGGING)` (startup). Skutek bez fix'a: gdy `MONGO_URL` set ale Mongo nie żyje, **Django startup wisi 2s i crashuje** zamiast czysto graceful'ować przez stderr fallback. Spec §3.5 explicit: "Tworzone przy pierwszym `get_collection(name)` call" — intencja lazy. Fix: przeniesiony do `emit()`, każde wywołanie tries lazy. Powtórzony pattern w D29 (review caught po raz drugi — wzorzec na CLAUDE.md addendum).
  - **BLOCKER brak `MONGO_URL` / `MONGO_DB` w `base.py`** — `logs_backend/__init__.py` używał `settings.MONGO_URL` i `settings.MONGO_DB`, ale `base.py` nigdzie ich nie czytał z env. `AttributeError` przy pierwszym `get_database()`. Plus `.env.example` też nie miał tych zmiennych (sprzeczne z CLAUDE.md §10). Fix: dorzucone `env("MONGO_URL", default="")` + `env("MONGO_DB", default="tibiantis_logs")` w `base.py` + 2 linie w `.env.example`.
  - **BLOCKER Mongo brakuje w `docker-compose.dev.yml`** — produkcyjny `docker-compose.yml` zakłada serwis `mongodb`, ale dev compose miał tylko postgres + redis. Bez fix'a lokalny smoke test (`logger.info(...)` w `manage.py shell`) zapisuje do nieistniejącego Mongo → graceful NullHandler/stderr fallback, ale brak weryfikacji że emit faktycznie działa. Fix: serwis `mongo:7` z healthcheck (`mongosh --eval "db.adminCommand('ping').ok"`) + volume `mongo_data`.
  - **Regresja po pierwszym CI push'u: `propagate: False` cisza pytest caplog.** Spec §2 + plan §Pułapka C zalecały `propagate: False` żeby uniknąć duplicate logging (mongo handler + root console). 4 pre-existing testy używające `caplog.records` na `apps.*` loggerach padły bo pytest `LogCaptureHandler` jest na root logger — bez propagacji nie widzi recordów. Spec/plan pułapka była **teoretyczna ale błędna w praktyce**: root logger w tym projekcie nie ma console handlera (Django default `LOGGING` konfiguruje tylko `django` logger, NIE root), więc duplicate by się nie pojawił przy `propagate: True`. Fix: `propagate: True` + komentarz inline w `LOGGING` dict wyjaśniający dlaczego. Lekcja: **propagate=False jest agresywne; zostawić default True chyba że istnieje konkretny root handler którego chcemy uniknąć**. M-future docs/CLAUDE.md addendum kandydat.
  - **Feedback memory mid-PR: `feedback_no_co_authored_by_trailer.md`** — po pushu 3 commitów user explicit zażądał usunięcia `Co-Authored-By: Claude Opus 4.7` trailera z każdego commit message. Override system-prompt default. Recovery: `git filter-branch -f --msg-filter 'sed "/^Co-Authored-By: Claude Opus 4.7/d"' master..HEAD` (3 commity zrewritowane) + `git push --force-with-lease`. Filter-branch deprecated ale prostszy niż interactive rebase dla 3 commitów. Memory zapisana z linkiem do `[[project_workflow]]` (role split — developer owns implementation, Claude mentors).
  - 11 unit testów (D28), 100% coverage `logs_backend/__init__.py` + `logs_backend/handlers.py`. Pre-commit auto-fix: `mixed-line-ending --fix=lf` znormalizował CRLF→LF na pierwszej iteracji feat commitu.
- **#108 D29 (merge 2026-05-13):** Code review 5 issues, 2 BLOCKERY:
  - **BLOCKER `EXTENSIONS = {...}` w `config/settings/base.py` (Django) zamiast `scrapers/tibiantis_scrapers/settings.py` (Scrapy)** — Scrapy używa własnego `Settings` objektu (z `tibiantis_scrapers/settings.py`), Django settings module nigdy nie wpływa na Scrapy crawler. Skutek bez fix'a: extension by się **nie zarejestrował**, signals by nie wpięły, `spider_closed` flush by nigdy nie zaszedł. Plan §Pułapka B explicit, ale i tak miss w pierwszej iteracji — Django + Scrapy mają osobne `settings.py` files i wzorzec łatwo pomylić. Fix: usunąć z `config/settings/base.py:192-194`, dorzucić do `scrapers/tibiantis_scrapers/settings.py` obok `ITEM_PIPELINES`.
  - **BLOCKER eager `get_collection("scrape_logs")` w `MongoStatsExtension.__init__`** — **identyczna pułapka jak w D28** (`MongoLogHandler.__init__`). Tu jeszcze bardziej krytyczna bo `from_crawler` zwraca `cls()` po sprawdzeniu `MONGO_URL` — wywołanie `__init__` z eager `get_collection` rzuca przy Scrapy starcie zamiast graceful disable. Drugi wystąpienie pułapki w jednym milestone'ie. Fix: przeniesiony lookup do `spider_closed` (lazy). **Wzorzec do CLAUDE.md addendum:** `get_collection`/`get_database` NIGDY w `__init__` handler/extension classes; zawsze lazy w hot path metodzie.
  - **🟡 `errors=[]` zawsze pusty** — spec §4.2 zakładał populated when `log_count/ERROR > 0`. Plan §Pułapka G dopuszczała empty, ale wtedy test `test_spider_closed_populates_errors_when_log_count_error_nonzero` nie ma co assertować. Kompromis: marker `[f"log_count/ERROR={n}"]` gdy nonzero, `[]` gdy zero. Sygnał debug bez per-message detail (Scrapy stats i tak nie trzymają per-message strings — to byłby `DOWNLOADER_MIDDLEWARE` scope, kandydat M-future).
  - 9 unit testów (D29), 100% coverage `scrapers/tibiantis_scrapers/extensions.py`. `MagicMock(spec=...)` dla Spider+Crawler — Twisted reactor pominięty (in-process Crawler bringup byłby reactor reuse hazard, plan §Pułapka G).
- **#109 D30 (merge 2026-05-13, ten PR):** Feature PR (e2e) + closure PR (ten dokument) — M5-D27 pattern repeat. Najbardziej wartościowe momenty:
  - **Scrapy `MongoStatsExtension` e2e SKIPPED celowo** per plan §Task #3 Optional AC rekomendacja (opcja 3). Trzy approaches z trade-offs (subprocess vs `get_crawler` vs skip) — wybór skip bo D29 unit testy pokrywają flow z mockiem, a real `scrapy crawl` subprocess inside pytest brings up Twisted reactor z reactor-reuse hazards (M1-D8 lesson historyczny). DoD §8 "scrapy crawl <spider> zapisuje doc do scrape_logs" zakryte manual smoke (admin uruchamia lokalnie + sprawdza `db.scrape_logs.count_documents({})`).
  - **2 Django logger e2e testy** przez configured Django LOGGING dispatch + real Mongo. Sanity assertion `_apps_logger_has_mongo_handler()` na początku każdego testu — gdy `MONGO_URL` był empty przy Django startup, factory zwrócił `NullHandler` i testy by failowały z mylącą `find_one(...) is None`. Lepszy fail-loudly message niż head-scratching debug.
  - **Cumulative coverage 100%** na trzech plikach M6 (`logs_backend/__init__.py`, `logs_backend/handlers.py`, `scrapers/tibiantis_scrapers/extensions.py`) — 80/80 statements, 22 testów (11 + 9 + 2). DoD §7.4 cumulative ≥95% easily cleared.
  - **Closure PR od fresh master** (plan §Pułapka C, M1-D8 lekcja repeat z M5-D27): `git checkout master && git pull && git checkout -b docs/close-m6-mongo-logging` PRZED edycją PROGRESS.md. M5-D27 retro już to wskazywało — wzorzec utrwala się.

### Definition of Done M6 (ze spec'a §8) — ✅ wszystkie domknięte
- [x] **3 Issues zamknięte** (#107 D28 + #108 D29 + #109 D30) + 4 PR-y merged (#110 feat D28 + #111 feat D29 + #112 feat D30 + ten closure PR).
- [x] **`logs_backend/` package** (`__init__.py` + `handlers.py`) z lazy `MongoClient` singleton (`serverSelectionTimeoutMS=2000`) + `MongoLogHandler` + `factory_or_null` — D28.
- [x] **`scrapers/tibiantis_scrapers/extensions.py`** z `MongoStatsExtension` rejestrowanym w `EXTENSIONS` dict (priority 500) — D29.
- [x] **Django `LOGGING`** w `base.py` z handler `"mongo"` przez `'()'` factory, attached do logger `"apps"` (`propagate=True` po regresji caplog z D28).
- [x] **`MONGO_URL` empty → app startuje normalnie** — NullHandler dla Django (factory_or_null), NotConfigured dla Scrapy (MongoStatsExtension.from_crawler). Testy D28 + D29 enforce'ują oba paths.
- [x] **`MONGO_URL` set + Mongo running → docs land** — D30 e2e testuje Django path (`logger.info(...)` → `app_logs`); Scrapy path zakryte D29 unit testy + manual smoke.
- [x] **Mongo down → app NIE crashuje** — `MongoLogHandler.emit` → `self.handleError(record)` → stderr (D28 `test_emit_falls_back_to_stderr_on_mongo_failure`); `MongoStatsExtension.spider_closed` → `spider.logger.error` (D29 `test_spider_closed_logs_error_on_mongo_failure_and_does_not_raise`).
- [x] **Indexes idempotent** — `app_logs.timestamp DESC` + `scrape_logs.spider_name+finished_at DESC` compound, tworzone przy pierwszym `get_collection(name)` call przez `_initialized_collections` guard set (D28 `test_get_collection_idempotent_on_second_call`).
- [x] **Pre-commit + CI zielone** dla każdego z 4 PR-ów (#110, #111, #112, closure).
- [x] **Coverage cumulative `logs_backend/*` + `scrapers/.../extensions.py` ≥ 95%** — osiągnięte **100%** (80/80 stmts).
- [x] **PROGRESS.md** rozszerzony o sekcję M6 z retro per Issue (ten dokument).
- [ ] **Milestone M6 zamknięty na GitHub** — TODO ostatni krok po merge tego PR-a (`gh api -X PATCH repos/bgozlinski/tibiantis-scraper/milestones/6 -f state=closed`).

### Podsumowanie M6 (2026-05-13, 1 dzień vs 2-3 dni z budżetu — wyprzedzenie 1-2 dni, drugi raz pod rząd po M5)
- **3 Issues (#107-#109) + 4 PR-y w 1 dniu.** Strict chain D28→D29→D30 zachowany. Wszystkie merge'owane tego samego dnia w jednej długiej sesji.
- **DoD M6 spełnione** — MongoDB jako observability backend działa end-to-end (Django `apps.*` logger → `app_logs`, `scrapy crawl <spider>` → `scrape_logs`), graceful disable przy empty `MONGO_URL`, silent fallback przy Mongo down. Cumulative 100% coverage na trzech plikach produkcyjnych M6.
- **Najwartościowsze lekcje M6:**
  1. **Eager `get_collection` w `__init__` to recurring pułapka** — wystąpiła **dwa razy w jednym milestone'ie** (D28 `MongoLogHandler`, D29 `MongoStatsExtension`). Code review caught oba razy przed merge'em, ale wzorzec ewidentnie nie-intuicyjny. **CLAUDE.md addendum kandydat:** "Mongo / DB resource lookups NIGDY w `__init__` handler/extension classes — zawsze lazy w hot path metodzie (`emit`, `spider_closed`, etc.). Konstruktor odpala się przy startup'ie (Django `dictConfig`, Scrapy `from_crawler`) — eager init couples startup czas do availability resource'a, podczas gdy lazy odracza failure mode do real call site gdzie graceful fallback już istnieje."
  2. **`propagate: False` cisza pytest caplog** — pułapka która ujawniła się dopiero w CI po pierwszym push'u D28 (4 pre-existing testy padły). Spec/plan zalecały `propagate: False` jako defense przed duplicate logging, ale ta obawa była **teoretyczna w tym projekcie** (root nie ma console handlera). caplog używa root `LogCaptureHandler` — non-propagating loggers go nie widzą. **CLAUDE.md addendum kandydat:** "Default Python logging behavior to `propagate=True`. Override do `False` tylko gdy istnieje konkretny root handler którego chcemy uniknąć — w pure-Django projekcie bez explicit root handlers, propagate=False zerwie pytest caplog dla wszystkich `apps.*` loggerów."
  3. **`EXTENSIONS` dict to Scrapy concept, nie Django** — easy slip bo oba framework'i mają `settings.py`. Lesson: gdy commit dotyka Scrapy-specific config, double-check że trafia do `scrapers/tibiantis_scrapers/settings.py`, NIE `config/settings/base.py`. Memory candidate jeśli powtórzy się w M7+.
  4. **Mid-PR feedback memory: `feedback_no_co_authored_by_trailer.md`** — po pushu 3 commitów user explicit zażądał usunięcia Co-Authored-By trailera. Recovery `filter-branch` + `force-push --force-with-lease` zadziałał czysto. Wzorzec utrwala się: gdy user explicit override systemowego defaultu, save to memory natychmiast (M5 D24 `feedback_services_user_type_hint.md` precedens). Memory ma `[[project_workflow]]` cross-link — kontekst dlaczego override ma sens (developer owns implementation).
  5. **Real Mongo > mongomock** — spec §7.1 decyzja validated przez praktykę. CI mongo:7 service już skonfigurowany od M0, insert+drop ~10ms, bypass real driver bug surface (`client.options.timeout_settings` vs `client.options.pool_options.connect_timeout` discovery wymaganą realnym pymongo — mongomock by zwracał inne API). 0 fake-mock testów w M6.
  6. **Process bug nie wystąpił** — `.pre-commit-config.yaml` local hook `no-commit-to-branch` (M5 carry-over tech debt, zaadresowane przed M6 w PR #105) **działa**. 0 commitów na master w sesji M6, 0 recovery procedures z `git reset --hard`. Tech debt #1 z M5 retro spłacone.

### Tech debt z M6 (do adresowania post-M6)
- **TTL retention indexes** (spec §9 open question) — `app_logs` 30 dni, `scrape_logs` 90 dni. Implementation: TTL index na `timestamp`/`finished_at` przez `expireAfterSeconds`. Decyzja przy pierwszej observability sesji gdy Mongo storage rośnie. Niska priority dopóki traffic mały, ale staraj się nie zapomnieć — unbounded growth.
- **GraphQL admin query** dla `scrape_logs` — `recentScrapeRuns(spider: String, limit: Int)` JWT-protected, admin-only. Kandydat na M7+ (Discord bot dashboard albo standalone admin view).
- **Scrapy MongoStatsExtension real e2e test** — D30 świadomie skipnął (rekomendacja plan §Task #3 opcja 3, Twisted reactor reuse hazards). Aktualnie zakryte: D29 unit + manual smoke. Jeśli kiedyś pojawi się regresja w extension wiring trudna do złapania unit'em, wtedy reach for subprocess approach (plan opcja 1).
- **Dead code `logger = logging.getLogger(__name__)`** w `scrapers/tibiantis_scrapers/extensions.py:16` — review flagged, nie usunięte. Trywialne 1-liner cleanup w next chore PR.
- **`pymongo.MongoClient` generic typing** — `logs_backend/` jest top-level Python package, NIE w `[tool.mypy] exclude`. Aktualnie pre-commit hook `mypy: files: ^apps/` go pomija (tylko `apps/` strict). Jeśli kiedyś rozszerzymy mypy scope na `logs_backend/`, pojawi się `MongoClient[...]` generic param requirement (`MongoClient[dict[str, Any]]`). Niska priority.
- **Async/queued emit** (spec §9 open question) — `MongoLogHandler.emit` jest synchronous (blocking insert ~5-50ms per call). Niska priority dopóki traffic mały, ale `queue.Queue` + daemon thread + batch insert byłby naturalnym wzorcem przy 10x current traffic.
- **Carry-over z M4 tech debt (wciąż otwarte):**
  - **Branch protection master required `test / Pytest` check** — wciąż brak (6+ M-cykli bez tego gate'a). Niska priority ale ślepa plama w CI.
  - **`django_stubs_ext.monkeypatch()` w `base.py`** — `# type: ignore[type-arg]` na 4 admin classes (characters, accounts, deaths, bedmages). Sweep w jednym chore PR.
  - **Dev runbook dla Celery na Windows** — `docs/dev-runbook.md` skonsolidowany z M3-D17 + M4-D21 retro fragments. M6 nie dorzucił nowych Celery patternów, ale runbook nadal worth.
  - **`celery-types` package** — M6 NIE uderzyło (nie dorzuca `@shared_task`). Carry-over do M7.

## 🎉 Milestone M7 — Discord bot commands COMPLETED (2026-05-15)
Wszystkie 5 zadań ukończone + 1 hotfix, milestone zamknięty. Design spec: [`docs/superpowers/specs/2026-05-13-m7-discord-bot-commands-design.md`](docs/superpowers/specs/2026-05-13-m7-discord-bot-commands-design.md). Implementation plan: [`docs/superpowers/plans/2026-05-13-m7-implementation-plan.md`](docs/superpowers/plans/2026-05-13-m7-implementation-plan.md). Discord bot jako **osobny proces** (CLAUDE.md §8) z 4 slash commands end-to-end: `/bedmage add/remove/list` (BedmageCog, user-private ephemeral) + `/deaths threshold` (DeathsCog, admin-gated, public ack). Pierwszy **top-level Django app** w projekcie (`discord_bot/` — różny od `logs_backend/` z M6 bo Django app z `apps.py` + `models.py` + migrations), pierwszy use py-cord, pierwsza integracja z Discord Gateway.

### Ukończone (M7)
- ✅ #115 [M7-D31] discord_bot/ app scaffold + DiscordChannel model + initial services (2026-05-14) — PR [#120](https://github.com/bgozlinski/tibiantis-scraper/pull/120) — squash `4060771`
- ✅ #116 [M7-D32] py-cord bootstrap + run_discord_bot + LOGGING dispatcher (2026-05-15) — PR [#121](https://github.com/bgozlinski/tibiantis-scraper/pull/121) — squash `d279a21`
- ✅ #117 [M7-D33] BedmageCog + /bedmage add/remove/list + bedmage wrapper services (2026-05-15) — PR [#122](https://github.com/bgozlinski/tibiantis-scraper/pull/122) — squash `5283c9e`
- ✅ #118 [M7-D34] DeathsCog + /deaths threshold + admin perm check (2026-05-15) — PR [#123](https://github.com/bgozlinski/tibiantis-scraper/pull/123) — squash `c8e7dc7`
- 🩹 hotfix [PR #124] remove future annotations from cogs (py-cord runtime introspection) (2026-05-15) — squash `71e737c`
- ✅ #119 [M7-D35] M7 e2e + closure (2026-05-15) — feature PR [#125](https://github.com/bgozlinski/tibiantis-scraper/pull/125) — squash `342134a` + ten closure PR

### Notatki z retro M7 (dopisywane progresywnie)
- **#115 D31 (merge 2026-05-14):** Pierwsza decyzja designowa pre-implementacji: `User.discord_id` jako `CharField` (M2 design) vs migracja na `BigIntegerField` dla zgodności z py-cord `ctx.author.id: int` signature. Analizowane razem z developerem — option B (migracja) wymagałaby też override w GraphQL `UserType` na `str | None` z resolverem cast, bo Discord snowflake (~1.4×10¹⁸ w 2026) przekracza GraphQL `Int!` 32-bit range. Option A (zostawić CharField + `str(discord_id)` w service boundary) wygrała: 0 migracji, 0 GQL change, spójność z M2 design choice + Discord REST API convention (snowflake jako string w wire format). Empirycznie potwierdzone w testach że Django `CharField.get_prep_value()` auto-coerce'uje `int → str` na save i lookup → explicit `str()` w service ostatecznie nie został dodany (decyzja użytkownika, runtime behavior był poprawny). Plus tech debt review caught: `apps.py` class name `DeathsConfig` (copy-paste z deaths app) → `DiscordBotConfig`, oraz `UniqueConstraint(name=...)` w `Meta.constraints` zamiast `unique=True` na sam field (M5-D23 idiom). 9 unit testów, 100% coverage `discord_bot/*` (D31 część).
- **#116 D32 (merge 2026-05-15):** Bug w spec'u pułapka H wykryty przez runtime: `env.int("DISCORD_DEV_GUILD_ID", default=0)` **NIE** fallbackuje do `0` gdy env var istnieje ale jest pusty (`.env.example` ships z `DISCORD_DEV_GUILD_ID=` empty by convention). django-environ używa `default` tylko gdy klucz nieobecny w `os.environ` — empty string idzie do `int("")` → `ValueError`. User skopiował `.env.example` → `.env` → Django import wisi przy `dictConfig`. Fix: defensywny `_dev_guild_raw = env(..., default=""); DISCORD_DEV_GUILD_ID = int(_dev_guild_raw) if _dev_guild_raw else 0`. Druga decyzja designowa: Python range narrowing — py-cord caps `<3.14`, projekt miał `>=3.13,<4.0`. Narrow na `>=3.13,<3.14` (Python 3.14 wyszedł 2025-10 per PEP 745; CI runs 3.13 anyway). Mypy + py-cord interaction: 3 strict errors (`no-untyped-call` na `discord.Bot(...)`, `union-attr` na `bot.user.id`, missing type hints na `handle()`). Fix: inline `# type: ignore[no-untyped-call]` + `assert bot.user is not None` + `def handle(self, *args: Any, **options: Any) -> None`. 7 unit testów, `tests/integration/test_m6_logging_e2e.py` regression check (LOGGING dict edit dodanie `"discord_bot"` named logger).
- **#117 D33 (merge 2026-05-15):** Mypy `valid-type` na `discord.Option(str, ...)` annotations był **false-positive ze stale cache**. Pierwszy mypy run flag'ował 2 errors, user dodał `# type: ignore[valid-type]` na obu liniach, drugi mypy run powiedział że ignore są nieużywane. Po `pre-commit clean` mypy zielony bez ignore. Lekcja: **`pre-commit clean` przed flagowaniem py-cord/mypy errors jako blocker** — annotation strict mode + nowo zainstalowane stubs ma timing-sensitive cache behaviour. Test failure debrief: `watch.user.discord_id == "42"` (str) asercja padła bo in-memory User po `get_or_create(discord_id=42, ...)` ma `discord_id=42` (int) — Django coerce'uje dopiero na save round-trip. Naprawione przesunięciem asercji na DB-level lookup (`User.objects.filter(discord_id="42", pk=watch.user_id).exists()`) zamiast in-memory state. 15 nowych testów (7 service wrappers + 7 cog handlers + 1 cog registration), 100% coverage M5 wrappers + BedmageCog.
- **#118 D34 (merge 2026-05-15):** Mypy `union-attr` + `arg-type` po DM check — py-cord typuje `ctx.author: Member | User` i `ctx.channel_id: int | None` konserwatywnie. Runtime po `if ctx.guild is None: return` gwarantuje że to Member + int, ale mypy nie inferuje correlation. Decyzja: narrow asserts `assert isinstance(ctx.author, discord.Member)` + `assert ctx.channel_id is not None` PRZED admin check. Czystsze niż inline `# type: ignore` (dwa accesses `ctx.author.guild_permissions` na różnych liniach, 2× ignore byłby noise). Plus runtime sanity — gdy py-cord typing kiedyś się zmieni, assert łapie regresję. **Test fallout**: 4 testy padły po dodaniu assertu bo `MagicMock(spec=ApplicationContext).author` to nie `discord.Member` — fix `mock_ctx.author = MagicMock(spec=discord.Member)` w testach admin/non-admin (DM rejection test untouched, short-circuits przed assert). Plus `pyproject.toml` override `[[tool.mypy.overrides]] module=["discord_bot.cogs.*"] disable_error_code=["valid-type"]` dodany prewencyjnie (po D33 cache fluke) — mimo że po `pre-commit clean` valid-type nie wraca, user wolał zostawić dla skalowania na przyszłe cogi (M-future kandydat na cleanup gdyby py-cord stubs się ustabilizowały). 6 nowych testów (2 guard + 3 success path + 1 registration), 100% coverage DeathsCog.
- **hotfix #124 (merge 2026-05-15):** **NAJWAŻNIEJSZA LEKCJA M7.** Po D33 merge user zrobił manual smoke `/bedmage add yhral` w dev guildzie → `TypeError: issubclass() arg 1 must be a class`, surfacowany przez D32 global error handler jako "❌ Something went wrong". Root cause: `from __future__ import annotations` w obu cog modules robi annotation strings (PEP 563) — py-cord introspectuje parameter annotations **przy invocation komendy** (gdy Discord wysyła Interaction), nie przy imporcie modułu. `issubclass("<string>", SomeClass)` crash. Fix: usunąć `from __future__ import annotations` z `discord_bot/cogs/bedmages.py` + `deaths.py` (Python 3.13 ma natywnie wszystkie potrzebne types — `tuple[X,Y]`, `User | None`, builtin generics — bez PEP 563). Dodać komentarz `# NOTE: NO __future__ annotations — py-cord runtime introspection` żeby kolejny dev nie wrócił. **Test coverage gap**: 38 unit testów było zielonych przed hotfix'em bo `.callback` bypass pattern omija py-cord option-parsing pipeline. Wszystkie 4 commands były złamane w runtime, manual smoke był jedyną siecią. Fix-test ratio: 2 plików zmienionych (po 1 linii each), 0 nowych testów (regression byłaby trudna — wymaga symulacji Discord Interaction). Lesson dla D35 e2e: dorzucić sanity test który chodzi przez py-cord registration path.
- **#119 D35 (merge 2026-05-15, ten PR):** Feature PR + closure PR (M5-D27 + M6-D30 pattern repeat). E2e test `tests/integration/test_m7_bot_e2e.py` z 1 assertion `setup_bot().cogs[...].walk_commands()` zwraca wszystkie 4 commands. Dwa fixes wymagane do uruchomienia:
  1. **`bot.walk_application_commands()` returns empty set** — `SlashCommandGroup`-nested commands siedzą w cog'u do gateway sync, top-level walk ich nie widzi. Fix: iterate per-cog (`for cog in bot.cogs.values(): for command in cog.walk_commands(): ...`).
  2. **`Cog already loaded` przy 2 wywołaniach `setup_bot()`** — D32 `test_setup_bot_returns_a_discord_bot_instance` + nowy e2e wołają w tej samej sesji pytest, drugi `add_cog` na module-level `bot` singletonie rzuca `discord.errors.ClientException`. Pułapka F w spec'u przewidziała to. Fix: production code — `setup_bot()` idempotent (`if "BedmageCog" not in bot.cogs: ...`). Zero kosztu w prod (single call), unblocked tests + future test writers nie wpadną w pułapkę. Plus stale docstring `"D32: no cogs yet (D33+D34 add them)"` zaktualizowany przy okazji.

  **Manual smoke w dev guildzie:** wszystkie 8 punktów ze spec'a §7 (3× `/bedmage *` happy path + 1× duplicate-ack + 1× empty list + 1× admin success + 1× non-admin reject + 1× DM reject) — **all OK** (potwierdzenie użytkownika). Cumulative coverage `discord_bot/*` = **100%** na 14 plików, 39 unit + 1 integration test.

  **Closure PR od fresh master** (pułapka C, M1-D8 lekcja repeat z M5-D27 + M6-D30): `git checkout master && git pull && git checkout -b docs/close-m7-discord-bot-commands` PRZED PROGRESS.md edycją. Wzorzec utrwala się.

### Definition of Done M7 (ze spec'a §8) — ✅ wszystkie domknięte
- [x] **5 D-tasków zamkniętych** (#115 D31 + #116 D32 + #117 D33 + #118 D34 + #119 D35) + 1 hotfix (PR #124) + closure PR.
- [x] **`discord_bot/` jako top-level Django app** zarejestrowany w `LOCAL_APPS` (bez `apps.` prefix), migracja `0001_initial` aplikuje się czysto.
- [x] **`DiscordChannel` model** z `UniqueConstraint(name="discord_channel_one_per_guild")` w `Meta.constraints` + Django admin (list_display, search_fields, readonly_fields).
- [x] **5 services** w `discord_bot/services.py` — `get_or_create_user_by_discord_id` + `set_death_threshold_for_guild` (D31) + 3 bedmage wrappers (D33: `add/remove/list_bedmage_for_discord_user`).
- [x] **4 slash commands** funkcjonalnie kompletne:
  - `/bedmage add/remove/list` — ephemeral responses, auto-create user, idempotent (✅ created vs ℹ️ already-on-list / removed vs not-on-list).
  - `/deaths threshold` — public ack, admin perm check (`ctx.author.guild_permissions.administrator`), DM rejection, DiscordChannel upsert.
- [x] **Bot startup** — `python manage.py run_discord_bot` loguje się do Discord, sync'uje commands per-guild (dev) lub globalnie (prod). Manual smoke confirmed.
- [x] **Global error handler** — `on_application_command_error` → `logger.exception` (→ Mongo via M6 dispatcher) + ephemeral generic msg. `ctx.response.is_done()` rozróżnia `respond` vs `followup.send`.
- [x] **LOGGING dict** rozszerzony o `"discord_bot"` named logger (`handlers: ["console", "mongo"]`, `propagate: True` per M6 retro #2 caplog lesson).
- [x] **Pre-commit + CI zielone** dla każdego z 6 PR-ów (#120 D31, #121 D32, #122 D33, #123 D34, #124 hotfix, #125 D35 feat).
- [x] **Coverage cumulative `discord_bot/*` ≥ 95%** — osiągnięte **100%** (154/154 stmts na 14 plików).
- [x] **PROGRESS.md** rozszerzony o sekcję M7 z retro per Issue (ten dokument).
- [x] **Manual smoke** — wszystkie 4 commands zweryfikowane w dev guildzie po lokalnym `manage.py run_discord_bot` (8 punktów ze spec'a §7).
- [ ] **Milestone M7 zamknięty na GitHub** — TODO ostatni krok po merge tego PR-a (`gh api -X PATCH repos/bgozlinski/tibiantis-scraper/milestones/7 -f state=closed`).

### Podsumowanie M7 (2026-05-14 → 2026-05-15, ~2 dni vs 2-3 dni z budżetu — w budżecie)
- **5 Issues (#115-#119) + 6 PR-ów (5 feat + 1 hotfix) + closure PR w 2 dniach.** Strict chain D31→D32→D33→D34→D35 zachowany. Hotfix #124 wpadł między D33 i D34 (wykryty manual smoke'em po D33 merge, fix przed D34 startem).
- **DoD M7 spełnione** — 4 slash commands działają end-to-end w dev guildzie (manual smoke confirmed all 8 punktów). Cumulative 100% coverage na 14 plikach produkcyjnych M7. Pierwszy top-level Django app w projekcie zarejestrowany czysto, py-cord zintegrowany, Mongo dispatch dla bot errors wpięty.
- **Najwartościowsze lekcje M7:**
  1. **`from __future__ import annotations` ≠ py-cord cogs (HOTFIX #124).** PEP 563 robi annotations stringami w runtime; py-cord introspectuje parameter types **przy invocation komendy** (`issubclass(annotation, SomeClass)`), nie przy imporcie. Crash niewykryty unit testami (`.callback` bypass omija option-parsing). **Manual smoke był jedyną siecią**. Lekcja praktyczna: keep future annotations OFF w cog modules (Python 3.13 ma natywnie wszystkie potrzebne types), plus pre-merge manual smoke dla każdego nowego cog'a do M-future gdy mamy lepszy regression test setup. **CLAUDE.md addendum kandydat:** "py-cord cog modules MUSZĄ mieć `from __future__ import annotations` WYŁĄCZONY — runtime annotation introspection przy slash command invocation."
  2. **Cog `.callback` test pattern ma znany blind spot.** Wszystkie 38 unit testów było zielonych przed hotfix'em. Pattern jest user-friendly (parametr values passed directly, no Interaction simulation needed) ale całkowicie omija py-cord's option-parsing + command-registration pipeline. **D35 e2e jest częściowym fix'em** — `bot.cogs[...].walk_commands()` walk weryfikuje że cogi są zarejestrowane, ale nadal nie symuluje Interaction. Pełne pokrycie wymagałoby `bot._invoke(interaction)` lub subprocess test z real Discord — trade-off complexity, M-future jeśli pojawi się druga regresja w tym layerze.
  3. **`pre-commit clean` przed mypy false-positive flagging.** D33 stracił ~20 minut na inline `# type: ignore[valid-type]` które okazały się "unused" po następnym mypy run. Stale cache po fresh py-cord install + first mypy invocation = transient false errors. **Wzorzec na CLAUDE.md addendum:** "Gdy mypy zgłasza nowy error na świeżo zainstalowanej bibliotece (zwłaszcza ze stub'ami), `poetry run pre-commit clean` przed dodaniem `# type: ignore` — w 30% przypadków stale cache."
  4. **Mypy strict + py-cord union types → narrow asserts.** `ctx.author: Member | User` i `ctx.channel_id: int | None` w py-cord są konserwatywne, runtime guards (DM check) nie są inferowane przez mypy. Pattern: defensive `assert isinstance(ctx.author, discord.Member)` + `assert ctx.channel_id is not None` PRZED accessem. Bonus runtime sanity (łapie py-cord typing regresję). **Lepsze niż inline `# type: ignore`** gdy access używany w >1 miejscu — dwa lokale `ctx.author.guild_permissions` w cog'u, 2× ignore byłby noise. Test fallout: `MagicMock(spec=discord.Member)` po dodaniu assertu (D34 test fix).
  5. **`env.int(..., default=int)` jest fragile dla `.env.example` empty-by-convention.** django-environ tylko fallbackuje na default gdy klucz NIEOBECNY w `os.environ` — empty value (`DISCORD_DEV_GUILD_ID=`) idzie do `int("")` → ValueError. Spec pułapka H twierdziła inaczej. Defensywny pattern: `_raw = env("KEY", default=""); X = int(_raw) if _raw else 0`. **CLAUDE.md addendum kandydat:** "django-environ `.int()`/`.bool()`/`.list()` z empty env var crashuje, NIE fallbackuje. Defensive pattern: explicit `env(...) or default` + manual cast."
  6. **`setup_bot()` idempotent** (D35) — module-level singleton `bot` współdzielony przez testy wymagał guard'a `if "BedmageCog" not in bot.cogs: ...` żeby D32 `test_setup_bot_returns_a_discord_bot_instance` + D35 e2e mogły coexist w jednej sesji pytest. Production code stał się czystszy przy zerowym koszcie (single call w prod). **Wzorzec ogólny:** kiedy production singleton współdzielony przez testy, idempotency pays for itself.
  7. **Discord IDs jako CharField, nie BigIntegerField** (D31 design decision). Discord snowflake ~1.4×10¹⁸ przekracza GraphQL `Int!` 32-bit range, plus Discord REST API ships snowflakes jako stringi. M2 design choice się obronił po dyskusji — auto-coerce CharField przez Django `get_prep_value` wystarcza, eksplicytny `str()` boundary w service ostatecznie nie był potrzebny (runtime confirms). Carry-over: zmiana w przyszłości wymagałaby też GQL UserType resolverze override — koszt rośnie nieproporcjonalnie.
  8. **Process bug nie wystąpił po raz drugi.** `.pre-commit-config.yaml` local hook `no-commit-to-branch` (M5 carry-over zaadresowane PR #105) dalej działa. 0 commitów na master w M7. Tech debt #1 z M5 retro permanently fixed.

### Tech debt z M7 (do adresowania post-M7)
- **Regression test dla py-cord option-parsing path** (carry-over lekcji 2 + hotfix #124). Pełne pokrycie wymaga symulacji `discord.Interaction` lub subprocess test z real `bot.run()` w mocked gateway. Trade-off complexity. Kandydat na M8 (outbound notifications) jeśli pojawi się druga regresja w tym layerze — póki co manual smoke pre-merge wystarczy.
- **`bot.py setup_bot()` idempotent guard** używa string-based name lookup (`"BedmageCog"`). Jeśli cogi się przemianują (np. refactor `BedmageCog → BedmageTrackerCog`), guard milcząco zawodzi (nigdy nie matches → cog dorzucany duplicate-times). Defensywniej: `if BedmageCog not in {type(c) for c in bot.cogs.values()}`. Niska priority dopóki nie ma rename'u w planach.
- **`[[tool.mypy.overrides]] discord_bot.cogs.*` z `disable_error_code=["valid-type"]`** dodany prewencyjnie w D34 — aktualnie nie tłumi żadnego błędu (post `pre-commit clean`). Kandydat na cleanup gdy py-cord stubs się ustabilizują (kontrolne sprawdzenie przy każdym `poetry update` py-cord).
- **`discord.Bot` mypy `no-untyped-call` inline ignore** (`# type: ignore[no-untyped-call]` na `bot = discord.Bot(intents=intents)` w `bot.py:11`) — gdy py-cord doda runtime types do `Bot.__init__`, można usunąć. Watching releases.
- **Stale docstring `bot.py setup_bot()`** — zaktualizowany w D35 ("Register cogs and return configured bot. Idempotent ..."). **Spłacone.**
- **Carry-over z M4/M5/M6 tech debt (wciąż otwarte):**
  - **Branch protection master required `test / Pytest` check** — wciąż brak (7+ M-cykli bez tego gate'a). Niska priority ale ślepa plama w CI.
  - **`django_stubs_ext.monkeypatch()` w `base.py`** — `# type: ignore[type-arg]` na 5 admin classes (characters, accounts, deaths, bedmages, discord_bot). M7 dorzucił 5. piąty (DiscordChannelAdmin). Sweep w jednym chore PR coraz bardziej kuszący.
  - **Dev runbook dla Celery na Windows** — `docs/dev-runbook.md` skonsolidowany z M3-D17 + M4-D21 retro fragments. M7 dorzucił dev runbook fragment dla bot'a (`DISCORD_BOT_TOKEN` + `DISCORD_DEV_GUILD_ID` env setup, dev guild Server ID copy z Discord Developer Mode).
  - **`celery-types` package** — M7 NIE uderzyło (nie dorzuca `@shared_task`). Carry-over do M8.
  - **TTL retention indexes na Mongo** (z M6 tech debt) — dopóki traffic mały, niska priority. Bot logi via `"discord_bot"` named logger lecą do `app_logs` z M6 (D32 LOGGING dispatch), więc applicable do whole `app_logs` collection.

---

## 🎉 Milestone M8 — Discord outbound notifications COMPLETED (2026-05-15)
Wszystkie 5 zadań ukończone + 2 hotfix (#133/#139, oba unrelated to M8 issues — fix tangential bugs surfaced during M8 work), milestone zamknięty. Design spec: [`docs/superpowers/specs/2026-05-15-m8-outbound-notifications-design.md`](docs/superpowers/specs/2026-05-15-m8-outbound-notifications-design.md). Implementation plan: [`docs/superpowers/plans/2026-05-15-m8-implementation-plan.md`](docs/superpowers/plans/2026-05-15-m8-implementation-plan.md). Pętla outbound notifications zamknięta end-to-end: bedmage alerty trafiają jako Discord DMs (`DiscordDMHandler` zastępuje M5 `LoggingHandler` jako default), death announcements jako embed messages w per-guild kanale z `death_level_threshold` filtering. Discord REST API wywoływany **bezpośrednio z Celery worker'a** (CLAUDE.md §8 — bot proces nadal tylko inbound, outbound NIE wymaga gateway connection). Pierwszy use `httpx` w projekcie, pierwszy `Protocol` z dwoma równoległymi implementacjami (LoggingHandler dev + DiscordDMHandler prod) per M5 wzorzec.

### Ukończone (M8)
- ✅ #128 [M8-D36] DiscordRESTClient + httpx dep + 6 tests (2026-05-15) — PR [#135](https://github.com/bgozlinski/tibiantis-scraper/pull/135) — squash `5a5a3d4`
- ✅ #129 [M8-D37] DiscordDMHandler + bedmage message format + settings default flip + 3 tests (2026-05-15) — PR [#136](https://github.com/bgozlinski/tibiantis-scraper/pull/136) — squash `8ef6f20`
- 🩹 hotfix [PR #134] allow NULL email (#133 — Discord auto-create collided on unique empty email) (2026-05-15) — squash `9e2afee`
- ✅ #130 [M8-D38] DeathEvent.announced_on_discord migration + DeathAnnouncementHandler + DiscordChannelHandler + 3 tests (2026-05-15) — PR [#137](https://github.com/bgozlinski/tibiantis-scraper/pull/137) — squash `e6be801`
- ✅ #131 [M8-D39] announce_unannounced_deaths service + scrape_deaths integration + 10 tests (2026-05-15) — PR [#138](https://github.com/bgozlinski/tibiantis-scraper/pull/138) — squash `4ab77dd`
- 🩹 hotfix [PR #140] allow NULL house/guild_membership (#139 — spider crash on low-level characters without house/guild) (2026-05-15) — squash `8f9daf6`
- ✅ #132 [M8-D40] M8 e2e + closure (2026-05-15) — feature PR [#141](https://github.com/bgozlinski/tibiantis-scraper/pull/141) — squash `acaa3c9` + ten closure PR

### Notatki z retro M8 (dopisywane progresywnie)
- **#128 D36 (merge 2026-05-15):** Foundation PR — `DiscordRESTClient` z 6 testami via `httpx.MockTransport`. Najtrudniejsze zaprojektowanie fixture: naive `monkeypatch.setattr("httpx.Client", lambda **kw: httpx.Client(transport=transport, **kw))` self-recurses, bo lambda woła `httpx.Client` które już jest patched. Fix: capture `real_client_cls = httpx.Client` PRZED patch'em, lambda używa capture (Pułapka E z spec'a). Tighter pin `httpx (>=0.28.1,<0.29.0)` zamiast spec'owego `(>=0.27,<1.0)` żeby ograniczyć Dependabot churn — łatwo poluzować w follow-up. Per-call `httpx.Client()` przez context manager (M6 retro lekcja — brak eager instance-level client; Pułapka H z spec'a) trade-off: brak TCP connection reuse między `_post` calls, akceptowalne dla low-frequency notification traffic. Coverage `discord_client.py` 80% (CI threshold 70%) — uncovered branches (malformed DM response, timeouts, empty bot token, embed branch, double-5xx) świadomie poza scope AC, podniesione potem w D40 do 96% cumulative.
- **#129 D37 (merge 2026-05-15):** `DiscordDMHandler` + flip `BEDMAGE_NOTIFICATION_HANDLER` default `LoggingHandler` → `DiscordDMHandler` w `base.py` + `.env.example`. Kluczowa decyzja: **lazy import `DiscordRESTClient` wewnątrz `notify()`** zamiast top-level import. Powód: top-level `from apps.notifications.discord_client import DiscordRESTClient` ciągnie `httpx` przy Django startup, co przebija się przez `apps.notifications` import chain do mypy pre-commit hooka (isolated venv bez `httpx`). Lazy import unblockował D37 bez osobnego infra PR-a na `.pre-commit-config.yaml additional_dependencies` (CLAUDE.md §12). Drugi nieoczywisty kawałek: `int(watch.user.discord_id or "")` w None handling — coerce `None → "" → int("") → ValueError → caught`. Czystsze niż `except (TypeError, ValueError)`, mypy strict happy bez `# type: ignore`. Regression guard: `test_m5_bedmages_e2e.py` zaktualizowany o `@override_settings(BEDMAGE_NOTIFICATION_HANDLER=LoggingHandler)` żeby istniejący `@mock.patch("...LoggingHandler.notify")` dalej intercepted po flip (Pułapka D z spec'a — załapana w PR review).
- **hotfix #134 (#133, merge 2026-05-15):** Pierwszy hotfix M8 — Discord auto-create przez `/bedmage add` collided na `email=""` unique constraint (`AbstractUser.email: EmailField(unique=True)`). Wykryty manual smoke'em w dev guild po D37 merge. Fix: narrow `email` w `apps/accounts/models.py User` na `null=True, blank=True` z `# type: ignore[misc]` na nadpisanie field shape z AbstractUser. `accounts/migrations/0003_alter_user_email.py` + 1 unit test (`UserManager.create_user(email=None)` works). REST register serializer nadal wymusza real email — divergence kontraktów (model permissive, API restrictive) świadoma. Tech debt review: za 5-10 PR-ów warto przemyśleć custom `User` z `email` opcjonalnym from-the-start zamiast nadpisywać AbstractUser.
- **#130 D38 (merge 2026-05-15):** `DeathAnnouncementHandler` Protocol + 2 implementacje (`DiscordChannelHandler` prod, `DeathLoggingHandler` test) + `DeathEvent.announced_on_discord = BooleanField(default=False, db_index=True)` migracja `0003`. `db_index=True` wymagany dla D39 hot path `filter(announced_on_discord=False)`. Pułapka C z spec'a załapana: `embed["color"]: int 0xDC143C` (NIE `"#DC143C"` string) — Discord API returns 400 dla hex string colors. Test pinuje `isinstance(embed["color"], int)` defensively. Decyzja design'owa: **`DeathEvent` + `DiscordChannel` imports w `if TYPE_CHECKING:`** w `handlers.py` (Django app registry init issue z mypy plugin + `from __future__ import annotations`) — caught w PR review, nie spec. **`DeathLoggingHandler` distinct name od M5 `LoggingHandler`** (same module, identical names → resolver collision na fully-qualified path). 3 unit testy z AC + 3 bonus dla parity z M5 resolver coverage = 6 total na nowy plik. Trzecie `# type: ignore[misc]` (i.e. carry-over z M5/M6) — handlers.py `notify()` signatures.
- **hotfix #140 (#139, merge 2026-05-15):** Drugi hotfix M8 — spider crashował w `house = "None"` (string literal "None"!) → `Character.house = "None"` save'd, ale nowy spider build próbował NULL na low-level characters bez domu/gildii → `IntegrityError: null value in column "house"`. Root cause: `Character.house: CharField(max_length=128, blank=True, default="")` był NOT NULL, ale spider sometimes parsed `None` literal from page. Fix: narrow `house` + `guild_membership` na `null=True, blank=True, default=""` + migracja `0005_alter_character_house_and_more.py` + spider/service regression tests (8 nowych testów na `characters/services.py` upsert path). Tech debt review: konsystencja default — pusta string `""` vs `None` w blank field. Wybrano `null=True` żeby `IntegrityError` nie blokował scrape'u; database-level `""` vs `NULL` znaczy "no data" tak samo. Spłacenie carry-over z M0/M1 character model design (CLAUDE.md §5 spec to bez `null=True` mówił).
- **#131 D39 (merge 2026-05-15):** Najgrubszy commit M8 — `announce_unannounced_deaths()` z multi-guild fan-out + `scrape_deaths` integration. Wzorzec **all-or-nothing per-event**: any guild fail → event stays `False`, retry next cycle (Pułapka E z spec'a — `try/except` wokół `handler.announce` łapie unexpected raises jako fail bez aborting batcha). Trade-off documented spec §3.3: partial guild outage powoduje duplicate sends do healthy guilds next cycle. M-future split na M2M tracking. Drugi nieoczywisty: **"evaluated + skipped" semantyka** — gdy 0 applicable guilds (threshold above level), mark `announced=True` mimo braku wysłania, żeby low-level events nie re-evaluating w każdym scrape cycle. Test failure z M4 regression: dict equality `assert result == {"yielded": 50, ...}` padł bo D39 dorzucił 3 nowe klucze do task summary. Fix: zamiana na subset assertions (`assert result["yielded"] == 50`, per-key) — forward-compat na D40 + M-future additions. Pułapka F z spec'a. 10 testów (8 service unit + 2 task integration).
- **#132 D40 (merge 2026-05-15, ten PR):** Feature PR + closure PR (M5-D27 + M6-D30 + M7-D35 pattern repeat). E2e test `tests/integration/test_m8_outbound_e2e.py` z 2 testami: bedmage flow (service → settings → DiscordDMHandler → mocked send_dm) + death flow (service → settings → DiscordChannelHandler → mocked send_channel_message). **Odstępstwo od spec'a issue body:** patch target `monkeypatch.setattr("apps.notifications.handlers.DiscordRESTClient", ...)` z AC #132 NIE działał — handlery robią lazy `from apps.notifications.discord_client import DiscordRESTClient` wewnątrz `notify()`/`announce()`, więc w handlers-module namespace nie ma `DiscordRESTClient` w ogóle. Correct path: source module `apps.notifications.discord_client.DiscordRESTClient`, ten sam wzorzec co M8 unit testy (`test_discord_dm_handler.py:48`, `test_discord_channel_handler.py:54-55`). Spec patch target był copy-paste z draft'u prawdopodobnie zanim D37/D38 zdecydowali o lazy import — caught przy weryfikacji przed napisaniem testu.

  **Coverage bump:** cumulative `apps/notifications/*` + `apps/deaths/services.py` po samych e2e testach było 93% < 95% AC. Dorzucone 3 unit testy do `test_discord_client.py` (embed wrapping w `embeds: [...]`, empty bot token early return, 5xx-after-retry give-up) — bumpują discord_client.py 80% → 88%, cumulative 93% → **96%**. Decyzja: zostawić uncovered linie (42-46 malformed DM JSON, 88-90 httpx timeout/connect error, 123 unreachable fall-through) jako carry-over tech debt z D36 — wszystkie 3 to defensive paths trudne do hit'owania bez complex httpx mock setup, niska value vs koszt testu.

  **Closure PR od fresh master** (Pułapka C z spec'a, M1-D8 + M5-D27 + M6-D30 + M7-D35 lekcja repeat): `git checkout master && git pull && git checkout -b docs/close-m8-outbound-notifications` PRZED PROGRESS.md edycją. Wzorzec utrwala się — 5. raz pod rząd.

### Definition of Done M8 (ze spec'a §8) — ✅ wszystkie domknięte
- [x] **5 D-tasków zamkniętych** (#128 D36 + #129 D37 + #130 D38 + #131 D39 + #132 D40) + 2 hotfixy (#133/PR #134, #139/PR #140) + closure PR.
- [x] **`apps/notifications/discord_client.py`** z `DiscordRESTClient` (httpx, bot token z settings, send_dm + send_channel_message, 1-retry na 5xx/429, graceful degradation na empty bot token).
- [x] **`apps/notifications/handlers.py`** rozszerzony o `DiscordDMHandler` + `DeathAnnouncementHandler` Protocol + `DiscordChannelHandler` + `DeathLoggingHandler` test variant.
- [x] **`apps/notifications/__init__.py`** export `get_death_handler()` resolver równolegle do M5 `get_bedmage_handler()`.
- [x] **`apps/deaths/models.py`** z `announced_on_discord = BooleanField(default=False, db_index=True)` + migracja `0003_deathevent_announced_on_discord.py`.
- [x] **`apps/deaths/services.py`** rozszerzony o `announce_unannounced_deaths()` z multi-guild iteration + "evaluated + skipped" semantyką + all-or-nothing per-event.
- [x] **`apps/deaths/tasks.py scrape_deaths`** wywołuje announce inline po subprocess parse, defensive try/except żeby Celery nie marked task failed.
- [x] **`config/settings/base.py`** — `BEDMAGE_NOTIFICATION_HANDLER` default `LoggingHandler` → `DiscordDMHandler` + NEW `DEATH_NOTIFICATION_HANDLER = env(..., default="apps.notifications.handlers.DiscordChannelHandler")`.
- [x] **`pyproject.toml`** dorzucone `httpx (>=0.28.1,<0.29.0)` (tighter pin niż spec'owe `(>=0.27,<1.0)`).
- [x] **`.env.example`** rozszerzone o `BEDMAGE_NOTIFICATION_HANDLER` (flip do DiscordDMHandler) + `DEATH_NOTIFICATION_HANDLER`.
- [x] **Pre-commit + CI zielone** dla wszystkich 7 PR-ów (#135 D36, #136 D37, #134 hotfix, #137 D38, #138 D39, #140 hotfix, #141 D40 feat).
- [x] **Coverage cumulative `apps/notifications/*` + `apps/deaths/services.py` (M8 części) ≥ 95%** — osiągnięte **96%** (cumulative na 161 stmts).
- [x] **PROGRESS.md** rozszerzony o sekcję M8 z retro per Issue (ten dokument).
- [x] **Manual smoke** udokumentowany w closure PR description (4 punkty z §9 spec).
- [ ] **Milestone M8 zamknięty na GitHub** — TODO ostatni krok po merge tego PR-a (`gh api -X PATCH repos/bgozlinski/tibiantis-scraper/milestones/8 -f state=closed`).

### Podsumowanie M8 (2026-05-15, 1 dzień vs 2 dni z budżetu — wyprzedzenie 1 dnia, trzeci raz pod rząd po M5+M6+M7)
- **5 Issues (#128-#132) + 7 PR-ów (5 feat + 2 hotfix) + closure PR w 1 dniu.** Strict chain D36→D37→D38→D39→D40 zachowany. Dwa hotfixy (#133/#139) wpadły między D-tasks, oba unrelated do M8 issues — surfaced przez M8 work ale ich root cause był pre-M8 (NULL handling w User.email + Character.house/guild_membership).
- **DoD M8 spełnione** — outbound pętla zamknięta end-to-end: bedmage DMs + death channel embeds, oba przez `DiscordRESTClient` z 1-retry + graceful degradation. Cumulative coverage 96% (AC ≥95%). 55 testów (44 unit notifications/deaths + 1 e2e + 10 regression). Pierwsze `httpx` w projekcie, drugi `Protocol`-based handler abstraction (M5 wzorzec replicated dla deaths).
- **Najwartościowsze lekcje M8:**
  1. **Lazy import jako workaround na pre-commit mypy isolated venv** (D37, D38 wzorzec). Top-level `from apps.notifications.discord_client import DiscordRESTClient` w `handlers.py` ciągnęłoby `httpx` przez Django app load chain do mypy pre-commit hooka (isolated venv bez `httpx`). Alternatywa: dodać `httpx` do `.pre-commit-config.yaml additional_dependencies` w osobnym infra PR (CLAUDE.md §12). Wybór lazy import — handler funkcjonalność nie wymaga `httpx` przy module load, tylko at-call-time. Bonus: defers `httpx` loading dla startup speed. **CLAUDE.md addendum kandydat:** "Gdy nowa dependency (`httpx`, `redis`-py extras itd.) jest używana tylko by jeden handler/service, lazy import wewnątrz funkcji może uniknąć infra PR-a na pre-commit hook venv."
  2. **Spec patch targets mogą być stale po decyzjach D-task'ów.** D40 issue body proponował `monkeypatch.setattr("apps.notifications.handlers.DiscordRESTClient", ...)` bo spec napisany ZANIM D37/D38 zdecydowali o lazy import. Lekcja: **weryfikuj patch targets przed pisaniem testu** — `grep` na faktyczny import statement w handlers.py. Drobny przypadek "spec lagging implementation" — typowy gdy spec pisany top-down z domain design, a implementacja decyduje details bottom-up. Caught w fazie pre-implementacji (Read tool przed Write).
  3. **"Evaluated + skipped" jako idempotent termination state** (D39). Gdy 0 applicable guilds dla DeathEvent (threshold above level), mark `announced=True` mimo braku wysłania — żeby low-level events nie re-evaluating w każdym scrape cycle. Pattern: **terminal state to NIE tylko "success"** ale też "evaluated, no targets". Carry-over kandydat na inne queues/queries: gdy item nie ma targets ale jest already evaluated, mark go żeby workflow nie reentered. Trade-off: backfill historycznych deaths po dodaniu DiscordChannel z niższym threshold = NIE retroaktywny send (M-future M2M tracking).
  4. **Tighter version pins kontrolują Dependabot churn.** D36 dorzucił `httpx (>=0.28.1,<0.29.0)` zamiast spec'owego `(>=0.27,<1.0)`. Powód: szeroki range znaczyłby weekly Dependabot PR na każdy patch bump (0.27.1 → 0.27.2 → 0.28.0). Tight pin = ręczna decyzja gdy chcemy upgrade'a. Trade-off: świadome ignorowanie security fixes do manualnego review. CLAUDE.md §10 brzmi o tym kontekście (`poetry add` defaults to caret) — addendum kandydat.
  5. **All-or-nothing per-event ma znany trade-off na partial guild outage** (D39). Jak jeden z N guildów failuje, event stays `announced=False`, next cycle re-sends do WSZYSTKICH N guildów (w tym tych co już dostały). Spec §3.3 documented. M-future: M2M `(event, channel, sent_at)` tracking model + migration backfill. Aktualnie 1 dev guild, ryzyko teoretyczne. **Wzorzec ogólny:** *prosta state model + accepted retry duplication* > *complex per-target state + zero duplication* dopóki traffic mały + 1 target.
  6. **Forward-compat assertions w regression tests** (D39 + D40). M4 e2e test miał `assert result == {"yielded": 50, "duplicates": 0, "returncode": 0}` strict equality — padł gdy D39 dorzucił 3 klucze do dict. Fix: subset assertions per-key (`assert result["yielded"] == 50` × 3). Aktualizacja w D39 PR, D40 e2e już używa subset asercji. **Lekcja generalna:** dla return-dict contracts, subset assertions > strict equality, żeby additive changes nie były breaking. **Hard equality OK** dla shape contracts (np. summary dict z exact 3 kluczami — D39 test_announce_summary_dict_reports_counts).
  7. **Coverage bump przez unit testy zamiast e2e**. D40 e2e dał `apps/deaths/services.py` 100% ale `discord_client.py` zostało na 80% (z D36). Cumulative 93% < 95% AC. Pokusa: rozszerzyć e2e żeby chodziły bez mocka `DiscordRESTClient`. **Zły kierunek** — e2e powinien testować INTEGRACJĘ warstw, nie pełną pokrycie najniższej warstwy. Właściwy fix: 3 unit testy w `test_discord_client.py` na edge cases (embed wrapping, empty token, 5xx-after-retry). Lekcja: **per-layer coverage gaps fix per-layer testami**, nie poprzez wyższy poziom abstrakcji.
  8. **Process bug nie wystąpił po raz drugi (kontynuacja z M7).** `no-commit-to-branch` hook dalej działa. 0 commitów na master w M8. Closure PR pattern (fresh master → branch → PR) 5. raz pod rząd bez błędu. Tech debt #1 z M5 permanently spłacone.

### Tech debt z M8 (do adresowania post-M8)
- **`discord_client.py` uncovered defensive branches** (42-46 malformed DM JSON, 88-90 httpx TimeoutException/ConnectError, 123 unreachable fall-through). Carry-over z D36, częściowo zaadresowane w D40 (88% line coverage), reszta wymagałaby kompleksowego `httpx.MockTransport` setupu dla edge cases. Niska priority — defensywne branche są krótkie, runtime crash unlikely (każda ma `return None`/`return False` exit).
- **`User.email` model divergence z REST serializer.** Hotfix #134 narrow `email: EmailField(unique=True, null=True, blank=True)` z `# type: ignore[misc]`. REST register serializer dalej wymusza real email. Kontrakt: model permissive ("Discord auto-create może bez email'a"), API restrictive ("regularne signup wymaga email'a"). Świadoma decyzja, ale za 5-10 PR-ów warto przemyśleć custom `User` z `email` opcjonalnym from-the-start zamiast `# type: ignore` workaround.
- **`Character.house` + `guild_membership` `default=""` vs `null=True`** — hotfix #140 narrow obu na `null=True`. CLAUDE.md §5 spec mówił bez `null=True` (tylko `blank=True`). Spec aktualizacja kandydat — albo spec ma rację i fix powinien być na spider side (mapować `"None"` literal na `""`), albo praktyka ma rację (real-world site renders missing house'y jako `None` literal, narrow w modelu jest właściwy). Decyzja: praktyka wygrała w hotfix, ale spec dalej mówi blank=True. Resolve w M-future cleanup PR.
- **Multi-guild M2M tracking model dla DeathAnnouncement** (carry-over z D39 design trade-off). Aktualnie 1 dev guild, `announced_on_discord: bool` wystarczy. Gdy uderzy się problem "guild outage → duplicate sends do healthy guildów next cycle", M-future: konwersja na `DeathAnnouncement(death_event, discord_channel, sent_at, message_id)` z data migration backfill + retroactive backfill dla `announced=True` events gdy admin doda nowy DiscordChannel z niższym threshold.
- **Embed batching / async queued send** (z spec'a "Poza zakresem M8"). Discord allows 10 embeds per message. YAGNI dla pojedynczych deaths aktualnie, ale gdy traffic > 100 deaths/scrape — batch retry queue. Niska priority.
- **`DEATH_LEVEL_THRESHOLD` env var deprecation** — po M8 env var nie używany dla outbound (per-guild threshold w `DiscordChannel` ma precedence). `.env.example` ma go nadal — chore PR żeby usunąć + sanity audit settings na innych unused env varsach.
- **Carry-over z M4/M5/M6/M7 tech debt (wciąż otwarte):**
  - **Branch protection master required `test / Pytest` check** — wciąż brak (8+ M-cykli bez tego gate'a, M8 dorzucił szansę gdy mergowaliśmy 7 PR-ów w 1 dzień). Niska priority ale ślepa plama w CI.
  - **`django_stubs_ext.monkeypatch()` w `base.py`** — `# type: ignore[type-arg]` na admin classes. M8 NIE dorzucił nowego admin'a (DiscordChannelAdmin był z M7, DeathEventAdmin/CharacterAdmin pre-existing). Aktualnie 5× `# type: ignore` w base.py, sweep w jednym chore PR coraz bardziej kuszący po 8 milestones.
  - **Dev runbook dla Celery na Windows** — `docs/dev-runbook.md` skonsolidowany z M3-D17 + M4-D21 + M7-D32 retro fragments. M8 dorzuca runbook fragment: `DISCORD_BOT_TOKEN` musi być real Discord bot token żeby `DiscordRESTClient` nie no-op'ował z ERROR log; test/dev variant `LoggingHandler`/`DeathLoggingHandler` jako settings override gdy nie chcesz hitować Discord.
  - **`celery-types` package** — M8 NIE uderzyło (`@shared_task` w `apps/deaths/tasks.py` nie dorzucił nowych mypy issues). Carry-over do M9+.
  - **TTL retention indexes na Mongo** (z M6 tech debt) — bot logi + notifications handlers logi lecą do `app_logs`, traffic mały, niska priority.

---

## 🎉 Milestone M9 — Dockeryzacja + prod-ready COMPLETED (2026-05-15)
Wszystkie 4 zadania ukończone, milestone gotowy do zamknięcia. Design spec: [`docs/superpowers/specs/2026-05-15-m9-dockerization-design.md`](docs/superpowers/specs/2026-05-15-m9-dockerization-design.md). Implementation plan: [`docs/superpowers/plans/2026-05-15-m9-implementation-plan.md`](docs/superpowers/plans/2026-05-15-m9-implementation-plan.md). Pętla "deployable artifact" zamknięta end-to-end: każdy push na master triggeruje GHA build → push image do `ghcr.io/bgozlinski/tibiantis-scraper:master` + `:sha-<commit>`. Operator może `docker compose pull && docker compose up -d` na VPS-ie z `.env` + 7 services (postgres + redis + mongo + web + celery_worker + celery_beat + discord_bot) + 1 one-shot migrate. M9 nie obejmuje real production deploy (M-future post-Hetzner brainstorm).

### Ukończone (M9)
- ✅ #143 docs(m9): design spec + implementation plan + 4 issue bodies (2026-05-15) — PR [#143](https://github.com/bgozlinski/tibiantis-scraper/pull/143) — squash `8778a0f`
- ✅ #144 [M9-D41] apps/core healthcheck app + /health/ endpoint + 4 tests (2026-05-15) — PR [#148](https://github.com/bgozlinski/tibiantis-scraper/pull/148) — squash `f37d0dd`
- ✅ #145 [M9-D42] Multi-stage Dockerfile + .dockerignore + gunicorn dep (2026-05-15) — PR [#149](https://github.com/bgozlinski/tibiantis-scraper/pull/149) — squash `e342a5c`
- ✅ #146 [M9-D43] Production docker-compose.yml + 7 services + migrate one-shot + .env.example update (2026-05-15) — PR [#150](https://github.com/bgozlinski/tibiantis-scraper/pull/150) — squash `41a2025`
- ✅ #147 [M9-D44] docker.yml CI workflow (build + push ghcr.io) + closure (2026-05-15) — feature PR [#151](https://github.com/bgozlinski/tibiantis-scraper/pull/151) — squash `c26d2f1` + ten closure PR

### Notatki z retro M9 (dopisywane progresywnie)
- **#144 D41 (merge 2026-05-15):** Trzy odstępstwa od spec'a wykryte podczas implementacji + review:
  1. **`REDIS_URL` jako nowy Django setting** — spec §4 zakładał istnieje (`settings.REDIS_URL`), ale w `base.py` był tylko `CELERY_BROKER_URL = env("CELERY_BROKER_URL")`. `.env.example` miał `REDIS_URL=redis://localhost:6379/0` od M0/M5 ale nigdy nie był wczytywany przez Django. Test fail surface'ował to: `AttributeError: 'Settings' object has no attribute 'REDIS_URL'. Did you mean: 'MEDIA_URL'?`. Fix: `REDIS_URL = env("REDIS_URL")` dorzucone do base.py + `os.environ.setdefault("REDIS_URL", "redis://localhost:6379/0")` w stubs.py dla mypy isolated venv.
  2. **`[[tool.mypy.overrides]] redis ignore_missing_imports`** — spec §4.4 zakładał że `redis` jako direct dep z M5 daje stubs w pre-commit mypy hook'u. **Mit:** pre-commit mypy hook ma **isolated venv** z explicit `additional_dependencies` (`.pre-commit-config.yaml:37-46`) — bez `redis` package wcale. Lazy import wewnątrz funkcji NIE pomaga bo mypy widzi imports **statycznie**. Real fix: override w `pyproject.toml` per established pattern (`environ`/`celery`/`discord`). Lekcja: **zawsze dorzucaj mypy override dla nowych libraries których pre-commit mypy hook nie ma w additional_dependencies** — szybciej niż osobny chore PR na `.pre-commit-config.yaml` (CLAUDE.md §12).
  3. **Test location `tests/unit/core/` zamiast `apps/core/tests/`** — issue body D41 spec'd `apps/core/tests/test_health.py` ale established convention to `tests/unit/<app>/test_*.py` (M5-M8 wszystkie inne apps). Plus `apps/core/tests/` byłby w `^apps/` mypy scope (`.pre-commit-config.yaml:36`) → "Untyped decorator makes function untyped" errors bo brak `pytest-django` stubs w mypy hook'u. Fix: move do `tests/unit/core/`. Lekcja: **issue body często nie odzwierciedla project conventions established w innych M-cyklach** — verify before write.
- **#145 D42 (merge 2026-05-15):** Multi-stage Dockerfile zbudowany czysto (no leaked builder layers), ale **image size 480MB > 250MB target z spec'a**. `docker history` audit pokazał breakdown:
  - 232MB `/home/app/.local` (Python deps): scrapy+twisted+lxml ~80MB + strawberry ~30MB + py-cord+aiohttp ~30MB + httpx+celery+pymongo+redis+django+gunicorn+... ~50MB + transitive ~40MB
  - 120MB python:3.13-slim-bookworm base
  - 6MB curl + apt (HEALTHCHECK dep, `--no-install-recommends`)
  - 1.5MB app code

  Brak quick wins — wszystko legit Python deps weight. Flag jako M9 tech debt (kandydaci M-future: split scraper image od web image — scrapy 80MB w `web`/`celery_worker` niepotrzebne dla `web` route'ów; distroless base; multi-arch dla mniejszych ARM64 wheels). **Drugi mini-finding:** issue body D42 smoke command miał tylko 2 env vars (`DJANGO_SECRET_KEY` + `DATABASE_URL`), ale `base.py` wymaga bez default'u również `CELERY_BROKER_URL`, `CELERY_RESULT_BACKEND`, `REDIS_URL` (3 nowe, ostatni z D41). Container crash `ImproperlyConfigured: Set the CELERY_BROKER_URL environment variable`. **Spec lesson na future:** każdy nowy `env("X")` bez default'u w `base.py` wymaga update D-task smoke commands w issue bodies.
- **#146 D43 (merge 2026-05-15):** Najbardziej empiryczny D-task M9 — pięć gotchas złapanych podczas manual smoke (poszły do PR description + 5 lessons w PROGRESS.md tech debt):
  1. **`procps` NIE w `python:3.13-slim-bookworm`** — issue body Pułapka E spec'owała `celery_beat` healthcheck `test -f /tmp/celerybeat.pid && ps -p $(cat $PID)`. `ps` nie installed → zawsze fail → "unhealthy" mimo Beat'a żyje. Fix: `kill -0 $(cat $PID) 2>/dev/null` (POSIX, brak `procps` dependency). Cleaner i równoważne semantycznie.
  2. **Inherited Dockerfile HEALTHCHECK propaguje do compose services bez explicit `healthcheck:` directive** — `discord_bot` świadomie bez healthcheck per spec §3.3, ale dziedziczył `curl /health/` z Dockerfile → zawsze "unhealthy" (bot bez web server). Fix: explicit `healthcheck: { disable: true }` w compose. Spec §3.3 powinien to przewidzieć.
  3. **Compose v2 interpoluje `$VAR` w `.env` values jako shell-style expansion** — `DJANGO_SECRET_KEY` wygenerowany przez `get_random_secret_key()` może zawierać `$` (np. `$v4`, `$zj6`) → Compose treats jako variable expansion → empty substitution + spam warnings `The "v4" variable is not set. Defaulting to a blank string.` + corrupted SECRET_KEY w compose context (choć `env_file:` passthrough do containera jest OK — Django dostaje real wartość). Fix: alphanumeric-only generator `python -c "import string, secrets; print(''.join(secrets.choice(string.ascii_letters + string.digits) for _ in range(50)))"`. Dodane do `.env.example` jako "UWAGA" komentarz.
  4. **MSYS path conversion na Windows Git Bash** — `docker compose exec celery_beat ls /tmp/` → MSYS translates `/tmp/` na `C:/Users/.../AppData/Local/Temp/` PRZED przekazaniem do docker → container nie widzi tego (Windows path). Fix dev: `MSYS_NO_PATHCONV=1` prefix lub `//tmp/` (double-slash escape). Tylko dev gotcha (`docker compose exec`), runtime container Linux path działa OK (compose YAML healthcheck przez Docker daemon, nie shell). Kandydat do `docs/dev-runbook.md` (M9 tech debt).
  5. **Hybrid `image:` + `build:` race condition gdy 5 app services share same image** — wszystkie 5 services (web/celery_*/migrate/discord_bot) próbują równolegle buildować i tagować jako `ghcr.io/.../master` → buildx race: `target celery_beat: failed to solve: image "ghcr.io/.../master": already exists`. Fix: `docker compose build` PRZED `up -d --no-build` (single build, no race) zamiast `up -d` z auto-build. Issue body Pułapka B miało hint, ale nie wprost jako workflow.

  Plus 3 problemy nie-bugowe ale czas-pożeracze: SECRET_KEY blank po `cp .env.example .env` bez fill (`ImproperlyConfigured`); `migrate` exit 1 first run (DB nie healthy yet, fixed przez `depends_on: postgres: condition: service_healthy`); `git mv` na untracked file fail (musisz `mv` regular + manual `git rm -r` source dir). Wszystko legit user error / dev workflow trivia, ale 30-60 minut łącznie.

  **Manual smoke final state:** 7 services Up (6 healthy + 1 Up bez healthcheck = discord_bot) + migrate Exited (0). `curl /health/` → 200 + `{"db": "ok", "redis": "ok"}`. `docker compose ps` clean. Full stack up ≤90s.

- **#147 D44 (merge 2026-05-15, ten PR):** Feature PR + closure PR (M5-D27 + M6-D30 + M7-D35 + M8-D40 pattern repeat 6. raz pod rząd). docker.yml workflow YAML to **verbatim copy** ze spec'a §13.2 + issue body — zero modyfikacji. Pierwsze uruchomienie PR build mode (`push: false`, build-only verification) zielone. Po master merge → push mode kicked in, `Login Succeeded!` + push `ghcr.io/bgozlinski/tibiantis-scraper:master` + `:sha-c26d2f1` confirmed w workflow logs.

  **Pułapka G z issue body złapana pre-flight:** `gh api repos/.../actions/permissions/workflow` pokazał `default_workflow_permissions: "read"`. Per GitHub Actions docs workflow-level `permissions: { packages: write }` *teoretycznie* override'uje default w górę, ale issue body flag'ował jako known gotcha. Flip na `"write"` via `gh api -X PUT` przed merge — zero risk pierwszego push'u. **Lekcja:** pre-flight `gh api ... actions/permissions/workflow` przy każdym nowym workflow który push'uje do registry. Mała komenda, bigger pewność.

  **Closure PR od fresh master** (Pułapka H, M1-D8 + M5-D27 + M6-D30 + M7-D35 + M8-D40 lekcja repeat **6. raz**) — `git checkout master && git pull && git checkout -b docs/close-m9-dockerization` PRZED edycją PROGRESS.md. Wzorzec utrwala się na trwałe.

### Definition of Done M9 (ze spec'a §8) — ✅ wszystkie domknięte
- [x] **4 D-tasków zamkniętych** (#144 D41 + #145 D42 + #146 D43 + #147 D44) + closure PR.
- [x] **`apps/core/health/`** — view (`health_check`), urls (`health/`), 4 tests, **100% coverage** (target ≥95%).
- [x] **`config/urls.py`** dorzuca `path("health/", include("apps.core.urls"))`.
- [x] **`config/settings/base.py`** dorzuca `"apps.core"` do `LOCAL_APPS` + `REDIS_URL = env("REDIS_URL")` (nowy setting).
- [x] **`config/settings/stubs.py`** dorzuca `os.environ.setdefault("REDIS_URL", "redis://localhost:6379/0")` dla mypy isolated venv.
- [x] **`pyproject.toml`** dorzuca `gunicorn (>=23.0,<24.0)` jako prod dependency + `[[tool.mypy.overrides]] redis ignore_missing_imports` (lazy import nie pomaga statycznemu mypy).
- [x] **`Dockerfile`** multi-stage (builder + runtime), non-root user `app` (UID 1000), `python:3.13-slim-bookworm` base, HEALTHCHECK via curl `/health/`.
- [x] **`.dockerignore`** excluding `.git`, `.venv`, `__pycache__`, `tests/`, `docs/`, `.env*` (z `!.env.example`), `.idea/`, `.claude/`, `docker-compose*.yml`, `.github/`, `*.md` (z `!README.md`).
- [x] **`docker-compose.yml`** (prod) z 7 services + 1 one-shot `migrate` + healthchecki (z `kill -0` fix dla `celery_beat` i `disable: true` dla `discord_bot`) + `depends_on` chain z `service_healthy` + `service_completed_successfully`.
- [x] **`.env.example`** zaktualizowane na Docker DNS hostnames (`postgres`/`redis`/`mongo`) + "Operator MUSI wypełnić" comments na 3 sekretach + UWAGA o `$` w SECRET_KEY (Compose v2 interpolation gotcha).
- [x] **`.github/workflows/docker.yml`** — build na PR + push do `ghcr.io/bgozlinski/tibiantis-scraper` na master/tag, z `docker/metadata-action@v5` tags (branch/semver/sha-short), `cache-from/to: type=gha,mode=max`, `concurrency: cancel-in-progress`. Workflow zielony na pierwszym PR + zielony na master push (`Login Succeeded!` + image visible w ghcr.io).
- [x] **Pre-commit + CI lint + test zielone** dla wszystkich 5 PR-ów (#143 spec/plan + #148 D41 + #149 D42 + #150 D43 + #151 D44 feat).
- [x] **Coverage `apps/core/health/*` 100%** (target ≥95%).
- [x] **PROGRESS.md** rozszerzony o sekcję M9 z retro per Issue (ten dokument).
- [x] **Manual smoke** udokumentowany w closure PR description (5 punktów z §9).
- [ ] **Milestone M9 zamknięty na GitHub** — TODO ostatni krok po merge tego PR-a (`gh api -X PATCH repos/bgozlinski/tibiantis-scraper/milestones/9 -f state=closed`).

### Podsumowanie M9 (2026-05-15, ~1 dzień vs 4 dni z budżetu — wyprzedzenie 3 dni)
- **4 Issues (#144-#147) + 5 PR-ów (4 feat + 1 spec/plan) + closure PR w 1 dniu.** Strict chain D41 → D42 → D43 → D44 zachowany. Zero hotfixów (po M8 = 2 hotfixy, M9 czyściej).
- **DoD M9 spełnione** — deployable artifact gotowy: `ghcr.io/bgozlinski/tibiantis-scraper:master` + `:sha-c26d2f1` w registry, pull-able lokalnie. Full stack `docker compose up -d` startuje 7 services healthy w ≤90s. Pierwszy multi-service compose w projekcie (M0-M2 dev compose miał tylko 3 DB services).
- **Najwartościowsze lekcje M9:**
  1. **`docker compose build` PRZED `up -d --no-build` gdy N services share same image** (D43 race condition lekcja #5). Hybrid `image:` + `build:` pattern wymaga single-shot build orchestration, inaczej buildx race na shared tag. **CLAUDE.md addendum kandydat:** w prod compose runbook fragment dorzucić "always build first explicitly when N services share image".
  2. **Pre-commit mypy hook isolated venv** != poetry dev venv (D41 lekcja #2). Lazy import wewnątrz funkcji NIE pomaga statycznemu mypy. Dwa fixy: dorzucić library do `additional_dependencies` w `.pre-commit-config.yaml` (osobny PR per CLAUDE.md §12) lub `[[tool.mypy.overrides]] module = ["X"] ignore_missing_imports = true` w `pyproject.toml` (same PR jako kod). Override jest established pattern dla 4 libraries już (`environ`/`celery`/`discord`/`redis`).
  3. **Compose v2 `$VAR` interpolation w `.env` values** (D43 lekcja #3). Generated secrets z `$` chars (Django `get_random_secret_key`) corrupt w compose context. Fix: alphanumeric-only generator. **CLAUDE.md §10 addendum kandydat:** dorzucić sekcję ".env conventions: avoid `$` in values, use alphanumeric-only generator dla SECRET_KEY".
  4. **Issue body spec'y często nie odzwierciedlają project conventions** (D41 lekcja #3 + D42 spec'owany ps -p w healthcheck). Spec to drafted top-down, project conventions emerge bottom-up przez M-cykle. **Lekcja procesowa:** weryfikuj patch targets / file locations / library availability **przed** kodowaniem (grep, ls). 5-10 min weryfikacji oszczędza godziny re-work.
  5. **Pre-flight repo permission checks** (D44 lekcja Pułapka G). Workflow który push'uje do `ghcr.io` wymaga `default_workflow_permissions: "write"` na repo. `gh api ... actions/permissions/workflow` 30s check uniknął surprise "Permission denied" po pierwszym master merge. **Generalizable pattern:** dla każdego workflow który robi side effect (push do registry, deploy SSH, mass-create issues), pre-flight `gh api` check uprawnień.
  6. **MSYS path translation gotcha na Windows Git Bash** (D43 lekcja #4). `docker compose exec ... /tmp/...` → MSYS translates `/tmp/` na Windows TMP. Tylko dev gotcha, runtime OK. **CLAUDE.md / `docs/dev-runbook.md` addendum:** sekcja "Windows + Git Bash + Docker: `MSYS_NO_PATHCONV=1` dla container path arguments".
  7. **Inherited Dockerfile HEALTHCHECK propaguje do all compose services** (D43 lekcja #2). Każdy service który NIE potrzebuje base HEALTHCHECK musi mieć explicit `healthcheck: { disable: true }`. Inaczej "unhealthy" mimo proces żyje. Spec §3.3 dla `discord_bot` powiedział "brak healthcheck" — interpretation: "skip" ≠ "disable inherited". Subtle.
  8. **`procps` NIE w slim Python image** (D43 lekcja #1). `ps -p PID` w healthcheck zawsze fail. Use `kill -0 PID 2>/dev/null` (POSIX). Generalizable: `slim`/`alpine` images strip many utils — verify dostępność każdego command'a używanego w healthcheck.

### Tech debt z M9 (do adresowania post-M9 / M-future)
- **Image size 480MB** (D42 finding) — legit Python deps weight, brak quick wins. M-future kandydaci na slimming:
  1. Split scraper image od web image (`web`/`celery_worker` road `gunicorn` + Django, ale `scrapers/` 80MB nie potrzebny dla gunicorn worker'a — osobny `tibiantis-scraper-scraper:master` image dla `celery_worker`)
  2. Distroless base (`gcr.io/distroless/python3-debian12` zamiast `python:3.13-slim-bookworm` — mniejszy + mniejsza attack surface)
  3. Multi-arch builds (`linux/amd64,linux/arm64` w `docker/build-push-action@v6 platforms:`) — mniejsze ARM64 wheels dla `aiohttp`/`lxml`/`scrapy`. M-future jeśli deploy na Raspberry Pi / M-class Macs.
- **Static files w prod** (collectstatic + WhiteNoise) — M9 świadomie pomijał. Django admin CSS/JS broken przez przeglądarkę (SSH tunnel workflow dla dev). **M10 Hardening kandydat:** dorzucić WhiteNoise middleware + `RUN python manage.py collectstatic --noinput` w Dockerfile builder stage + `STATIC_ROOT` config.
- **Image scanning (Trivy/Snyk/Docker Scout)** — kandydat na M10 Hardening, dorzucić step do `docker.yml` workflow przed push (scan + fail na high/critical CVEs).
- **GHA deploy job (SSH na Hetzner + `docker compose pull && up -d`)** — wymaga real Hetzner VM (post-M9 brainstorm). Secrets: `HETZNER_HOST`, `HETZNER_USER`, `HETZNER_SSH_KEY`. Trigger: po build + push w `docker.yml`, dorzuć `deploy:` job z `needs: build` + `if: github.ref == 'refs/heads/master'`.
- **Multi-arch builds** — `linux/amd64,linux/arm64` w build-push-action `platforms:`. YAGNI dopóki single deploy target (`amd64` na typowych VPS-ach). M-future jeśli ARM64 deploy.
- **Docker secrets / Vault / SOPS** — env_file: .env wystarcza dla single-host. M-future gdy multi-node deploy. Plain `.env` jest acceptable dla single-VM Hetzner.
- **Semver tagging release flow** — M9 start z `:master` rolling. M-future: `git tag v1.0.0 && git push --tags` → image jako `:1.0.0` + audit + rollback przez registry.
- **nginx + TLS reverse proxy + Let's Encrypt** — YAGNI per execution plan §6.2 + spec §1. Post-Hetzner: kandydat na Caddy w docker-compose (auto-Let's Encrypt) lub nginx-proxy + acme-companion. Wymaga real DNS subdomain + Hetzner Cloud Firewall config.
- **Real production deploy** — out of scope M9 (spec §1 explicit). **Następny milestone** Hetzner brainstorm: DNS + Let's Encrypt + first deploy runbook (`docs/deploy-runbook.md`) + monitoring (Grafana/Netdata/Uptime Kuma) + backup strategy (postgres dump → Backblaze B2 / Wasabi) + fail2ban + UFW.
- **`discord_bot` healthcheck** — aktualnie `disable: true`. M-future: bot zapisuje heartbeat do Mongo co 60s, healthcheck script odczytuje recent timestamp. Albo: py-cord exposed healthcheck HTTP endpoint (mini aiohttp server).
- **`celery_beat` pidfile race** — gdy beat restart'uje, pidfile może być stale. M-future: dorzucić `healthcheck` z `celery -A config status` (per-worker ping) zamiast pidfile check. Lub: cron-style monitoring outside container.
- **Carry-over z M4/M5/M6/M7/M8 tech debt (wciąż otwarte):**
  - **Branch protection master required `test/Pytest` check** — wciąż brak (9+ M-cykli bez tego gate'a). M9 dorzucił szansę gdy mergowaliśmy 6 PR-ów (5 feat + closure) w 1 dzień. Niska priority ale ślepa plama w CI.
  - **`django_stubs_ext.monkeypatch()` w `base.py`** — `# type: ignore[type-arg]` na 5 admin classes. M9 NIE dorzucił nowego admin'a (`apps/core/` nie ma modelu/admin'a). Sweep w jednym chore PR coraz bardziej kuszący po 9 milestones.
  - **Dev runbook dla Celery na Windows** — `docs/dev-runbook.md` skonsolidowany z M3-D17 + M4-D21 + M7-D32 + M8 retro fragments. **M9 dorzuca 2 fragmenty:**
    1. `MSYS_NO_PATHCONV=1` dla `docker compose exec ... /tmp/...` na Windows Git Bash
    2. `docker compose build` PRZED `up -d --no-build` gdy N services share same image (race condition workaround)
  - **`celery-types` package** — M9 NIE uderzyło (workflow YAML + Dockerfile + compose to nie kod Python z `@shared_task`). Carry-over do M-future.
  - **TTL retention indexes na Mongo** (z M6 tech debt) — niska priority, traffic mały.
  - **`User.email` model divergence** (M8 hotfix #134) — model permissive (`null=True, blank=True`), REST serializer restrictive. Świadoma decyzja, M9 nie zmienia. Kandydat na custom User design w M-future.
  - **`Character.house`/`guild_membership` `null=True` vs spec `blank=True`** (M8 hotfix #140) — praktyka vs spec. Resolve w M-future cleanup PR.
